@AGENTS.md

# Claude Code

Общие правила для всех агентов — в `AGENTS.md` (импортирован выше); править правила
только там. Для Claude сверх этого: браузер — встроенная панель Browser, свои вкладки
закрывать; визуальные проверки — Playwright из свежего профиля, скрипты класть внутрь
проекта (иначе не резолвятся `playwright`/`sharp`) и удалять после.
