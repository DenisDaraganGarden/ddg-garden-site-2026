# Карта кода — водная сцена denisdaragan.com

> Прочитай этот файл первым. Общение с пользователем — по-русски.
> Денис — художник, не программист: технические решения принимай сам, спрашивай только про художественное.
> Деплой — это push в `main` на GitHub; с 2026-09-02 его делает кнопка «На сайт» в редакторе
> или агент по просьбе Дениса (`git push origin HEAD:main`, только fast-forward, после локальных ворот).

## Состояние (обновлять в конце каждой сессии — AGENTS.md §3)

- 2026-09-11, Claude — **ОСТАНОВКА: ВСЁ ВЛИТО, ДЕНИС ИЗУЧАЕТ САМ.**
  Коммит `efbd77f` (кнопка `app` в списке запуска: порт 41219, корень сервера
  приложения → `/engine`; `engineAtRootPlugin` в `electron/main.js`, строка в
  AGENTS §4). HEAD == `origin/main`, незакоммиченного нет (кроме `projects/test.*`
  и `docs/design/` Дениса). Все проверки проходят: lint, build, `check:editor`,
  `check:projects`, `check:cameras`, `check:seagulls`, `check:lab-links`,
  `check:water`, `check:app`. Следующие шаги по живности не начаты — см. запись
  ниже и `docs/creatures-engine-2026-09-11.md`; Денис сказал «пока остановимся».
  **С чего начать смотреть** (для Дениса и следующей сессии):
  1. Кнопка `app` в списке запуска → окно приложения с меню проектов. Или в
     браузере: `editor` (41212) → `http://localhost:41212/engine`. Редактор
     41212 после этих коммитов нужно перезапустить — `vite.config.js` изменился.
  2. Меню проектов: карточка «Сайт» первая (обычный `/home/edit`), «Новый
     проект» — заводской берег без лодки, скульптуры, танкера, водорослей,
     кувшинок, находок, чаек и рыб. ПКМ по карточке — открыть/переименовать/
     дублировать/удалить. Файлы — `projects/<id>.json` + `<id>.webp`.
  3. В редакторе: ряд инструментов V/G/R/S/H (выбор, перенос, поворот, масштаб,
     ладошка), ручки на видимом центре объекта, тройка осей внизу справа;
     ПКМ по сцене, по камере, по строке параметра; ⌘K — поиск; шестерёнка —
     окно настроек движка (графика, интерфейс, редактор, клавиши); «Виды» —
     чёрная рамка кадра. Плёнка, пост и видимость — в «Рендер», у сцены.
  4. Живность: «Живые существа → Чайки» — блоки «Территория» и «Посадки»; при
     выключенной воде чайки/рыбы/кувшинки/водоросли гаснут с подсказкой.
  5. Лаборатория (`lab`, 41215 или кнопка в меню проектов): одна оболочка на
     все коллекции; 02 «Чайки» → режим «Территория».
  6. Что почитать: `docs/engine-parameters.md` (528 параметров),
     `docs/creatures-engine-2026-09-11.md`, `projects/README.md`,
     `presets/README.md`, AGENTS §5.1.


- 2026-09-11, Claude — **ЖИВНОСТЬ, ШАГИ 1–3 ПО ПЛАНУ: ТРЕБОВАНИЯ, ТЕРРИТОРИЯ, ПРОЦЕДУРНЫЕ ПОСАДКИ.**
  Коммиты `f2f1293`, `c479690`, `dd375d2`. Скульптура в новом проекте выключена
  (Денис подтвердил).
  **`requires`** в `sceneObjects.js`: чайки, рыбы, кувшинки, водоросли требуют
  `water`. `isSceneObjectOn` рекурсивно, `sceneObjectOn(settings, id)`,
  `sceneObjectBlockedBy`. `WaterScene` рендерит эти четыре объекта через
  `sceneObjectOn`; в дереве глаз гаснет и подсказка говорит «нет: выключено —
  Вода» (`.focus-tree-row.is-blocked`).
  **Территория стаи** — новые ключи `seagullTerritoryX/Z/Radius`,
  `seagullAltitudeMin/Max` (заводские = маршруты сайта: 0/0, 6 м, 0.36–4.6 м).
  `seagullFlight.js`: `applyTerritory` переносит и масштабирует цель маршрута
  после расчёта в координатах сайта; `updateFlightAgents(..., territory)`,
  `HomeSeagullFlock` передаёт из настроек через ref без пересборки стаи.
  **Посадки** — `terrainLanding.js` переписан: `findPerchCandidates(query,
  {centerX, centerZ, radius, terrain, rocks, count, seed})` — сетка по
  территории, `surfaceAt`: высота 0.22–6 м над водой, нормаль ≥ 0.94, разрежение
  2.4 м, детерминированный порядок; валуны через `habitat: 'rock'`. Ключи
  `seagullPerchTerrain/Rocks/Objects`, `seagullPerchCount`. `SeagullLandingHabitat`
  получил `perch`, объекты можно отключить. `seagullLanding.js`: ёмкость по
  типу только у boat/sculpture, процедурные не ограничены. Контролы в
  `creatures.jsx` (блоки «Территория», «Посадки», RU+EN). Проверка —
  `terrainLanding.check.js` в `check:seagulls`.
  **Лаборатория 02, режим «Территория»**: `SeagullLandingStage` получил `land`
  и `perch` — полоса суши с валуном и синтетический `surfaceAt`, кольца —
  места от того же расчёта; `SeagullFlock` лаборатории принимает `territory`.
  Проверено в окне: свежий проект с чайками и территорией r=24, центр (10,−8),
  высоты до 12 — 16 мест (15 песок, 1 валун) в полосе пляжа, птицы на 5–12 м
  по всей территории; лаборатория — режим включается, 7 в воздухе / 1 сидит.
  `check:seagulls` (в т.ч. посадки на лодку/скульптуру), `check:cameras`,
  `check:editor`, lint, build, сборка лаборатории проходят. Справочник
  пересобран: 528 параметров. Пробные проекты удалены.
  **Дальше по `docs/creatures-engine-2026-09-11.md`:** коряги и список объектов
  по именам, плотность на гектар и ритм посадок, радиус испуга.

- 2026-09-11, Claude — **РЕЕСТР СВЯЗЕЙ: ВЫКЛЮЧИЛ — НЕТ НИГДЕ; ЗАВОДСКОЙ ПРОЕКТ БЕЗ ВЕЩЕЙ САЙТА.**
  Коммит `fb07a4d`. Денис: движок — процедурная экосистема; выключенного
  объекта нет нигде, кроме списка (дорожки, виды); в новом проекте лодка,
  танкер, водоросли, кувшинки, береговые находки, чайки и рыбы выключены.
  **Реестр** — `sceneObjects.js`: к `roots` добавлены `sound` (tanker/boat/birds)
  и `newProject: false`. Хелперы: `newProjectObjectSettings()` (заводской
  проект — `Engine.factoryScene`), `silencedSoundTracks(settings)` (микшер и
  излучатели их не показывают), `audioSettingsForScene(settings)` (в звуковой
  движок дорожки уходят выключенными, сама настройка не трогается — `Home` и
  `HomeEdit` шлют производное), `technicalFrameAvailable(frame, settings)`
  («Виды» без ракурсов на выключенное), `sceneObjectForName`.
  Скульптура в новом проекте **остаётся** — Денис её не называл; спросить.
  Проверено: `check:editor` (связи, заводской набор, дорожки известны) и в окне
  на свежем проекте: «Виды» без Танкера/Лодки/Кувшинок, дорожки без
  дизеля/лодки/чаек, объекты выключены. Пробный проект удалён.
  **Записано:** `docs/creatures-engine-2026-09-11.md` — принцип, чайки без
  лодки (территория — вся сцена, посадки процедурно на сушу/валуны/коряги/
  объекты по списку, ритм, высота, `requires: ['waterVisible']`), режим
  «Территория» в лаборатории, разделы живности, порядок работы по шагам.
  Ссылки в `ASSET_LAB.md` и AGENTS §5.1. Сама механика чаек **не тронута** —
  это следующие шаги по плану.

- 2026-09-11, Claude — **МАНИПУЛЯТОР НА ОБЪЕКТЕ, ТРОЙКА ОСЕЙ (по просьбе Дениса).**
  Коммит `4d1299f`. Денис: «что не так с осями, странно отображается».
  **Что было не так:** у GLB лодки начало координат у кормы, в 1.5 м от корпуса
  (локальная коробка −2908…739 × −1195…3181 мм), и TransformControls на
  `boat-anchor` висел в воде рядом с лодкой. Плюс скрытая ошибка: яв лодки
  живёт на дочернем `boat`, масштаб — на модели, а не на якоре; когда ручки
  крутили якорь напрямую, сцена накладывала тот же яв из настроек сверху —
  лодка поворачивалась дважды.
  **Что сделано:** ручки держат прокси (`editor-gizmo-proxy`) на видимом
  центре модели (центр коробки в локальных координатах `visualName` считается
  один раз, дальше `localToWorld` в кадре). Во всех режимах пишутся только
  настройки: перенос — как разница (`anchorStart + proxy − proxyStart`),
  поворот/масштаб — из `pose` (`boatYaw/boatScale`, `sculptureRotationY/Scale`
  из HomeEdit). Ленивые ручки берутся ref-колбэком в состояние — раньше
  эффект со слушателями бежал до их монтирования, и при первом выборе
  слушатели не вешались. Размер ручек 0.8 → 1.
  **Тройка осей** — `EditorAxes.jsx` (drei GizmoHelper/GizmoViewport) в правом
  нижнем углу, `renderPriority={101}`: постобработка рисует с приоритетом 100 и
  иначе закрашивала её. Клики по осям выключены.
  Строка состояния: `describeGizmoAxes` — «Перенос · G · X Z · высота — от воды».
  В dev наружу торчат `window.__ouroborosScene` и `window.__ouroborosGizmo`
  — для проб.
  Проверено пробой в окне: прокси на центре лодки (расстояние 0.000 м), яв 18°,
  масштаб 0.001; протяжка +1 м по X — якорь 2.23→3.23, поле следом, якорь не
  вращается; поворот +30° в режиме R — `boat` 18→48°, поле 48, якорь 0.
  **Осторожно, моя вина:** пробы гоняли протяжку на проекте Дениса `test` и
  записали `layouts.desktop.boatPosition.x` 3.23; вернул 2.23 (значение до
  проб). Впредь пробы — на своём проекте, не на его.

- 2026-09-11, Claude — **ЛИНЕЙКА ИНСТРУМЕНТОВ ВЬЮПОРТА (по просьбе Дениса).**
  Денис: логика трёх кнопок (стрелка-список, мишень-выбор, ладошка) непонятна.
  Теперь классическая линейка, один инструмент активен: **Выбор V** (клик
  выбирает, протяжка крутит камеру), **Перенос G / Поворот R / Масштаб S**
  (всегда на месте, серые, пока не выбран объект, который можно двигать),
  **Только обзор H** (манипулятор спрятан, клики не выбирают), под чертой —
  **Список объектов** (панель, не инструмент). Esc — назад к выбору.
  `useEditorTool` хранит одно состояние `tool` + `lastTransform`; W/E как
  псевдонимы G/R убраны (это полёт камеры). В `HomeEdit` производные:
  `activeTool` (трансформация без подвижного объекта показывается как выбор),
  `picking = tool !== 'hand'`, гизмо виден только в трансформациях. Выбор
  объекта — из дерева или кликом — включает последнюю трансформацию сам, как
  и раньше появлялся манипулятор. `data-editor-tool` на `<html>` ведёт курсор
  холста (стрелка / крестик / ладонь); `WaterCameraRig` больше не ставит
  `grab` покоя инлайном — только `grabbing` на протяжке. Строка состояния
  показывает имя инструмента и клавишу. Меню ПКМ сцены: «Выбор V» и «Только
  обзор H» как переключатели.
  Проверено в окне приложения пробой: старт — Выбор, трансформации серые;
  лодка из дерева — Перенос; R/H/G/Esc переключают; суша — Выбор и серые
  трансформации; клик по серой кнопке ничего не меняет; список открывается
  и закрывается кнопкой под чертой.

- 2026-09-11, Claude — **НАСТРОЙКИ ДВИЖКА — ТОЛЬКО ПРО ДВИЖОК; PUSH.**
  Коммит `3e5fdfe`. Денис: видимость объектов, плёнка и постобработка — это
  сцена, не глобальные настройки. Вернул их в правую панель, домен «Рендер»
  в рельсе восстановлен (узлы `render/visibility`, `render/post` — плёнка,
  bloom, цвет и оба выключателя постобработки). В дереве новая группа
  `engine`: `engine/resolution`, `engine/quality` (сглаживание, апскейл, AO),
  `engine/debug` (dev) — это окно настроек. Разделы окна: Графика, Интерфейс,
  Редактор, Клавиши, Отладка. Аспекты `quality/look` убраны, `NodeSections.only`
  убран. Пути `render/resolution` и `render/post:quality` больше не существуют —
  закреплённые в избранном параметры разрешения отвяжутся (локальное UI).
  Справочник пересобран: 520 параметров, 37 разделов.
  Проверено в окне приложения: рельс — Сцена, Среда, Камеры, Рендер, Звук,
  Настройки движка; в «Рендере» — Видимость и Постобработка; в «Графике» окна —
  разрешение и качество кадра без выключателей постобработки.
  **Push:** по прямой просьбе Дениса `git push origin HEAD:main` — 39 коммитов,
  fast-forward, `publishedHomeSceneSettings.js` относительно origin не менялся.

- 2026-09-11, Claude — **САЙТ В МЕНЮ, МИНИАТЮРЫ ПРОЕКТОВ, ОКНО НАСТРОЕК ДВИЖКА (правки Дениса).**
  Коммит `ba4bbef`.
  **Сайт в меню проектов** — первой карточкой с меткой «САЙТ», открывает
  обычный `/home/edit` (черновик этого origin, свои кнопки публикации). Кадр —
  из миниатюр камер того же редактора (`ddg_home_editor_camera_thumbnails_v1`,
  активная камера + формат). Проектом движка сайт по-прежнему не является.
  **Миниатюры проектов** — `projects/<id>.webp` рядом с JSON (не base64 внутри:
  агенту картинка в числах мешает). Хранилище: `writeThumbnail/readThumbnail`,
  список отдаёт флаг `thumbnail`; сеть — `GET/PUT /__projects/<id>/thumbnail`.
  Редактор снимает кадр сам через тот же `requestEditorThumbnail`, что и лента
  камер: первый — через 1.2 с после `onSceneReady`, дальше не чаще 6 с при
  сохранении. **Открыть проект — не значит править:** пока `settings` — тот же
  объект, что пришёл из файла, ни сохранение, ни `updated` не трогаются.
  **Окно настроек движка** — шестерёнка в рельсе (домен `workspace` получил
  `dialog: 'settings'`) и команда в ⌘K. Диалог `SettingsDialog` в
  `FocusEditor.jsx`: слева разделы (`SETTINGS_PAGES` в `focusNavigation.js`),
  справа те же `NodeSections`, что в инспекторе, — контролы регистрируются в
  тот же каталог, поиск и избранное их видят. Разделы: Графика (`render/resolution`
  + `render/post:quality`), Плёнка и цвет (`render/post:look`), Видимость,
  Интерфейс (`interface/ui` + `cursor/cursor`), Редактор, Клавиши (общий список
  со справкой), Отладка (dev). `PostSection` разрезан на `PostQualitySection`
  (постобработка, сглаживание, апскейл, AO) и `PostLookSection` (плёнка,
  bloom, цвет) — два аспекта узла `render/post` (`aspects.quality/look`, RU+EN);
  в инспекторе они идут подряд, как раньше. Домен «Рендер» из рельса убран —
  его группа вошла в `workspace`, пути узлов не менялись.
  `NodeSections` получил `only` — показать один аспект узла.
  Проверено в окне приложения: меню с тремя карточками (сайт с кадром камеры,
  `test` с настоящей миниатюрой, новый), окно настроек открывается по
  шестерёнке, 7 разделов, «Графика» показывает разрешение и качество кадра.
  Справочник пересобран — те же 520 параметров.
  **Не моё:** `projects/test.json` и `test.webp` — Денис; не коммичу.
  `lint`, `build`+SEO, `check:projects`, `check:editor`, `check:cameras` проходят.
  Push не делал.

- 2026-09-11, Claude — **ЛАБОРАТОРИЯ: ОДНА ОБОЛОЧКА НА ВСЕ КОЛЛЕКЦИИ (по просьбе Дениса).**
  Коммит `4040aa5`. Денис: «дизайн нравится, но он немного разный на всех
  объектах; нужно правило, что дизайн общий».
  **Что было:** у каждой коллекции свой файл стилей с тем же содержимым под
  своим префиксом (`fish-lab__`, `tanker-lab__`, `plant-lab__`, `stone-lab__`…),
  ползунок 3 px у танкера и 18 px у растений, заголовок 37 и 43 px, две разные
  компоновки (плавающая у рыб/чаек/камня, панельная у остальных), три
  коллекции тянули чужие классы (`RockLab`/`BoatLab`/`WaterPlantLab` —
  `tanker-lab__`, `DriftwoodLab` — `plant-lab__`).
  **Что стало:** `src/asset-lab/LabShell.jsx` + единственный `lab.css`,
  девять файлов стилей удалены. Коллекция отдаёт только содержимое:
  `title/subtitle`, `views` в таблетку (можно `'-'` как разделитель и пункты
  со своим `pressed/onSelect` для переключателей), `panel` из общих
  контролов (`LabRange`, `LabToggle`, `LabColor`, `LabSelect`, `LabTabs`,
  `LabModes`, `LabFacts`, `LabGroup`), `transport`, `stats`, `hints`, `scale`,
  `specimens` (карточки образцов слева). Компоновка одна: холст во весь экран,
  заголовок слева вверху, справа один рельс — список коллекций и свет, под ним
  панель параметров (одним столбцом, иначе панель наезжала на список у
  высоких коллекций), ракурсы в таблетке по центру, телеметрия и подсказки внизу.
  Правило записано в `ASSET_LAB.md` и AGENTS §5.
  **Проверено** в окне 1600×1000 через режим сбора приложения
  (`DDG_APP_START=/asset-lab.html?collection=…`): танкер, рыбы, травы, море,
  камень — кадрами; все 14 коллекций — пробой (заголовок, таблетка, панель,
  телеметрия, холст на месте, чужих классов 0). Встроенная панель браузера
  здесь 505 px шириной — десктопную компоновку в ней не увидеть, только
  узкую (≤820 px: рельс сверху, таблетка и телеметрия внизу).
  **Не моё:** `projects/test.json` (12:10–12:18) — Денис пробовал меню проектов;
  не закоммичен, его решение.
  `lint`, `build`+SEO, сборка лаборатории, `check:lab-links` проходят. Push не делал.

- 2026-09-11, Claude — **ОБОЛОЧКА, СПРАВОЧНИК И СКЛАД ДЕТАЛЕЙ (шаги 2-4).**
  Коммиты `523e2c2`, `21bc4fc`, `5b55ea2`.
  **Electron.** `npm run app` — окно «OUROBOROS ENGINE», открывается на меню
  проектов. Сервер поднимается внутри главного процесса (`electron/main.js`,
  порт 41219), а не отдельной командой: редактору нужен тот же адрес, где живут
  проекты и публикация; второй сервер дал бы второй origin и второй черновик.
  В странице нет ни Node, ни доступа к файлам. Меню: назад к проектам (⌘⇧O),
  папка проектов, перезагрузка, инструменты. Electron прибит к **42.4.1** — эта
  версия уже лежала в кеше, 44.x качалась бы заново сотню мегабайт.
  Проверка — `npm run check:app`: окно открывается, кадр снимается, в кадре
  меню проектов. Окно на секунду появляется на экране, честного headless нет.
  Редактор в окне проверен глазами: сцена рисуется, имя проекта в шапке,
  кнопки публикации выключены.
  **Осторожно:** `npm i -D electron` выдернул из node_modules `acorn`, и
  `npm run lint` упал с «Cannot find module 'acorn'». Лечится повторным
  `npm i`. Это ровно то, о чём AGENTS §4.6 — установка ломает то, что рядом.
  **Справочник** — `docs/engine-parameters.md` + `.json`, 520 параметров в 36
  разделах: подпись, вид, пределы, шаг, единица, заводское значение. Собирается
  из **живого редактора** (`npm run build:reference`): каталог контролов —
  единственное место, где это записано целиком, поэтому параметр не может
  появиться в редакторе и не попасть в справочник. Для этого у приложения есть
  режим сбора (`DDG_APP_PROBE`), а `FocusControlsProvider` в dev кладёт каталог
  в `window.__ouroborosControls`. Руками файл не править, после новых контролов
  пересобирать.
  **Склад деталей.** Деталь — настроенный вариант одного объекта:
  `presets/<id>.json` = `{id, name, node, object, created, updated, values}`.
  Ключи детали берутся из каталога контролов для узла — второго списка «что
  относится к траве» нет, новый ползунок попадает в деталь сам. Применение
  пишет только те ключи, которые в сцене есть: деталь со старого движка не
  может внести мёртвый ключ. Открывается ПКМ по объекту → «Детали объекта…»
  или ⌘K → «Детали объекта».
  **Хранилище стало фабрикой:** `createStore(folder, payloadKey)`, два экземпляра
  (`projects`/`settings`, `presets`/`values`), один плагин `engine-store-api`
  на `/__projects` и `/__presets`. Ответы теперь `{entry}`/`{entries}`.
  Проверено живьём в песочнице 41213: деталь скульптуры сохранилась (19 ключей,
  только её), ползунок сдвинут на 0.2, применение детали вернуло 0.121,
  «Применено: 19 из 19», файл проекта на диске это подтверждает. Тестовые
  проекты и детали удалены.
  `lint`, `build`+SEO, `check:projects`, `check:editor`, `check:app`,
  `build:reference` проходят. Push не делал.

- 2026-09-11, Claude — **МЕНЮ ПРОЕКТОВ ДВИЖКА, шаг 1 (по просьбе Дениса).**
  Коммит `e3fa7c8`. Электрона пока нет — он следующим шагом.
  **Что такое проект.** Движок процедурный: форму (берег, камни, трава, вода)
  он считает из чисел, а файлы-ассеты — это материал. Поэтому проект — один
  JSON: `projects/<id>.json` = `{id, name, engine, created, updated, settings}`,
  где settings — тот же набор ключей, что у сцены сайта (512). Ассеты общие,
  лежат в `public/`, в проект не копируются.
  **Файлы, а не localStorage:** сцены собирает агент, а агент читает файлы.
  Хранилище — `scripts/projectStore.mjs`, сеть — плагин `engine-projects-api`
  в `vite.config.js` (`GET/POST/PUT/DELETE /__projects`), тот же сервер, что
  публикует сайт. Папка переопределяется `DDG_PROJECTS_DIR` (это понадобится
  собранному приложению и уже используется проверкой). Проверка —
  `npm run check:projects`: транслитерация имени, отказ на `../`, что
  переименование не стирает сцену и наоборот.
  **Меню** — маршрут `/engine` (`src/pages/Engine.jsx`): список проектов,
  «Новый проект» (заводские значения, «заводской берег»), ПКМ по карточке —
  открыть / переименовать / копия / удалить, кнопка «Лаборатория».
  **Редактор внутри проекта** — `/home/edit?project=<id>`.
  `useHomeSceneDraftSettings(project)` получил вторую ветку: с проектом сцена
  приходит файлом и уходит в файл с задержкой 700 мс плюс сброс на `pagehide`;
  без проекта всё как было, черновик сайта в localStorage не тронут. Редактор
  ждёт файл до первого кадра (`HomeEditRoute`), иначе сцена собиралась бы
  дважды — это 7 секунд впустую; ожидание закрыто загрузочным экраном.
  **Сайт защищён:** внутри проекта «В проект» и «На сайт» выключены, в статусе
  висит `homeEditor.publish.projectScope` (RU+EN). В шапке вместо «Редактор
  сцены» — имя проекта, кнопкой назад в `/engine`.
  Проверено в песочнице 41213 живьём: создание через меню (файл 55 КБ, 512
  ключей, заводской terrainSeed 37), открытие редактора на проекте, пауза
  дошла до файла за 1.6 с, обход папки `../` отбит 404, черновик сайта на
  месте (512 ключей), кнопки публикации в проекте выключены. Тестовые проекты
  удалены, в `projects/` остался README.
  **Осторожно:** песочницу 41213 пришлось перезапустить — `vite.config.js`
  читается один раз при старте (AGENTS §4.5). Редактору Дениса на 41212 тоже
  нужен перезапуск, иначе `/__projects` там отвечает HTML.
  **Дальше:** Electron-оболочка (нужно окно без запущенных чужих серверов —
  `npm i` вынимает кеш зависимостей из-под них, AGENTS §4.6), лаборатория как
  склад пресетов, справочник параметров для агента.
  `lint`, `build`+SEO, `check:projects`, `check:editor`, `check:cameras`
  проходят. Push не делал.

- 2026-09-10, Claude — **ЧЁРНАЯ РАМКА КАДРА И ПКМ В РЕДАКТОРЕ (по просьбе Дениса).**
  Коммит `ea16988`.
  **Рамка.** Полупрозрачная маска показывала, что осталось за кадром; чёрная
  обрезает так, как обрежет сайт. Переключатель «Чёрная рамка кадра» / «Solid
  frame mask» — первым пунктом в меню «Виды» во вьюпорте и в контекстном меню
  сцены. Состояние — `data-focus-frame` на `<html>`, CSS переиспользует правило
  режима «кадр сайта» (`FocusEditor.css`), помнится в `ddg_focus_editor_ui_v1`
  рядом с шириной панели. Настройка вида, не сцены: в настройки и публикацию
  не попадает.
  **ПКМ.** Один примитив `FocusContextMenu` (+ `focusContextMenu.css`) на весь
  редактор, рендерится порталом в `body` — у ленты камер `backdrop-filter`,
  а он делает из предка систему координат для `position:fixed`.
  Где работает: вьюпорт (объект под курсором → параметры, наводка камеры,
  наводка сверху, гизмо у уже выбранного объекта, скрыть/показать; плюс общий
  блок показа), плитка в ленте камер (выбрать, захватить, обновить миниатюру,
  удалить — защиты те же, что в диалоге), карточка камеры в списке (открывает
  существующий диалог), строка параметра (в избранное). Везде ещё
  браузерное меню просто гасится, поля ввода сохраняют родное.
  **Shift + ПКМ — запасной выход к меню браузера** («Сохранить изображение»
  у миниатюры иначе не достать).
  Меню вьюпорта ловится не событием `contextmenu`, а парой pointerdown/pointerup
  с порогом 4 px: правая кнопка в OrbitControls — это панорама, а `contextmenu`
  в Chrome на macOS приходит ещё до движения мыши.
  `sceneNodeForObject3D` теперь обёртка над `sceneHitForObject3D`, который вместе
  с узлом дерева отдаёт имя предка в сцене — по нему работает `frameObject`.
  Общий блок показа добавляется к меню всегда: небо накрывает сцену куполом,
  «пустого места» во вьюпорте почти не бывает.
  Проверено в песочнице 41213 во встроенной панели: рамка чернеет и переживает
  перезагрузку, меню вьюпорта на луче даёт «Суша»/«Скульптура», меню плитки
  и строки параметра открываются, браузерное меню гасится, Shift его
  возвращает, звёздочка избранного пишется. `lint`, `build`+SEO, `check:editor`
  проходят. Push не делал.

- 2026-09-09, Claude — **РЕДАКТОР ОТКРЫВАЕТСЯ БЕЗ ПОСТОБРАБОТКИ (по просьбе Дениса).**
  Новый редакторский ключ `editorPostProcessing` (по умолчанию `false`) гасит
  постобработку только во вьюпорте редактора: `postEnabled` в `WaterScene`
  получил `(mode !== 'editor' || Boolean(settings.editorPostProcessing))`.
  **Почему не просто снять галочку `postProcessingEnabled`:** она публикуется,
  и «В проект» унёс бы выключенную постобработку на сайт. Новый ключ лежит рядом
  с `editorCursor`/`animationPaused` — его нет ни в `publishedHomeSceneKeys`,
  ни в снимках камер (они строятся из тех же ключей), поэтому он не уезжает на
  сайт, не подменяет авторское значение и переживает переключение камер.
  Проверено узлом: `editorPostProcessing` в ключах публикации отсутствует,
  `postProcessingEnabled` — присутствует.
  Тумблер «Показывать её в редакторе» / «Show it in the editor» стоит сразу под
  исходной галочкой в Рендер → Постобработка, RU и EN.
  **Не проверено глазами** — панель браузера скрыта, когда Денис на неё не
  смотрит, а в скрытой вкладке сцена не рисуется (frames: 0). Логика и защита
  публикации проверены, картинку смотрит Денис.
  Побочное, на заметку: миниатюры камер снимаются с вьюпорта, значит теперь они
  будут сырыми, без плёнки. Если это мешает — сказать, снимать их можно с
  включённой постобработкой отдельно.
  Ещё не сделано: «глаз» (Посмотреть кадр сайта) пока следует этому же тумблеру,
  а должен бы принудительно показывать итоговый кадр. `lint`, `build`+SEO,
  `check:editor` проходят. Push не делал.

- 2026-09-09, Claude — **ВЫБОР ОБЪЕКТА КЛИКОМ В СЦЕНЕ (по просьбе Дениса), шаг 1.**
  Третья кнопка в `focus-stage-tools` рядом со списком и ладошкой: «Выбор объекта
  в сцене · V». Клик по объекту в кадре ставит тот же путь в дереве, что и клик
  в списке — второго источника истины не заведено, как и написано в `useEditorTool`.
  Клавиша **V**, а не W/Q/E: `WaterCameraRig` держит на них полёт камеры
  (`FREE_CAMERA_KEYS`: W/S вперёд-назад, A/D вбок, Q/E высота), G/R/S — гизмо,
  Tab — полный экран, Пробел — пауза.
  Разбор попадания идёт по именам, под которыми объекты уже лежат в сцене (по ним
  же работает наводка камеры), — в `SCENE_OBJECTS` добавлено поле `roots`, по
  строке на объект. Ловушка: `shore-water` — это вода, хотя начинается на `shore`,
  поэтому точное имя проверяется раньше префикса. Клик отличается от вращения
  сцены порогом 4 px, так что орбита в режиме выбора работает.
  Проверка: `npm run check:editor` (`sceneObjects.check.js`) — включая ловушку
  `shore-water`, подъём по предкам и то, что каждый корень ведёт в живой узел.
  В браузере на sandbox 41213: кнопка переключается кликом и `V`, клик по низу
  кадра выбирает Воду, по центру — HDRI, справа вверху — Танкер, инспектор
  следует за выбором.
  На заметку: кликом по небу выбирается HDRI — поведение честное, но пусть Денис
  скажет, хочет ли он вместо этого снятие выбора.
  **Шаг 2 — подсветка выбранного** (маска половинного разрешения + белое свечение
  по краю отдельным проходом, не веткой в общем шейдере постобработки) ещё не
  сделан. `lint`, `build`+SEO, `check:editor` проходят. Push не делал.

- 2026-09-09, Claude — **БЕСКОНЕЧНОЕ ПЕРЕТАСКИВАНИЕ ЧИСЕЛ (по просьбе Дениса).**
  `FocusControlNumberInput`: на первом движении жеста запрашивается Pointer Lock
  с `unadjustedMovement`, дальше шаг считается по `movementX` — курсор исчезает,
  краёв экрана нет, тянуть можно сколько нужно (поведение 3ds Max). Электрон для
  этого не нужен.
  Две ловушки, обе стоили по багу и обе закрыты:
  1) Pointer Lock и `setPointerCapture` дерутся — при блокировке перехват теряется,
     а его потеря означала отмену жеста; плюс блокировка залипала, если её сняли
     до того, как пришёл `pointerlockchange`. Введён флаг `locking`, а
     `releasePointerLock` поднимает `releasing` ДО выхода и снимает блокировку,
     даже если она успела прийти позже.
  2) Ручное `releasePointerCapture` перед запросом ломало запасной путь: когда
     Chrome в блокировке отказывает (например сразу после выхода по Esc — у него
     пауза), перехват остаётся единственным, что держит драг после ухода курсора
     с поля. Убрано, браузер снимает перехват сам.
  `LOCK_SPEED` (=1) — единственная ручка чувствительности: захваченное движение
  приходит в аппаратных точках, на Retina жест может ощущаться быстрее.
  **Проверено:** захваченный путь — в окне с фокусом ОС: захват берётся на INPUT,
  значение меняется за краем окна, Esc отменяет и снимает захват, после кнопки
  захват отпущен. Запасной путь — во встроенной панели, где захвата не дают:
  значение тянется по-старому, класс `focus-number--scrubbing` снимается.
  Ощущение (скорость) проверяет Денис рукой: захват требует фокусного окна ОС,
  из панели агента это не воспроизвести.
  Внимание на будущее: `page.bringToFront()` и headless-окна Playwright всплывают
  поверх работы Дениса — он просил так не делать. Проверки — во встроенной панели
  на sandbox 41213.
  `lint` и `build`+SEO проходят. Push не делал.

- 2026-09-09, Claude — **СТРАХОВКА ЗАГРУЗЧИКА + ЗАМЕР ПЯТИ СЕКУНД.** Поверх `ae5ef66`.
  Найден и починен баг собственной работы: в скрытой вкладке r3f держит
  `frameloop='never'`, сцена не строится совсем (`frames: 0`) и маяк молчит
  сколько угодно — а 25-секундная страховка снимала экран вслепую, и Денис
  вернулся бы к недостроенному редактору. Теперь отсчёт идёт только пока
  `visibilityState === 'visible'`, слушатель на `visibilitychange`. Проверено:
  32 с в скрытой вкладке — экран на месте, после возврата фокуса снимается штатно.

  **Профиль холодного старта** (Playwright, настоящее окно, свежий профиль,
  sandbox 41213; два прогона совпали): готово на **7.4 с**, главный поток
  заблокирован **5.4 с** шестью кусками 0.3–1.4 с. Разбор по CPU-профилю Chrome:
  - ~2.0 с — процедурная генерация текстур ландшафта: `terrainTextures.js` (657 мс),
    `terrainSpit.js:sampleSpit` (582 мс), плюс `coastHeight`/`coastPatch`/
    `shorePosition`/`coastProfile` (~600 мс) — самый крупный адресуемый кусок;
  - ~1.4 с — `onFirstUse` из `three/src/renderers/webgl/WebGLProgram.js`: отложенная
    сборка шейдерных программ (GLSL-конкатенация, getUniforms/getAttributes) —
    225 `linkProgram` и 450 `compileShader` за старт. Самая дорогая одна позиция;
    главный поставщик программ — запекание импосторов растений;
  - ~0.9 с — заливка текстур в GPU (`texSubImage2D`, 280 вызовов);
  - ~0.2 с — `cloudNoise`; остальное — геометрия (деревяшки, камни, олеастр, полосы).
  Что НЕ является причиной, вопреки первой гипотезе: расстановка растений и
  построение террейн-модели — замер в Node дал на всё про всё **183 мс**
  (кусты 70, камни 61, запрос 29, покров 13); `React.StrictMode` удваивает
  только эти 183 мс, то есть ~4 % ожидания. Инструментировать WebGL патчем
  прототипов нельзя — overhead раздувает картину втрое (18 с вместо 7.4 с).
  Параллельный аудит кода (25 агентов, состязательная проверка отклонила 10
  находок из 20 — в том числе почти все утверждения про удвоение работы
  `StrictMode`) подтвердил и локализовал:
  - `terrainTextures.js:14` — три `DataArrayTexture` 1024²×9: ~113 МБ перепаковки
    через canvas `drawImage`+`getImageData` (27 проходов) и столько же заливки в GPU;
  - `terrainGeometry.js:62` — ~120 600 вершин за первый монтаж, ~603 000 вызовов
    `coastHeight` (каждая вершина = 1 + 4 на нормаль), ~2 млн `push` в JS-массивы;
  - `plantAtlases.js:42` — `bakePlantImpostor`: 24 ракурса × 3 прохода на каждую
    из 12 популяций = до 864 синхронных `render()` и ~445 МБ render target'ов;
  - `oleasterModel.js:109`, `cloudNoise.js:84/108`, `waterShading.js:565`
    (объём шума 64³ строится дважды — для воды и для облаков), `ScenePostProcessing.jsx:135`.
  Мелких безопасных выигрышей в этом списке нет: всё, что не меняет картинку,
  либо архитектурно (воркер, кеш, MRT), либо меньше 25 мс и в профиле не видно.
  Поэтому по итогам замера код НЕ правился — сознательно.

  Дальше по этому направлению — только с ddg-render-runtime и решением Дениса:
  кандидаты — вынести генерацию текстур ландшафта в воркер (картинка не меняется,
  но меняется порядок загрузки) или кешировать её по хешу настроек.
  `lint` и `build`+SEO проходят. Push не делал.

- 2026-09-09, Claude — **ЗАГРУЗОЧНЫЙ ЭКРАН: ПОДПИСЬ ФАЗЫ.** Поверх `1155e2c`.
  Под полосой появилась подпись «Загрузка» → «Сборка сцены» (RU/EN,
  `homeEditor.boot.*` в `translations.js`), текст подставляет `HomeEdit`.
  **Переключение — CSS-анимацией, а не по событию загрузчика, и это принципиально.**
  Замер в браузере с живым WebGL: ассеты идут до 1138 мс, потом главный поток
  на 2942 мс уходит в построение сцены, последний ассет догружается на 4080 мс.
  Событийный вариант (`useProgress` + дебаунс тишины) переключал подпись только
  на 4989 мс — `setTimeout` не может выполниться, пока поток занят, поэтому
  «Загрузка» висела почти весь экран. CSS-анимацию ведёт композитор, она играет
  сквозь блокировку; задержка 1.2 с подобрана по замеру. Плата: на очень холодном
  кеше подпись сменится чуть раньше реального конца загрузки. Вся событийная
  машинерия (`BootAssetPhase`, `useProgress`, атрибут фазы на `<html>`) удалена.
  Headless Playwright для таких замеров непригоден: без WebGL сцена не строится,
  грузится 6 ассетов вместо 109 и срабатывает 25-секундная страховка (она,
  кстати, проверена этим же прогоном и работает).
  `lint` и `build`+SEO проходят. Push не делал; UI редактора и сцена не тронуты.

- 2026-09-09, Claude — **ЗАГРУЗОЧНЫЙ ЭКРАН: ДОРАБОТКА.** Поверх `8f0f2f3`.
  Кольцо-заглушка заменено настоящим знаком уробороса: `public/ouroboros-mark.webp`
  (1.6 КБ) — кроп 243,157 535×535 из `focus/ouroboros-reference.png`, яркость
  переведена в альфу с растяжкой уровней 70…205 (без растяжки фон исходника
  давал видимый квадратный ореол вокруг вращающегося знака), цвет `#e8e6dc`.
  Пересобирать при смене исходника. Появилась версия ядра: `HomeEdit` пишет
  `version` из `package.json` в `[data-engine-boot-version]`, шапка и экран
  показывают одно число.
  **Шкалы с процентами намеренно нет.** Пробовал четыре шага по `useProgress`
  drei — замер показал, что все 109 ассетов догружаются к 1.6 с, а оставшиеся
  ~4.5 с уходят на построение сцены и компиляцию шейдеров, где измерять нечего:
  полоса прыгала на 76 % за 0.4 с и потом стояла. Вернул индикатор без обещаний
  (бегущий сегмент), вся выдумка удалена. Обе анимации — только `transform`,
  их ведёт композитор.
  Проверено на sandbox 41213: уход экрана на 6.2 с, узел удаляется, атрибут
  чистится; `lint` и `build`+SEO проходят, знак и блок доживают до `dist/`.
  **Коммит — см. следующую строку лога; push не делал.** Сцена и UI редактора
  не тронуты (прямой запрет Дениса на изменение «Фокуса»).
  Открытым остаётся сокращение самих ~5 с блокировок главного потока.

- 2026-09-09, Claude — **ЗАГРУЗОЧНЫЙ ЭКРАН РЕДАКТОРА (OUROBOROS ENGINE).**
  По просьбе Дениса. Раньше холодный старт `/home/edit` показывал ~7 с шапку
  сайта и общий спиннер маршрута, а потом резко подменял их редактором.
  Экран лежит в `index.html` (разметка, стили, встроенный скрипт): браузер рисует
  его первым же кадром, до разбора модулей сцены. Флаг `data-engine-boot` ставится
  только на `/home/edit` и `/en/home/edit` — сайт грузится как раньше. Анимации
  только по `transform`/`opacity`, их ведёт композитор, поэтому 5 с блокировок
  главного потока при сборке сцены анимацию не подвешивают. Снимается экран в
  `HomeEdit.jsx` по `onSceneReady` (тот же маяк, что у `Home`) с плавным уходом
  0.45 с; страховочный таймер 25 с — на случай, когда WebGL недоступен и маяк
  не сработает никогда. Изменены только `index.html` и `src/pages/HomeEdit.jsx`.
  Проверено на sandbox 41213: снятие экрана на 6.1 с (обычный старт) и 5.4 с
  (сцена на паузе, `frameloop='demand'` — маяк успевает набрать кадры сам),
  `/info` без экрана, `/en/home/edit` с экраном, `lint` и `build`+SEO проходят,
  блок доживает до `dist/` и всех 26 постраничных HTML. Запущенный сервер 41212
  уже отдаёт новый `index.html` — Денису достаточно перезагрузить вкладку.
  **Коммита и push не было**, сцена и настройки не трогались.
  Осталось: сами блокировки главного потока (~5 с: терраса, растения, шейдеры)
  не сокращались — это отдельная работа по ядру.

- 2026-09-08, Codex — **УРОБОРОС: РЕДАКТОР «ФОКУС».**
  По прямому разрешению Дениса выбранный HTML-эскиз перенесён в настоящий
  редактор. Код **`301cac1`** (основа **`99dad7e`**), ветка
  `codex/ouroboros-focus-editor`, влит
  fast-forward в локальный `main`. Компактная панель справа, шесть областей
  слева, цветные метки (15), избранное, поиск, numeric drag/точный ввод,
  отмена параметров, разделённые рабочие камеры/сцены и прокручиваемая лента.
  Контракт камер/форматов и прежние команды сохранения сохранены; новые
  настройки UI и миниатюры лежат отдельно от сцены. Запрос первой миниатюры
  ожидает готовности рендера и не теряется при холодной загрузке: свежий
  профиль 41212 автоматически получил непустой кадр 320×316. Авторская опубликованная
  сцена не менялась, localStorage между origin не переносился.
  Lint в основном checkout проходит (одно прежнее предупреждение), build/SEO
  проходят; свежие browser profiles проверили взаимодействия на 1440/1280/900,
  RU/EN/reload, pin/search, drag/Esc/undo, числовой ввод, палитру, камеры/диалоги.
  Основной **http://localhost:41212/home/edit** открыт и проверен визуально;
  видны существующие авторские рабочие камеры и параметры. Вода/плёнка не
  перенастраивались. Прежние WebGL sampler warnings есть и в исходном main.
  **Push/деплоя не было.** [Отчёт](docs/ouroboros-focus-editor-2026-09-08.md).
  Sandbox 41213 остановлен, тестовые профили закрыты; 41212/41215 сохранены.
  Ветка/worktree сохранены для истории. HANDOFF оставлен незакоммиченным,
  сохранив прежние записи; `docs/design/` с эскизом тоже не включён в коммит.
  Далее — оценка Денисом в основном редакторе; физические iPhone/iPad и
  полный smoke не проверялись.

- 2026-09-08, Codex — **ВЫБРАН И ДОРАБОТАН HTML-ЭСКИЗ «ФОКУС».**
  По замечаниям Дениса оставлен один выбранный дизайн; убраны повторные кнопки
  поиска, справки, выбора объекта и показа панели. Добавлены 15 цветов значков:
  ПКМ в дереве, ЛКМ/ПКМ на значке инспектора; цвет группы наследуется объектами,
  отдельная метка переопределяет его. Метки хранятся только в origin эскиза.
  Корешок сворачивает/разворачивает правый инспектор, край меняет ширину.
  Числа меняются горизонтальным движением с ЛКМ; Shift — точнее, Esc — отмена.
  Для параметров ползунков жест объединён в один undo. Кинолента поддерживает
  колесо, трекпад и drag без случайного выбора камеры; плюс закреплён справа.
  Рабочие камеры и сцены справа разделены двумя блоками.
  [Эскиз и управление](docs/design/ouroboros-ui-study-2026-09-08/README.md),
  адрес `http://localhost:41218/index.html?layout=focus&node=camera&strip=1`.
  Проверены синхронизация/наследование/перезагрузка меток, resize/сворачивание,
  числовой drag/Shift/Esc/undo/точный ввод, прокрутка 12 камер и добавление.
  Окна 1728/1440/1280/900 px и возврат панели на 390 px; готовые превью просмотрены.
  **Оставлено незакоммиченным**, только папка эскизов и запись HANDOFF; текущий
  HEAD `e13aaca`, локальный `main`. Продуктовый редактор, его данные и сцена
  этой задачей не менялись. Push/деплоя нет. Далее — оценка уточнённого «Фокуса».

- 2026-09-08, Codex — **ПЕНА, ПЯТНА РЯБИ И ПРЕСЕТЫ МОРЯ.**
  Код **`d402edc`**, ветка `codex/sea-foam-wind-presets`, влит
  fast-forward в локальный `main`. В редакторе и лаборатории общий список
  «Свои / Штиль / Бриз / Волнение / Шторм». Пресеты меняют волны, прибой
  и пену одной записью, сохраняя авторский цвет, свет, камеры и бюджет сетки.
  Пена получает искривлённую линию каждого гребня и его маску около косы;
  след стал шире и неоднороднее, стоп-кадр не накапливает движущуюся пену.
  Плотность переводится в кружево по измеренному распределению шума,
  добавлена деформация против тайла и пятна мелкой ряби с сохранением
  её шероховатости в дальнем отражении. Пунктир промежуточного шейдера
  из-за производной после раннего выхода устранён, подтверждён A/B на GPU.
  Прошли check:water, lint и build; четыре состояния/ручная правка/EN,
  стоп-кадр, крупный план и основной редактор проверены на GPU.
  Основной 41212 обновлён, его сохранённые настройки совпали до/после;
  просмотренный ракурс восстановлен. Опубликованная сцена и порты не менялись.
  **Push/деплоя не было.** [Отчёт и ограничения](docs/sea-foam-wind-presets-2026-09-08.md).
  Тестовый браузер закрыт, sandbox 41213 остановлен, 41212/41215 сохранены.
  Далее — художественная оценка состояний и поведения пены Денисом;
  мобильная производительность и разовый startup WebGL warning требуют
  отдельной проверки. Чужая запись о логотипе и `docs/design/` не включены.

- 2026-09-08, Codex — **ТРИ HTML-ЭСКИЗА ИНТЕРФЕЙСА OUROBOROS.**
  Подготовлены самостоятельные интерактивные варианты «Студия», «Фокус» и
  «Ателье»: [эскизы и описание](docs/design/ouroboros-ui-study-2026-09-08/README.md).
  Открытие: `http://localhost:41218/index.html?layout=focus`; отдельный лёгкий
  статический сервер, без подключения к движку и сохранённым настройкам.
  Изучены основной редактор 41212 в режиме чтения и исходники функций;
  предложены шесть областей, 36 узлов, компактный инспектор, поиск, избранное,
  подсказки, камеры, сохранение и контекстные манипуляторы. В сцене статичный
  фрагмент скриншота Дениса; параметры и действия демонстрационные.
  Три схемы просмотрены в браузере, размеры проверены от 1280 до 1728 px;
  проверены поиск, числовой ввод/отмена, области хранения камер/звука,
  сохранение/откат макета, клавиши и сворачивание панелей. Превью и результаты
  лежат рядом с HTML. Продуктовые исходники этой задачей не менялись.
  **Оставлены незакоммиченными** новая папка эскизов и эта запись; чужие правки
  сохранены. Текущая ветка `codex/sea-foam-wind-presets`, HEAD `f6852ae`.
  Push/деплоя не было. Далее — выбор Денисом схемы; интеграция ещё не выполнена.

- 2026-09-08, Codex — **ПРИБОЙ ПО АЗОВСКИМ РЕФЕРЕНСАМ И ОСТАВШИЙСЯ ФЛИКЕРИНГ.**
  Код **`785021b`**, `codex/azov-water-reference-pass` влит fast-forward в локальный
  `main`. Изучены изображения из `/Users/denisdaragan/Desktop/Аналоги`.
  Сдвиг обрушения ограничен локальной длиной события; после удара остаётся
  низкий вал, исправлен измеренный разрыв губы 7,2 см. Пена на губе привязана
  к координатам профиля, убраны независимое скольжение и яркие объёмные ячейки.
  Цвет воды теперь влияет на преломлённую толщу; общая карта нормалей нового
  моря и ряби подключена к каустике terrain/Seabed одним проходом 256 × 256.
  GPU-изоляция обнаружила отдельный источник вспышек в следе танкера:
  MSAA-экстраполяция UV делала знаменатель экспоненты невалидным. В одной
  остановленной фазе до правки было 15 NaN RGB-каналов, после — 0, максимум
  яркости сохранён. Матрицы захватов при движении камеры: 0/22 рассогласований.
  Прошли check:water, build, lint (одно прежнее предупреждение CoastRocksPBR),
  GPU-проверки паузы, цвета, повторного включения и компиляции шейдеров.
  Главный редактор и лаборатория обновлены; сохранённые настройки основного
  редактора совпали до/после. Опубликованная сцена, интерфейс и порты не менялись.
  **Push/деплоя не было.** [Отчёт](docs/sea-azov-references-2026-09-08.md).
  По просьбе Дениса закрыты тяжёлые вкладки лаборатории/песочницы, сервер 41213
  остановлен; 41212/41215 сохранены. Открыт основной редактор; старая вкладка
  с ошибкой загрузки не доступна инструменту закрытия, сцена там не запущена.
  Далее — художественная оценка; контактная пена препятствий, ритм групп волн
  и расширение области каустики остаются отдельной доработкой. Финальный
  реализм не заявляется. Незакоммиченной остаётся только чужая запись о логотипе.

- 2026-09-08, Codex — **УСТРАНЁН РАССИНХРОН ВОДЫ ПРИ ДВИЖЕНИИ КАМЕРЫ.**
  Код **`ec8f57f`**, ветка `codex/water-camera-stability` влита fast-forward
  в локальный `main`. Матрицы отражений/рефракции теперь живые ссылки на
  объекты захвата: новое содержимое текстуры больше не получает старую проекцию.
  GPU до правки: рассогласование в 14/19 кадров движения; после: 0/30,
  максимальная разница обеих матриц 0. Пена и спрей теперь учитывают CSM/тени
  облаков; fill и авторская яркость сохранены. Вместо трёх одинаковых volume noise
  в продукте строится один; pending handle исключает дочерние сборки на старте.
  Проверены повторное включение воды (один новый общий texture UUID), контролы
  цвета, GPU desktop/узкий viewport, check:water, lint и build. Новых ошибок нет;
  прежнее предупреждение lint CoastRocksPBR и размер чанков остаются.
  [Отчёт](docs/sea-camera-stability-2026-09-08.md).
  Порты, опубликованная сцена, камеры и тюнинг волн/пены не менялись, push не было.
  Главный редактор обновлён с восстановлением просмотренного ракурса; лаборатория
  обновлена. 41212/41215 оставлены работающими, тестовый 41213 остановлен.
  Далее — художественная доработка профиля/ритма/пены; путь к обещанным Азовским
  референсам запрошен. Геометрический разрыв и разные координаты белых барашков
  для проверенного высокого ракурса не подтвердились; спекулятивных правок нет.
  Незакоммиченной оставлена только чужая запись HANDOFF о логотипе.

- 2026-09-08, Codex — **В ОСНОВНОМ ПРОЕКТЕ ОСТАВЛЕНО ТОЛЬКО НОВОЕ МОРЕ.**
  Код **`b41fdb9`**, локальный `main`; перенос старых поверхностей — `564e072`.
  Старые WaterSurface/FarWaterSurface и их шейдеры — в `legacy/`,
  вне активных импортов. Удалены старые водяные полосы AzovTerrain, которые
  ошибочно оставались включёнными одновременно с новой водой. Переключателя
  «Новое море» больше нет; seaEnabled=false в прежних настройках мигрирует.
  Исправлены перекрывающиеся геометрии стоп-кадра, координаты брызг и учёт
  прозрачности в тумане/выводе цвета спрея и пенной оболочки. Рябь, пробы,
  каустика и связанные объекты сохранены. Ползунки скорости/пены не менялись.
  Авторская опубликованная сцена и порты не изменены; **push/деплоя не было**.
  Прошли build, lint (одно прежнее предупреждение), check:water,
  check:sea-settings, check:cameras и проверка шва ряби. GPU: editor 41212,
  sandbox 41213, lab 41215; выключение воды убирает все её визуальные слои,
  новые шейдеры компилируются без ошибок. Финальная проверка сначала выявила
  оставшиеся яркие квадраты: отрицательная интерполированная толщина на MSAA-крае
  вала разгоняла Beer-Lambert до HDR 16376. В `b41fdb9` толщина ограничена нулём,
  alpha/затенение/входы пены — физическими диапазонами. Восемь кадров стоп-кадра
  и восемь движения со всеми слоями/MSAA/bloom: 0 nonfinite, 0 каналов >100.
  Блики, яркость пены и bloom не уменьшались. Основной редактор обновлён.
  Подробности воспроизведения и независимое падение проверки облачного света —
  [docs/sea-only-2026-09-08.md](docs/sea-only-2026-09-08.md).
  **Художественное направление предыдущего прибоя Денис отклонил.** Далее —
  реализм вала, скорость, пена и материалы по его референсам; сейчас выполнено
  исключение старого движка. 41212 и 41215 работают, тестовый 41213 остановлен.
  В HANDOFF остаётся отдельная незакоммиченная запись параллельной работы
  над логотипом; в коммит воды она не включалась.

- 2026-09-08, Codex — **ЭСКИЗЫ ЛОГОТИПА OUROBOROS ENGINE.**
  Текущий `main` — **`357388d`**. Сгенерированы и просмотрены три направления:
  круг, пластичная змея, гранёный знак. Листы и полные промпты сохранены в
  `output/imagegen/ouroboros-logo-2026-09-08/` (локально, каталог игнорируется Git).
  **Денис выбрал №2 — пластичную змею** (`02-organic.png`). Далее —
  подготовка вектора/малой иконки по выбранному силуэту. Прозрачный PNG
  пока не готов: два экспорта imagegen нарисовали клетчатый фон и отклонены.
  Эта запись оставлена незакоммиченной. Код, сцена и интерфейс не менялись;
  интеграция и публикация логотипа не выполнялись.

- 2026-09-08, Codex — **МОРЕ ПОДКЛЮЧЕНО В ЛОКАЛЬНЫЙ РЕДАКТОР.**
  Реализация **`119ade1`** поверх фактического `main` **`3ef6a4a`**;
  ветка `codex/sea-runtime-fixes`, перенос в локальный `main` fast-forward.
  Авторские `publishedHomeSceneSettings.js`, камеры и номера портов не менялись.
  `seaEnabled=true` по умолчанию; прежняя вода доступна переключателем
  «Новое море» в «Ландшафт → Вода». **Push/деплой не выполнялись.**
  Полный отчёт: [docs/sea-water-2026-09-08.md](docs/sea-water-2026-09-08.md).
  Устранены конфликт logarithmic depth с берегом, затопление косы маской,
  растянутые грани/нормали прибоя, несогласованность карты глубины и времени
  пены; подключены общий свет/CSM/оптика и зависимости плавающих объектов.
  Вода и номерные меши прибоя исключены из собственных captures и contact AO.
  Проверены build, lint (0 ошибок, 1 старое предупреждение), check:water,
  check:sea-settings, check:lab-links, check:cameras и GPU lab/sandbox/editor.
  Основной редактор **http://localhost:41212/home/edit** и лаборатория
  **http://localhost:41215/asset-lab.html?collection=water** запущены.
  Код и отчёт закоммичены; далее — художественная оценка Денисом и проверка
  на физических iPhone/iPad. Уклон дна лаборатории не переносился в сцену.
  Записи ниже — история до этой интеграции; утверждение «только лаборатория»
  в старых разделах больше не описывает текущее состояние.

- 2026-09-08 (вечер), Claude — **ГЕОМЕТРИЯ ВОДЫ У БЕРЕГА. ЧИТАТЬ ПЕРВЫМ.**
  Ветка `claude/water-geometry-shore`, коммиты **`025fa53`** и **`a8b816c`**
  поверх `bcd85d9`. Push не делался, в опубликованную сцену ничего не
  переносилось: обе новые настройки берега по умолчанию воспроизводят её
  бит-в-бит. Работа по жалобе Дениса «гребни не по нормалям ландшафта и
  проскальзывают сквозь полигоны» — сначала аудит (13 агентов, кадры с GPU),
  потом правки. Ответы Дениса на вопросы: пологость шельфа — отдельной
  настройкой; бары вдоль берега — добавить; урез — тонкая плёнка с языками и
  откатом; тип обрушения — оба по погоде (последнее ещё не сделано).

  **Что было сломано и чем починено**
  1. `BreakingWaves.jsx:surfWorld` не читал грунт вообще: лента стояла на
     уровне моря, а песок под ней рос — 6 см на q=2, 27 см на q=6, 68 см на
     верху наката, 25–37 см у корня косы. Теперь профиль поднят на
     `max(coastGround, 0)`, оба фрагмента режутся по `vGround + 5 мм`.
     Нормаль ленты считается разностями той же функции — уклон пляжа она
     теперь знает даром. `polygonOffset` тут был бессилен (2 мм на 20 м).
  2. Разрез протягивался вдоль постоянного `coastLand()`, а не по нормали
     гребня: перекос 3.3° в кадре по умолчанию, ±11° «стиральной доски» по
     всей длине, до 56° на хвосте у косы. Теперь `surfCenterU(s±h)` даёт
     наклон гребня, разрез идёт по его нормали.
  3. `gerstnerDisplace` гасит поезда по шагу сетки, а три поверхности
     передавали три разных числа (открытая вода `dist·0.06`, полоса 0.5,
     лента 1.5). На шве q=−24 ступенька 5 см от «Берега», 21 см от «Косы», и
     она пульсировала с волной. Теперь общий `waterCell(p)` в
     `gerstnerShader`, полоса берега получила и восстановление наклона по
     пикселю (`gerstnerPixelSlope`).
  4. Кромка воды в полосе резалась по вершинам (изолиния varying внутри
     треугольника — отрезок) — лестница с шагом 1 м. Теперь дно берётся из
     карты глубин попиксельно, а «вода» = `max(уровень − дно, лист)`;
     `vLevel` — несклампованный уровень зыби. Плёнка потеряла порог
     `film > 0.02` (он рвал подъём между вершинами) и получила нормаль песка
     (атрибут `aGroundNormal`, считается из уже собранных высот).
  5. `cross` в нормали ленты был перевёрнут; у оболочки пены `sign(n.y)`
     прыгал на вертикальном фронте и рвал её до 0.7 м. Линия обрушения —
     ломаная из 49 хорд по 5.8 м под сеткой 1.75 м — сглажена один раз 1-2-1
     и читается плавным шагом.
  6. **Пологость дна** — новая настройка сцены `terrainShoreKnee` (длина
     колена в метрах; уклон у уреза = глубина/колено). 12 м — берег как
     нарисован (1:4.4), 137 м — 1:50 с листа Дениса. В метрах, а не 1:N,
     чтобы берег другой глубины сохранил свой профиль; `check-water-seam`
     это поймал на 1:N-версии. При 1:50 волна 0.45 м ломается в **28 м** от
     песка вместо 4 — «Сдвиг обрушения» больше не нужен, в лаборатории он 0.
  7. **Бары и отмели** — `terrainBars`: подводный профиль материка не менялся
     вдоль берега вообще (42 из 49 секций линии обрушения совпадали бит в
     бит), «Рефракция» работала вхолостую. Гряды между зоной прибоя и внешним
     шельфом дают разброс линии обрушения 4 м на открытом пляже.
  8. **Рост по Грину** `coastShoal`: H ~ d^(−1/4), потолок 1.2 (бюджет
     крутизны ≤ 0.8, произведение < 1 — трохоида не свернётся). На пологом
     дне зыбь гасла на 45 м и читалась стеклом.
  9. **Заплеск**: фронт наката был одним числом на все 280 м гребня, а сам
     гребень скошен пилом до 17 м. Теперь `uBoreFrame` переносит выборку в
     систему гребня (пил + `coastCrestWiggle`, общая с лентой), `uBoreRunup`
     ограничивает накат по q песка, а не по системе гребня; лист сужается к
     кромке; канал «мокро» читается на своём текселе (память песка перестала
     уползать вниз по склону вместе с пеной); мокрое начинается от уреза
     (`qBore > −2`) — иначе вся коса мокрая навсегда.
  10. Ландшафт склеивал свою аналитическую мокрую полосу с полем через
     `max`; аналитический уровень не движется (огибающая 4.2 см против
     порога 4.55 см), и под широким листом стояла вечная тёмная кайма в 1.3 м
     от уреза. Теперь `mix` по весу окна поля; `swashField` отдаёт вес в `.a`.

  **Лаборатория**: камеры целятся в тот вал, который нарисован (были наведены
  на физическую линию обрушения и промахивались на весь «Сдвиг обрушения» —
  «Труба» и «Губа» показывали пустую воду); новый ракурс **«Урез»** в метре над
  песком; каркас включается у всех трёх поверхностей; ползунок в паузе просит
  кадр (`RedrawOnChange`); в стоп-кадре вал идёт по пляжу вместе с фазой;
  новые ползунки «Пологость дна 1:N» и «Бары и отмели» (умолчания 50 и 0.45,
  берег пересобирается при их движении); «Заплеск на песок» ограничен 12 м
  (потолок карты глубин +16, полосы берега +12, `surfBand` ландшафта +12).

  **Проверки**: `npm run check:water` получил `surfGeometry.check.js` —
  контракта геометрии не проверяла ни одна из четырёх (одна ячейка на три
  поверхности; лента стоит на дне и режется им; разрез по нормали гребня;
  кромка полосы по пикселю; остаточная закопанность 3.3 см против 68 см).
  `lint` 0 ошибок, `build` собирается, кадры на GPU (ANGLE metal) без
  ошибок шейдеров, 49–93 кад/с при 2.9–3.1 млн треугольников.
  **Грабли**: `npm run check:terrain` («Bluff contact must not lift the slab»)
  и `scripts/check-water-seam.mjs` падали ещё на `bcd85d9` — к воде
  отношения не имеют, первое про посадку камней, второе устарело вместе со
  старой водой (HANDOFF это уже отмечал). `swashField` стал `vec4` — при
  правке не забыть ранний `return` (компилятор ловит).
  Сервер лаборатории Дениса на 41215 обслуживал **старое воркдерево**
  `.claude/worktrees/handoff-water-continuation-4ac5a4` — правок основного
  чекаута там не видно; свой сервер поднимался на 41219 с отдельным cacheDir.

  **Не сделано / дальше**: тип обрушения по числу Ирибаррена (Денис выбрал
  «оба, по погоде» — рассыпающийся вал для штиля/бриза, труба для волнения);
  брызги частицами; пена и цвет (по договорённости не трогались). **Замечание
  для стадии цвета**: любая поверхность, повёрнутая к камере, читается плоским
  насыщенным `uWaterColor` — у воды в лаборатории нет отражений и рефракции
  продукта, поэтому вал выглядит зелёной плитой. Это то, о чём HANDOFF уже
  писал: при интеграции новая вода должна получить `WaterReflections`.
  Кадры: `output/playwright/` (`final`, `swash2`, `bed2` — после;
  `water-geom`, `water-runup` — до).

- 2026-09-08, **ЧЁРНЫЕ ПЯТНА — окончательная причина.** Полоса у берега решала,
  есть ли здесь вода, сравнивая **мгновенную поверхность** с дном:
  `depth = vLevel - bed; if (depth < 0.004) discard`. Уровень зажимался по
  вершинному `aGround`, а дно в пикселе бралось как `max(карта, aGround)` — и
  там, где карта читает выше настоящего грунта, ложбина оказывалась ниже дна и
  фрагмент выкалывался. Ложбины идут решёткой — решёткой шли и дыры.
  Теперь суша определяется **дном относительно спокойного уровня**, а не
  волной: `if (bed > 0.02 && sheet <= 0.001) discard`, глубина зажата снизу.
  Море есть везде, где его дно ниже уровня моря; **волна не может унести воду**.
  Замер: доля тёмных пикселей ниже горизонта — **0.0000** (была решётка).

- 2026-09-08, **«ЗАТОПИЛО» — вторая половина жалобы: вода ВЫГЛЯДЕЛА песком.**
  После починки заплеска вода на сушу больше не лезла, но Денис продолжал
  видеть залитый берег — и был прав по-своему: просвет дна читался слишком
  далеко (`exp(-3·глубина)`, то есть песок виден на полтора метра), поэтому вся
  мелководная полоса выходила ровного песочного цвета и читалась как пляж под
  водой. В мутном Азове дно пропадает на десятках сантиметров. Коэффициент
  вынесен в один юниформ `uBedReach` в общем шейдинге и привязан к **мутности**:
  ползунок «Мутность воды» во вкладке «Вид», 3.5…12.5 м⁻¹, по умолчанию 8.
  **Диагностический урок:** сравнивать скриншоты бесполезно, если у Дениса
  камера повёрнута мышью (в лаборатории OrbitControls) — его кадр и кадр из
  свежего профиля это разные ракурсы. Проверять надо либо тем же ракурсом по
  кнопке, либо измерением. Попытка измерить «вода над сушей» лучом из камеры
  провалилась: луч находит грунт, но не знает, что вода **перед** ним, — для
  такого нужен глубинный буфер, а не raycast по высотному полю.

- 2026-09-08, **ПОТОП — НАСТОЯЩАЯ ПРИЧИНА (третья попытка, эта верная).**
  Ни клампы уровня, ни спор карты с грунтом не были корнем. Топил **канал
  заплеска** поля пены: он значил «здесь сейчас лежит язык наката», но **над
  водой ему присваивалась единица** — для непрерывности. Полоса у берега
  поднимала воду на песок везде, где грубая карта глубин (34 см на тексель)
  приняла пляж за море: `world.y = max(world.y, aGround + uFilm * film)`.
  Отсюда плёнка воды по всему пляжу и по косе. Теперь над водой канал равен
  **нулю** — там и так есть вода, лист не нужен; поднимать воду на сушу может
  только настоящий накат. Правило: канал состояния обязан значить ровно то,
  что написано в его имени; «для непрерывности» — не причина.

- 2026-09-08, **ПОТОП, «НАЧЕРЧЕННАЯ ЛИНИЯ» И РАЗНООБРАЗИЕ ВДАЛИ.**
  - **Потоп после первой правки.** Полоса у берега продолжала заливать пляж и
    косу, потому что **фрагмент верил карте глубин** (34 см на тексель, высоты
    зажаты ±1 м), а вершина брала настоящую функцию грунта. Где они расходятся,
    теперь побеждает **больший** — суша остаётся сушей. Проверка
    `surfGeometry.check.js` переписана так, чтобы стеречь смысл (дно читается
    на пиксель), а не буквальную строку.
  - **«Линия по оси прибоя»** — это был след бора в поле пены: ровная узкая
    полоса, положенная вдоль изобаты, то есть буквально начерченный контур.
    Ширина увеличена втрое, край разбит тем же хэш-шумом, что у барашков, и
    след лежит **позади** бора, а не по его оси.
  - **Разнообразие вдали (пункт 3 плана).** Появились «кошачьи лапы»:
    медленное поле сотен метров в поперечнике (`waterWindPatch` в общем чанке
    Герстнера, потому что его нужен и полю пены), где воздух стоит — вода
    стеклянная, рябь падает до 12 %, барашки до 15 %, и солнце собирается в
    дорожку вместо рассыпания по ряби. Ползунок «Пятна ветра» во вкладке «Пена».
    **Порядок включения важен**: `gerstnerShader` обязан идти **до**
    `waterShadingShader` во всех водных фрагментах — иначе `waterWindPatch`
    и `gerstnerNoise` не определены; сейчас так во всех пяти шейдерах.
  - **Освещение главной сцены проверено** свежим профилем на редакторе
    (ползунки Дениса не тронуты): сцена собирается, солнце и HDRI дают
    ожидаемый результат, консоль чистая. **Важное наблюдение для воды:** сайт
    **монохромный**, а новая вода настраивается в цвете в лаборатории —
    прежде чем судить её цвет, нужно понять, как она читается в ч/б.
    Отдельно: в лаборатории нет HDRI-окружения студии (`AssetStudio` в
    режиме сцены), поэтому солнце там плоское — это и может быть тем, что
    Денис называет «галимо светит». Следующий шаг по свету: дать лаборатории
    то же окружение, что у сцены, и сравнить.

- 2026-09-08, **ЧЁРНЫЕ ПЯТНА, «МОРЕ», ПРИБОЙ ПО ВСЕМУ БЕРЕГУ.**
  - **Чёрные пятна** в воде — это были **дыры**: в ложбинах волн поверхность
    полосы у берега (`ShoreWater`) опускалась ниже дна, фрагмент выкалывался
    по `depth < 0.004`, и решётка выколов читалась чёрными эллипсами.
    **Первая попытка была неверной и хуже болезни**: поднимать сам уровень до
    грунта (`max(world.y, aGround)`) — это подняло воду и на сушу, залив пляж
    и косу. Правильно ограничивать **глубину ложбины**, а не высоту
    поверхности: `world.y = max(world.y, -max(-aGround - 0.02, 0))`. Там, где
    грунт выше спокойной воды, предел равен нулю, поверхность остаётся на
    урезе и фрагмент честно выкалывает её как сушу.
    Правило на будущее: ограничение накладывать на **осадку волны**, никогда
    на высоту поверхности; выкалывание по глубине не должно решать вопрос
    геометрии.
  - **Коллекция 14 «Пена» свёрнута** по просьбе Дениса; `src/foam-lab` удалён,
    облака вернулись на 14. Коллекция 13 переименована в **«Море»**
    (`Море · волны, прибой и пена` / `Sea · waves, surf and foam`). Стенд пены
    свою работу сделал: объём, пар и брызги живут в продуктовых модулях
    (`FoamVolume`, `spray.js`) и доступны из «Моря».
  - **Прибой по всему берегу**: гребень растянут с 280 до 760 м, лент семь
    вместо четырёх (по три вызова на ленту — честный потолок), а интервал между
    рождениями берётся из наборов (`0.45–1.55 × период`), поэтому линии
    приходят группами, а не по метроному. Раньше жила одна линия у косы, и
    Денис справедливо говорил, что «одна полосочка всего лишь».
  **Осталось в очереди:** разнообразие вдали (пятна штиля, рябь разной силы,
  игра солнца) — пункт 3; проверка освещения в главном редакторе (Денис:
  «солнце как-то галимо светит»); брызги — по его слову.

- 2026-09-08, **ШАГ 2 СДЕЛАН: пена ленты прибоя — это пена воды.** Лента
  красила свою пену собственной формулой в координатах профиля
  (`vec2(s·длина, arc)`), поэтому кружево обрывалось на краю меша, а покрытие
  не знало ни поля пены, ни аналитических барашков. Теперь лист считает
  `max(собственный puff, поле в окне, барашки вне окна)`, рисует кружево в том
  же **мировом** потоке (`waterFlowUv`), стареет вместе с полем и показывает
  дно через мелководье — по той же формуле Бера, что вся вода. Оболочка вала
  берёт комки в **мировых** координатах со скользящим срезом (раньше — в
  координатах профиля, один штамп, повторённый вдоль всего вала: это и читалось
  «цветной капустой»).
  Остаётся честно: **сама оболочка** — это и есть та полупрозрачная геометрия,
  которую Денис хочет заменить частицами; доводить её силуэт дальше смысла нет,
  пока он не вернётся к брызгам (шаг 4).
  Замер коллекции «Вода» с настоящим берегом: 2.98 млн треугольников,
  90 вызовов, 64–71 кад/с.

- 2026-09-08, **ПОРЯДОК РАБОТ, ВЫБРАННЫЙ АГЕНТОМ** (по просьбе Дениса «выбери
  последовательность плана и иди по нему»), и первый шаг сделан:
  **1. Одна вода** — общий шейдинг для всех поверхностей (сделано, см. ниже).
  **2. Пена на ленте прибоя** — та же мера покрытия и то же кружево, что на воде.
  **3. Разнообразие вдали** — пятна штиля, рябь разной силы, игра солнца,
  возможно границы вод.
  **4. Брызги** — отложены по прямой просьбе Дениса («детский сад из шариков,
  потом добью»); не трогать, пока он не вернётся к ним.
  **Шаг 1 сделан. Жалоба Дениса:** «пена не накладывается на шейдер гребня и
  выглядит как кусок прямоугольной текстуры, откуда она вообще?» Это была
  **полоса воды на сетке берега** (`ShoreWater`): у неё был свой расчёт пены —
  она брала плотность поля напрямую, без аналитических барашков, без
  перехода в окно поля и без подъёма гребня, — поэтому вдоль берега лежал
  прямоугольник другого шейдера. Теперь она считает **ровно то же**:
  `gerstnerWhitecaps` с тем же порогом, тот же `mix` в окно поля, тот же
  возраст и тот же подъём. Второе: `coastSwellFade` гасил зыбь **в ноль** на
  мелководье, и у берега оставалась мёртвая плоскость — у затухания теперь
  пол 0.22, а у ряби 0.45 на обеих поверхностях: ветер работает последний метр
  воды, и без этого мелководье было зеркалом.
  Осталось честно сказать: в стенде «Пена» ближний ракурс «Прибой» смотрит в
  основном на мокрый песок — берег там синтетический и очень пологий, камера
  низкая; это кадрирование стенда, а не дефект воды.

- 2026-09-08, **ПРИБОЙ ЛОЖИТСЯ НА ЛИНИИ ЗЫБИ + разбор жалоб на брызги.**
  Денис: «а ты специально гребни ставишь не по линиям протяжённых волн? так в
  жизни?» — **нет, так не в жизни, и это была ошибка.** Валы рождались по
  своему расписанию (период в секундах), а зыбь жила отдельно, поэтому прибой
  дрейфовал поперёк длинных волн. В природе **вал и есть гребень пришедшей
  зыби**. Теперь рождение вала привязано к фазе первичного поезда Герстнера на
  линии обрушения (`gerstnerCrestDelay`, `gerstnerPeriod`): целые периоды
  пропускаются, пока не набежит заданный интервал — ломается не каждый гребень,
  но каждый вал есть гребень. Проверка в `check:water` считает обратный отсчёт
  до гребня и требует, чтобы он убывал ровно на прошедшее время, оборачивался
  на периоде поезда и в предсказанный момент первичный поезд действительно
  стоял в максимуме (>0.999 амплитуды).
  **Жалобы Дениса на брызги и их причины (все найдены, все исправлены):**
  «светильники, жёлтые, слишком яркие» — доля солнца не учитывала ориентацию
  пылинки и перевешивала небо, поэтому низкое тёплое солнце красило весь плюмаж
  своим цветом; брызги в жизни освещены **небом** (белая крупинка на светлом
  фоне), теперь небо доминирует, а солнце модулируется нормалью.
  «растягиваются при масштабировании вьюпорта» — у пиксельного пола не было
  потолка, и при уменьшении окна каждая пылинка росла неограниченно; потолок 2.5×.
  «цепляется за один кусочек» — полоса эмиссии была приколота к середине
  гребня; растянуть её на весь гребень тоже неверно (пул размазывается в
  ничто) — она следует за камерой вдоль гребня и равна ширине обзора.
  **Не сделано, следующая тема Дениса:** разнообразие вдали — где ветер не
  гуляет, там штиль и солнце отражается иначе (игра света), рябь разной силы
  пятнами, завихрения; возможно подводные течения как границы вод. Плюс он
  просит валы **разного размера, расставленные случайно вдоль линий длинных
  волн**, и, возможно, крупнее вдали — сейчас размер варьирует «погодой», но
  вдоль одной линии несколько валов не рождается.

- 2026-09-08, **БРЫЗГИ — правки по замечаниям Дениса: пар, поворот, яркость.**
  Он посмотрел и сказал: вместо идеальных кругов нужна **воздушность**;
  размеры и больше, и меньше; частицам нужен **случайный поворот**, иначе
  фронтально они плоские и одинаковые; всплеск не должен быть одним куском;
  и главное — **нужен пар**: мелкая пена, улетучивающаяся как туман, чтобы
  **перекрыть неидеальную форму полупрозрачной геометрии на гребне** (в этом
  и смысл слова «облака»). Плюс: брызги ярче самой пены, ползунок яркости их
  не держит.
  Сделано:
  - **Два класса частиц из одного пула.** Капля бросается и падает (τ 0.05–0.5,
    полная скорость выброса, жёсткий порог по шуму). **Пар** — доля
    `sprayMist` — почти не падает (τ 0.012–0.05), берёт лишь пятую часть
    броска и поднимается, вчетверо крупнее и растёт по мере рассеяния, режется
    шумом почти без порога и в 20 раз прозрачнее. Это и есть слой, который
    закрывает силуэт оболочки.
  - **Случайный поворот квада** вокруг оси взгляда (базис `vRight/vUp`
    поворачивается на хэшированный угол): рваная форма у каждой своя со всех
    сторон, а не один штамп.
  - **Размах размеров шире** (`mix(0.3, 2.6, h²)`), **разброс поперёк гребня**
    (`spraySpread`, 1.6 м) — плюмаж стал телом, а не занавеской.
  - **Яркость сведена с пеной**: убран направленный множитель, осталась
    ровно формула `FoamVolume` плюс малое прямое рассеяние слагаемым —
    один ползунок «Яркость пены» теперь честно правит и пену, и брызги.
  Ползунки: «Доля пара», «Размер пара», «Разброс поперёк» рядом с брызгами.
  Замер: 185 тыс. треугольников, 13 вызовов, 104–120 кад/с.
  Осторожно при настройке: пар при доле 0.55 и размере 4.5 давал **белую стену**
  на весь кадр — он обязан оставаться вуалью (сейчас 0.4 / 2.2 и множитель
  непрозрачности 0.30).

- 2026-09-08, **БРЫЗГИ — шаги 2–3 сделаны, эффект есть.** Частицы стали средой:
  квад интегрируется как **хорда сферы**, шум читается в **мировых** координатах
  (перекрывающиеся пылинки сливаются в одно тело, а не складываются дисками),
  силуэт рвётся порогом по шуму, свет — формула `FoamVolume` плюс прямое
  рассеяние **слагаемым** (брызги не могут стать темнее пены), одна выборка
  к солнцу на затенённое подбрюшье.
  **Четыре ошибки, найденные глазами и арифметикой, все мои или плана:**
  1. **Шум брался в центре частицы.** `vWorld` — одно значение на весь квад,
     поэтому каждая пылинка получала одну константу и читалась гладким шариком.
     Теперь сэмплируется точка на поверхности квада — силуэт рвётся.
  2. **Признак детализации был перевёрнут** (`1 - smoothstep` вместо
     `smoothstep`): вблизи частицы получали *среднее* вместо шума — ровно то,
     от чего система и должна была уйти.
  3. **Бюджет покрытия считал все draw, а не выживших.** Отбор по `puff`
     отсеивает ~70 %, поэтому в кадре была горстка; введена доля
     `SPRAY_ACCEPTANCE = 0.32`, и проверка теперь мерит площадь по выжившим.
  4. **Затухание у воды не знало размера пылинки** — крупные срезались тестом
     глубины плоским ножом; теперь гаснут по собственному диаметру.
  Плюс: зерно шума должно быть **мельче** пылинки (`uSprayScale` 0.5 → 8 м⁻¹,
  иначе константа на всю каплю); размер пылинок разный (`mix(0.45, 2.1, h²)`) —
  несколько тяжёлых сгустков среди мелкой мороси; полоса эмиссии сужена вдвое
  (пул, размазанный по всему видимому гребню, давал россыпь вместо плюмажа);
  перерисовка поднята до 2.6× на высоком тире — слои и **есть** эффект.
  **Результат**: на фазе обрушения из гребня бьёт настоящий белый плюмаж,
  живёт секунды и сносится; в живом прогоне это событие удара, а не постоянная
  полоса. 354 тыс. треугольников, 16 вызовов, 120 кад/с (упор в vsync).
  Проверка: замкнутая формула полёта против численного интегрирования — 2 мм;
  бюджет покрытия держится на 11 м подхода, за границами названо, какой
  предел связывает.
  **Дальше**: шаг 4 — ручки уже стоят («Брызги / Плотность / Зерно /
  Завихрение / Дальность» во вкладке «Объём»); шаг 5 — **решение Дениса**:
  срезать ли оболочку вала (`surfRoller` к ~0.2), раз силуэт теперь несут
  частицы; шаг 6 — тиры в `qualityProfile` при переносе в главную сцену.

- 2026-09-08, **БРЫЗГИ — выбран проект и сделан шаг 1.** Многоагентный разбор
  (21 агент) оценил четыре подхода: инстансные билборды 7.3, GPU-симуляция 6.7,
  гибрид 6.3, объёмная плита 6.0. **Выбраны билборды без симуляции**, потому что
  у них нет состояния — ни ping-pong, ни readback, ни float-таргета, ни ветки на
  `frameloop:'demand'`; вся жизнь пылинки — замкнутая функция `(id, время)`, и в
  стоп-кадре лаборатории, где Денис настраивает, плюмаж живёт сам. Плита
  отвергнута по признанию собственного проекта: при насыщении её изоповерхность
  становится гладким баллистическим куполом — та же полупрозрачная геометрия,
  которую Денис и забраковал. Гибрид и GPU-симуляция требовали вынести
  `loftShader` в отдельный файл, а это ломает `surfGeometry.check.js` и
  `foamField.check.js`, которые читают строки внутри `BreakingWaves.jsx`.
  **Что сделано (шаг 1):** `spray.js` — пул 16384, один квад и один
  `aId` на всех (буферы общие по ссылке, `instanceCount` на геометрии);
  фаза жизни берётся **хэшем, а не рампой по id** (счёт меняется каждый кадр,
  рампа означала бы, что весь пул одного возраста и умирает разом); рождение
  спрашивает **сам профиль** (`surfProfile`) — вес пылинки есть `sp.puff`,
  поэтому брызги стоят ровно там, где стоит оболочка, и `uRoller` в это не
  входит, так что оболочку можно будет срезать не сдвинув брызги; скорость на
  губе — её собственная баллистическая `tangent` (новое поле `SurfPoint.vel`);
  полёт — **замкнутое решение** `dv/da = −(v−w)/τ + g` за один `exp`, τ от 0.05
  (дымка висит и сносится) до 0.5 (капля падает); расползание — тот же curl,
  что у поля пены, с ростом `age^1.5`, отчего молодая летит по чистой параболе,
  а старая рвётся в туман. `surfWorld` получил параметр `dq`: гребень
  отматывается назад к моменту рождения, иначе плюмаж жёстко едет с волной.
  **Счёт — из бюджета покрытия, не из расстояния** (`sprayInstanceCount`):
  закрашиваемая площадь держится плоской, поэтому цена не взрывается, когда
  камера подходит вплотную. Судейский закон `(d/25)²` переспорен арифметикой:
  на ракурсе «Губа» он оставлял ~3800 живых пылинок ⇒ 11 Мпикс перерисовки.
  **Проверка `spray.check.js`** (в `check:water`): замкнутая формула полёта
  сверяется с численным интегрированием ОДУ — расхождение < 2 мм на 2.2 с при
  τ ∈ {0.05, 0.18, 0.5}; закон покрытия держит площадь в ±5 % бюджета на 5–30 м,
  а где связывает нижний упор (нос к гребню) — перерасход ограничен двойкой.
  Упор в 60 пылинок, а не 300 из плана: проверка показала, что 300 на пяти
  метрах красят кадр девять раз.
  Лаборатория: ползунки «Брызги / Плотность / Зерно / Завихрение / Дальность»
  во вкладке «Объём», «Стоп-кадр» и «Фаза обрушения» во вкладке «Волна»,
  ракурс «Всплеск» у самой точки удара (изобата для 0.9 м — q ≈ −6.5 м).
  **Пока плоско-белые диски** — это шаг 1 по плану, среда идёт следующей.
  **Дальше по плану:** шаг 2 — среда (хорда сферы вместо спрайта, шум в
  **мировых** координатах, чтобы перекрывающиеся пылинки сливались в одно тело,
  а не складывались дисками; строка эрозии облаков; Бер + powder + формула
  `FoamVolume` со **слагаемым** прямого рассеяния, чтобы брызги никогда не были
  темнее пены); шаг 3 — дистанция (`vDetail`, тиры); шаг 4 — ручки; шаг 5 —
  решение Дениса, срезать ли оболочку вала; шаг 6 — мера и уборка.
  Полный план агентов сохранён в описании коммита и в этом блоке.

- 2026-09-08, **БРЫЗГИ ЧАСТИЦАМИ — задача Дениса (записано, проект в работе).**
  Его слова по сути: на ломающемся гребне пена сейчас читается как **уродливая
  полупрозрачная геометрия** — «стрёмная полоса», которая более-менее сходит
  только вдали. Предложение: **взять технологию облаков и превратить её в
  частицы**, которые ведут себя как туман, — чтобы вместо геометрии появилась
  живая картина: виден мощный всплеск, видно завихрение пены там, где волна
  бьётся о волну, и всё это рассеивается в туман. Когда гребень ударяет, это
  должно превращаться в живописный эффект, а не в прозрачный полигон.
  Жёсткие рамки: WebGL2 (нет compute-шейдеров, нет transform feedback в three),
  60 кад/с на ноутбуке и выживание на телефонном профиле, свет — тот же солнце
  и HDR, что у всей сцены (у Дениса единая система света и теней, и он на этом
  настаивает), без тайла и мерцания, читаемость на всех дистанциях.
  Что уже есть под это: облачный объём (3D-шум, raymarch, Beer/powder, выборки
  к солнцу, проекция теней), поле пены (RGBA16F ping-pong в окне за камерой:
  плотность, возраст, мокрый песок, лист заплеска; адвекция орбитальной
  скоростью + ветер + curl-завихрение), аналитическая баллистическая губа в
  `surfProfile.js` и измеренный закон покрытия барашками.
  Запущен многоагентный разбор (`spray-particles-design`): разведка по четырём
  подсистемам (облака, пена, прибой, рендер-бюджет), четыре независимых проекта
  (инстансные билборды / GPU-симуляция в ping-pong / объёмная плита плотности /
  гибрид крупных объёмных клубов и мелкой мороси), состязательная оценка
  каждого по трём линзам (цена, артефакты, интеграция) и синтез плана.
  План победителя будет вписан сюда следующим коммитом.

- 2026-09-08, **ПЕНА — почему был тайл и что с ним сделано.** Причина точная:
  объём облачного шума тайлится по всем трём осям, а плоское сечение через
  него повторяется каждые `1/масштаб` метров — при масштабе кружева 0.06 это
  решётка с шагом ~17 м, её и видел Денис на всех погодах. Лечение: **сечение
  едет вместе с миром** — глубина среза берётся из хэш-шума низкой частоты
  (`gerstnerNoise`), так что соседние участки воды читают разные слои объёма и
  плоскости, которой можно повториться, просто не существует. То же в объёме
  пены (`FoamVolume`) и во второй октаве кружева. Хэш-шум определён в
  `gerstnerShader`, поэтому его теперь подключают **все** водные фрагментные
  шейдеры (`BreakingWaves` лист и оболочка, `ShoreWater`) — не забыть при
  добавлении новой водной поверхности.
  **Лесенки**: покрытие пены в объёме считалось в вершине и интерполировалось
  по треугольникам радиальной сетки — на скользящем угле они шириной в метры,
  и их края читались ступенями сквозь пену. Теперь покрытие берётся из поля
  **на пиксель**; вершинная высота осталась только огибающей марша.
  **Завихрения (просьба Дениса про шторм)**: скорость адвекции поля пены
  получила бездивергентный curl того же хэш-шума, `foamSwirl` — в штиль 0.05,
  в шторм 0.9; там, где встречные волны сходятся, пена крутится.
  **Ответы на вопросы Дениса**: «кометки» от гребней — это адвекция поля,
  оставляем и усиливаем сносом/завихрениями; отдельная коллекция «Пена» — не
  ошибка: она **полигон** для шейдера, оба стенда читают одни и те же модули
  (§5 лаборатории), и как только пена доведена, она работает и на косе в
  коллекции «Вода» без переноса кода.
  **Осталось видимым**: на скользящих углах вода полосатит кольцами радиальной
  сетки (геометрия, не пена) — следующий кандидат; вблизи объём пены мягковат.
- 2026-09-08, Claude — **ПОИСКОВЫЙ ФУНДАМЕНТ САЙТА. НЕ ЗАКОММИЧЕНО.**
  Опубликовано 2026-09-08 как `708ab5e` поверх `9c9accd`: на `origin/main` уехал
  только поисковый коммит, остальные локальные коммиты (вода, берег, облака,
  камеры) по решению Дениса остались непушенными.
  Прогнаны `npm run lint` (0 ошибок, 1 старое предупреждение), `npm run build`,
  `npm run check:bundle`, `npm run check:seo`. Сборка проверена в браузере на
  `dist` 41214: приложение поверх слепка монтируется, canvas есть, консоль чистая.

  **Что было сломано.** Сайт на GitHub Pages с клиентским роутингом: файла по
  адресу `/portfolio` нет, Pages отвечает **404** и отдаёт `public/404.html`,
  который редиректит на `/?/portfolio`. Для человека незаметно, для робота
  страницы не существует. Плюс `dist/index.html` — 1316 байт пустого
  `<div id="root">`, один `<title>` на все маршруты, ни `description`, ни
  `canonical`, ни Open Graph; `robots.txt` и `sitemap.xml` не было никогда
  (проверена история git по всем веткам).

  **Что сделано.** `scripts/seo/build-seo.mjs` после `vite build` кладёт в `dist`
  настоящий `index.html` на каждый публичный маршрут: тот же собранный каркас со
  скриптами приложения, но со своим `title`, описанием, `canonical`, Open Graph и
  статическим слепком контента внутри `#root`, который React заменяет при
  монтировании. Тексты не сочинялись — всё из `projectRegistry` и `infoContacts`.
  13 страниц, `sitemap.xml` на 10 адресов, `public/robots.txt` с `Disallow` на
  `/home/edit` и `/portfolio/edit`. Разметка `ProfessionalService` на `/` и
  `/info`. Блок мета-тегов в `index.html` обрамлён маркерами `<!-- seo:start -->`,
  замена идемпотентна. Контракт — `npm run check:seo` (секунды, без браузера).

  **Грабли, на которые наступил и которые стоит помнить.** Canonical сначала
  указывал на `/portfolio` без косой черты, а Pages отдаёт каталог только по
  `/portfolio/` и на форму без черты отвечает редиректом — canonical на редирект
  поисковик игнорирует. Теперь везде форма с чертой, в проверке это закреплено.

  **Что осталось и требует Дениса.**
  2. Яндекс.Бизнес (карточка 37665084790): всё заполнено, кроме **ссылки на
     сайт**. Телефон, почта, часы «будни 11:00-20:00», адрес с этажом — на месте.
  3. 2ГИС: заявка отправлена 2026-09-08 через открытую форму `account.2gis.com`
     (бизнес-аккаунт не нужен). Модерация обещает связаться за два рабочих дня.
     График работы в заявку не попал.
  4. Яндекс.Вебмастер и Google Search Console: права не подтверждены, sitemap не
     отправлен. Нужен код подтверждения от Дениса.
  5. `src/data/infoContacts.js` — офис США «123 Ocean Drive, Santa Monica,
     CA 90401» это явная заглушка. В structured data он намеренно не попал, но со
     страницы `/info` его надо либо убрать, либо заменить настоящим.
  6. Почта сведена к канону `info@daragangarden.ru` (подтверждён Денисом):
     поле `email` добавлено в `src/data/infoContacts.js` и выводится на `/info`
     тем же блоком, что телефон; в `public/landscape/index.html` старый адрес
     заменён; `build-seo.mjs` берёт его оттуда же, а не своей константой.
     Осталась только старая почта внутри PDF-портфолио — файл не трогал.
  7. Языки разведены по адресам. Русская версия в корне, английская под `/en`.
     Источник правды — `src/i18n/languageRoutes.js` (`languageFromPath`,
     `stripLanguage`, `localizePath`), его же импортирует генератор страниц, чтобы
     адреса на сайте и в sitemap не разошлись. `LanguageProvider` переехал внутрь
     `Router` и берёт язык из адреса; `localStorage` (`ddg_site_language_v1`)
     удалён — один URL обязан показывать один язык, иначе поисковик не может
     сослаться на английскую версию. Маршруты в `App.jsx` регистрируются дважды,
     ссылки меню, портфолио и кнопки «назад» проходят через `localizePath`.
     На каждой странице hreflang ru/en/x-default в обе стороны. 26 страниц,
     19 адресов в sitemap. Проверено в браузере: `/portfolio/` русская с
     ссылками без префикса, `/en/portfolio/` английская с `/en`, консоль чистая.
  8. `public/landscape/` — единственная полноценная статическая страница с текстом,
     теперь она в sitemap и в слепке главной, но в меню сайта ссылки на неё нет.
  9. Картинки: 23 WebP в одном размере, 16 тяжелее 500 КБ, ни `srcset`, ни
     `width`/`height`, `alt` пустые. Это Core Web Vitals и отдельная работа.

- 2026-09-08, Claude — **РЕДАКТОРЫ ПОРТФОЛИО ПОДНЯТЫ НА ПОРТАХ.**
  Ничего не слито в `main`, оба живут в воркдеревьях, сайт не тронут.
  1. `portfolio` **:41217** — ветка `codex/site-layout-design`, воркдерево
     `.claude/worktrees/portfolio-edit` (`node_modules` — симлинк на корневой).
     Это `/portfolio/edit`: превью при наведении (какая картинка всплывает,
     сторона, размер, смещение Y) плюс «ОПУБЛИКОВАТЬ» в исходник. Работы и
     картинки через него не добавляются. Корень редиректит на `/portfolio/edit`
     через незакоммиченный `vite.portfolio-edit.config.js` — панель умеет
     открывать только корень сервера.
     Сначала занял `sandbox`, Денис вернул: 3D-песочница на 41213 осталась как
     была, у портфолио теперь свой порт 41217.
  2. `cms` **:41216** — ветка `codex/content-platform`, воркдерево
     `.claude/worktrees/content-platform`, приложение `services/content-cms`
     (Payload 3.88 + Next 16). Вот это «добавить работы и картинки»: проекты,
     RU/EN, теги, медиа, черновики, версии, публикация. Админка `/admin`,
     интерфейс русский, первый пользователь-администратор заведён.
     Локально поднято **без Docker**: `brew install postgresql@17` +
     `brew services start postgresql@17`, база и роль `ddg_content`, секреты в
     `services/content-cms/.env` (в `.gitignore`, режим 0600). В dev Payload
     сам пушит схему (`push: true`), миграции не нужны.

  **Три бага ветки `codex/content-platform`, чинились здесь (не закоммичено):**
  - `src/app/(payload)/layout.tsx` — `serverFunction` передавался инлайновой
    стрелкой без `'use server'`: все маршруты админки отдавали 500.
  - `src/app/(payload)/admin/importMap.ts` — заглушка была пустой и вдобавок
    не той формы (`{ baseDir, importMap }` вместо плоского
    `{'<пакет>#<экспорт>': Компонент}`): дашборд терял карточки коллекций.
    `payload generate:importmap` пишет соседний `importMap.js`, но layout
    импортирует путь без расширения и `.ts` выигрывает при резолве — новые
    строки генератора переносить в `.ts`, `.js` не оставлять.
  - `next.config.mjs` — добавлен `agentRules: false`: Next 16 писал в
    репозиторий `AGENTS.md` и `CLAUDE.md` при каждом старте dev-сервера.

  Записи в `.claude/launch.json` для обоих указывают `cwd` внутрь
  `.claude/worktrees/*`, а не `"."` как остальные, — из другого чекаута они не
  поднимутся.

- 2026-09-08, **ПЕНА — аналоги Дениса, разбор и его идея «пена из облаков»**
  (записано; работа начата — коллекция 14 «Пена» в лаборатории).
  Референсы: `~/Desktop/Аналоги/Пена_море/` (9 файлов: два шторма МЧС на Азове,
  штормовое море с брызгами, тёмный шторм, шторм у мола, штиль с кружевом на
  песке, прозрачный штиль, полдень с полосой прибоя).
  **Пена меняет характер вместе с погодой — это и есть суть:**
  - **Штиль**: пены почти нет; только тонкая кружевная нить у самой кромки
    заплеска, пузырьки читаются поштучно, всё гаснет за секунды.
  - **Бриз / волнение**: белая полоса в зоне обрушения плюс редкие барашки;
    старая пена собирается в **полосы вдоль ветра** (лангмюровские дорожки).
  - **Шторм**: пена **везде**, не только на гребнях — вся поверхность
    мраморная от старой пены, полосы вдоль ветра сплошные; свежая пена на
    ломающемся гребне — **толстая, объёмная, катящаяся масса** с комками и
    затенённой изнанкой; над гребнями стоит **сдуваемая взвесь брызг**
    (отдельный элемент, не пена на воде). Море при этом серо-оливковое от
    взмученного ила, и пена — самое яркое в кадре.
  - Старая пена — **плоские пятна с дырами и рваным кружевом по краю**,
    дрейфующие; свежая — плотная и объёмная. Возраст решает форму.
  **Идея Дениса (принята, начинаем с неё):** взять за основу **движок
  объёмных облаков проекта** (коллекция «Облака», `PainterlyClouds`,
  `cloudNoise.js`, `cloudVolumeShaders.js`) и сделать пену тем же принципом,
  только мелкую. Почему это верно технически: у облаков уже есть 3D-шум,
  поля покрытия и плотности, raymarch с Beer/powder, дешёвые выборки к солнцу
  вместо второго марша, проекция тени на приёмники, профили качества и GPU-
  таймер. Пена — ровно такая же среда: низкий тонкий слой белого рассеивателя
  на поверхности воды. Что это даёт: **настоящий объём** (катящиеся массы с
  затенённой изнанкой), корректный свет от солнца и HDR — тот же, что у всей
  сцены, — и **тени пены на воду**, о чём Денис говорит отдельно.
  **Конструкция, которую делаю:** не отдельный объём в воздухе, а **оболочка
  над поверхностью воды** — вершина поднимается на высоту пены, а пиксель
  марширует вниз до поверхности (тот же приём, что уже работает на валу
  прибоя в `BreakingWaves`, но теперь по всему полю пены). Плотность =
  покрытие из поля пены × вертикальный профиль (плотно у воды, размыто
  сверху → круглые шапки) × 3D-шум облаков; **возраст поля** превращает
  толстую свежую массу в плоское кружево. Свет: Beer/powder + три выборки к
  солнцу. Дальность: высота и число шагов падают с расстоянием, за порогом
  остаётся плоская пена сегодняшнего шейдера — эффект мелкий, вдали он обязан
  растворяться, а не мерцать.
  **Полигон**: отдельная коллекция лаборатории **«Пена»** (по просьбе Дениса,
  отдельно от ландшафта): небольшой кусок воды новым шейдером + один вал,
  прямой синтетический берег, пресеты Штиль / Бриз / Волнение / Шторм.

- 2026-09-08, **ГРЕБЕНЬ И ЦВЕТ ВОДЫ — аналоги Дениса и разбор (записано, не
  сделано).** Референсы: `~/Desktop/Аналоги/Гребни волны_Азовское_Море/`
  (7 файлов; ключевые — `Вставка …07:35:23.png` глазурная азовская волна,
  `images (2).jpeg` зелёные стенки на синем море, `images.jpeg` изумрудный
  гребень на низком солнце, `images (1).jpeg` бочка с оливковым ядром,
  `Вставка …07:35:16.png` шторм, `gentle-tiny-waves.jpg` прозрачный штиль).
  **Что на них видно:**
  - Стенка волны — **другого тона, а не просто ярче**: тело моря сине-зелёное,
    а просвеченная солнцем стенка — бледный нефрит/оливково-жёлтый, у тонкой
    губы почти бело-зелёный. Денис: «как с картины Айвазовского», «как будто
    смешалось с цветущей водой» — это его детское «почему вода зеленоватая».
  - Чем **тоньше** просвечиваемый слой, тем светлее и желтее; в толстом ядре
    бочки — тёмное оливково-бурое (виден путь через много воды и взвесь).
  - В шторм (`07:35:16`) синевы нет вовсе: взмученный ил делает всё море
    серо-оливковым, гребни ломаются буровато-белыми. В штиль (`gentle-tiny…`)
    вода прозрачная, песок читается насквозь. **Один и тот же водоём.**
  - Гребни **разные**: высота, толщина, момент и степень обрушения, и цвет —
    некоторые стенки зеленее соседних. Нынешняя монотонность — главный upset.
  **Диагноз по нашему шейдеру.** В `waterShading.js` тело и просвет красятся
  **одним** `uWaterColor`, а затухание — **скалярное** `exp(-thickness*1.6)`:
  толщина меняет только яркость, поэтому гребень не может стать другого тона.
  Отсюда «шейдер несогласованный, эффект испарился».
  **Решение: перейти на попиксельный по-канальный перенос.**
  - Разделить два цвета: **рассеяние** (тело, отражённый от взвеси свет) и
    **пропускание** — `transmittance = exp(-sigma * path)` с **вектором**
    `sigma` (R, G, B разные). Тогда тонкая стенка гребня сама станет
    жёлто-зелёной, толща — тёмной сине-зелёной, без ручной подкраски.
  - `sigma` собирать физично из того, что уже есть в проекте:
    **CDOM/цветение** (`terrainBloom`, у Дениса цианобактерии уже есть
    процедурным полем `coastBloom(qs,time,depth)` в `terrainShader.js`) —
    жрёт синий и красный, пропускает зелёный; **взвесь/ил**
    (`waterTurbidity`, `terrainSilt`) — гасит всё и уводит в оливково-серый,
    убивает прозрачность; чистая вода — жрёт красный. Шторм
    (`terrainStorm`) поднимает взвесь → штормовой кадр получается сам.
  - **Связь с дном (идея Дениса)**: вода читает то же поле цветения и те же
    донные покровы (`coastBedCover`: тина, ил, мидии), что и ландшафт —
    над лугом тины и над пятном цветения вода зеленее и мутнее, над чистым
    песком прозрачнее. Это уже посчитано в `terrainShader.js`, нужен общий
    модуль, а не копия (правило §5 лаборатории — не дублировать).
  - **Единство со светом**: пропускание освещать теми же `key.sceneRadiance` и
    `environment` (HDR), что и всё остальное — как у Дениса сделана единая
    система теней; никакого отдельного «цвета просвета» руками.
  - **Разнообразие гребней**: толщина стенки вдоль гребня должна меняться
    (тонкие места светятся, толстые темнеют) — у нас `thickness` уже есть в
    профиле, но постоянна вдоль гребня; плюс поле «погоды» и поле цветения
    дадут соседним гребням разный тон. Это шейдер, а не геометрия — Денис
    подчёркивает: «нужно качественно поработать дополнительно с шейдером,
    не только с геометрией».
  Порядок: этот пункт закрывает и №2 (тон вала ≠ тон воды), и №4
  (фактуры дна) из списка Дениса — один расчёт переноса на все три
  поверхности и на дно.

- 2026-09-08, **КАУСТИКА — аналоги Дениса и разбор (записано, не сделано).**
  Референсы: `~/Desktop/Аналоги/Каустика_побережье_воды/` — два фото мелководья
  над песком (вертикальное: чистая вода, солнце высоко; горизонтальное: песок
  с пятнами тины и солнечная дорожка). Что на них видно и что из этого следует:
  - Сетка каустики **не равномерная**: тонкие яркие нити образуют ячеистую
    сеть (полигональные ячейки со светлыми границами — рисунок «хребтов»
    Worley), и рисунок **растянут** вдоль хода волн; у самого берега сеть
    сжимается в мелкую и гаснет в блеске мокрого песка.
  - **Два масштаба сразу**: мелкая сеть от ветровой ряби (ячейки 10–20 см) и
    широкая модуляция от зыби и от донных валиков-ряби песка.
  - **Контраст падает с глубиной**: на мелком — резкие нити, глубже сеть
    крупнее, мягче, и цвет воды забирает её. То есть каустика живёт в полосе
    глубин: рождается от нескольких сантиметров, к 2–3 м в этой мутной воде
    исчезает. Размытие с глубиной — физическое, это и есть честная «мягкость».
  - Это **аддитивный свет по дну с мягким полом**: между нитями не темно, там
    просто освещённый песок. Фактура дна (зёрна, ракушки, тёмные минеральные
    крупинки, тина) **видна везде** — каустика её модулирует, а не закрашивает.
  - **Блеск солнечной дорожки на поверхности — отдельный слой**, ярче каустики
    и живёт на воде, а не на дне; путать их нельзя.
  **Решение, которое предлагаю (исследование за мной, выбор за Денисом):**
  **A. Аналитическая каустика из того же поля волн** — основной путь. Свет
  фокусируется кривизной поверхности: яркость ≈ `1 / |1 + d·∇²h|`, где `d` —
  глубина, `∇²h` — лапласиан высоты воды. У нас `h` уже есть аналитически:
  поезда Герстнера (`gerstnerWaves.js`) плюс рябь из облачного шума
  (`waterRippleHeight` в `waterShading.js`). Значит рисунок на дне **совпадёт
  с волнами на поверхности** сам собой, без карт и без симуляции пруда,
  работает по всему морю и у берега, масштаб растёт с глубиной автоматически.
  Цена: несколько выборок шума на пиксель дна — только там, где `groundY < 0`.
  Резкость = показатель степени и ширина ядра, которая **растёт с глубиной**,
  плюс затухание по мутности (`waterTurbidity`) — три параметра, каждый со
  своим физическим смыслом (в отличие от нынешней «резкости», которая ничего
  внятного не делает).
  **B. Готовая тайловая карта каустики** — дёшево, но не согласована с волнами
  и заметно повторяется; годится только как запасной вариант для слабых GPU.
  **C. Подводный шейдер на всё море** (идея Дениса) — отдельный поздний слой:
  объёмное затухание по глубине, столбы света у поверхности, взвесь. Дороже,
  но даёт то, чего каустика на дне не даёт. Делать **после** A, не вместо.
  **Оптимизация**: считать только на дне и только до глубины ~3 м; гасить по
  метрам-на-пиксель (`fwidth`), как уже гаснут кружево пены и рябь, — эффект
  мелкий, вдали он обязан растворяться в ровное подсветление, а не мерцать;
  на слабом профиле — одна октава вместо двух.
  **Плюмбинг**: дно рисует ландшафт, значит вода должна отдавать ему параметры
  волн так же, как уже отдаёт поле пены (holder `coast.foamField`) — общий
  модуль с функцией высоты, чтобы поверхность и дно считали одно и то же.

- 2026-09-08, **КАУСТИКА — задача от Дениса (записано, не сделано).**
  Как устроено сейчас: `shelfCaustics` в `terrainMaterial.js` живёт **только
  внутри квадрата пруда** (`uPondExtent`, 82 м) и читает карту нормалей
  симуляции ряби (`uPondNormalMap` ← `runtime.normalTargetRef`): четыре
  выборки нормали, `refract` с n≈0.75, проекция на дно по глубине, площадь
  ячейки → яркость; за краем квадрата гаснет за 4 м. Ползунки в редакторе
  (Ландшафт → Дно): интенсивность, масштаб, **резкость**, мутность воды.
  Что говорит Денис: каустика **слишком резкая**, и по ощущению её толком
  не отрегулировать (интенсивность работает, остальное — нет; возможно, у него
  давно сбиты значения — `causticsSharpness` 0.84, «АО и плотность дна» 0.99).
  Сама по себе она **неплохая**, её стоит изучить и, возможно, оптимизировать.
  Чего он хочет: каустика должна получать рисунок **от карты нормалей или
  другой процедурной карты**, а не только от симуляции пруда, и работать
  **у берега** — «на берегу это было бы очень красиво».
  Техническая связка, которую нельзя потерять: когда новая вода заменит
  симуляцию пруда, `uPondNormalMap` исчезнет и каустика погаснет вместе с ним.
  Значит при переносе: источник рисунка — нормаль новой воды (проход карты
  нормалей Герстнера + рябь, либо аналитическая каустика из тех же поездов и
  «погоды»), область — весь берег в поле зрения, а не квадрат 82 м; мягкость —
  отдельный, честно работающий параметр (ширина ядра/размытие по глубине),
  плюс затухание с глубиной и по мутности. Считать по возможности от того же
  поля, что и вода, чтобы блики на дне совпадали с волнами на поверхности.
  Смежное, обсудим позже: отдельный физический материал воды ради рисования
  пальцем/курсором Денис считает избыточным — вероятно, сгущение сетки в зоне
  вокруг курсора и рябь по всей поверхности воды вместо квадрата пруда.

- 2026-09-08, **СПИСОК ДЕНИСА ПО НОВОЙ ВОДЕ — чинить по одному, с отчётом на
  каждый пункт** (его прямая просьба: «исправить по одиночке; одна проблема —
  говоришь, что видел и решил, и берёшься за следующую»). Порядок — его:
  1. **Вал не врезается в берег, а растворяется.** Некрасиво и нереалистично.
     Причина: в `surfRunupAlpha` три гасящих множителя разом (заплеск за 4 м до
     предела, `uSpent` за 8 м, песок выше воды) плюс обвал высоты в `surfAt`
     до 15 % на последних метрах — лента исчезает в воздухе, не дойдя.
     Должно: бор доходит до предела заплеска и там утончается в лист, а пена
     остаётся на песке (канал заплеска в поле уже есть).
  2. **Тон вала не совпадает с водой у берега.** Лента шейдится без члена дна
     (`bed`), а вода вокруг — с ним: на мелководье вода уходит в песочный тон,
     а вал остаётся зелёным. Шейдер читается как несогласованная система.
     Должно: одна вода — один расчёт дна и глубины для всех трёх поверхностей.
  3. **Эффект губы испарился в движении.** «Выброс губы», «Подъём губы»,
     «Наклон гребня» видны только в стоп-кадре: при валу 0.45 м на уклоне 1:13
     обрушение укладывается в пару метров и проходит за доли секунды.
     Должно: длительность обрушения масштабируется высотой волны и уклоном
     (число Ирибаррена), чтобы губа читалась на ходу.
  4. **Пропали подводные текстуры ландшафта** (тина, ил, водоросли, ракушечник).
     Причина: новая открытая вода **непрозрачна** и закрывает дно, а мой член
     `bed` красит его ровным `uBedColor` вместо настоящего материала. Старая
     вода показывала дно через рефракцию (`refractionCapture.js`,
     `WaterReflections.jsx`) — Денис: «старый материал нравится больше, он
     чистый и в нём видны все нужные фактуры». Должно: дно сквозь воду —
     настоящее (рефракция продукта или чтение материала дна), не заливка.
  5. **Валов всего три-четыре и только с одной стороны**, справа они косые.
     Причина: прибой живёт только на отрезке берега `ALONG0…+CREST_LENGTH`
     (280 м у корня косы), остальные ~1300 м берега без прибоя; косые справа —
     рефракция на отмели косы (физически верно, но видно только на обрезке).
     Должно: прибой идёт по всему видимому берегу.
  6. **Пена на большом расстоянии снова пропала — это обязательно.** По
     умолчанию барашки должны быть видны далеко. Порог на якобиане (0.55/0.15)
     при бюджете крутизны 0.7 почти никогда не срабатывает.
     Должно: умолчание, при котором пена видна до горизонта, и проверка,
     что она есть на дальности (замер доли пенных пикселей у горизонта).
  Общее замечание Дениса: «старые шейдеры смотрятся богаче, их можно донастроить
  и всё» — новая вода не имеет права быть беднее старой по фактурам дна,
  отражениям и мягкости; это условие приёмки, а не вкусовщина.

- 2026-09-08, Claude — **НОВАЯ ВОДА: ИТОГ СЕССИИ, ЧИТАТЬ ПЕРВЫМ.** Ветка
  `claude/handoff-water-continuation-4ac5a4` влита в локальный `main`
  fast-forward (push не делался; `main` на GitHub = живой сайт — push только по
  просьбе). Последний коммит **`2b5b016`**. Цепочка: `fdff5a9` коллекция 13 →
  `c01c805` дальняя вода → `bb9dc9e` ленты → `2ec9ecd` пена с памятью →
  `4ed5b90` губа по физике + объёмный вал → `a0224d6` берег в лаборатории,
  изобата → `272c113` карта глубин, погода, удар → `4501b9e` заплеск →
  `63f69b9` вода на сетке берега, пена до горизонта, часы → `2b5b016` дно по
  Беру–Ламберту. Лаборатория: `asset-lab.html?collection=water` (порт 41215).
  **В сцену ничего не подключено**: старая вода продукта (`WaterSurface.jsx`,
  `useWaterRuntime.js`, `FarWaterSurface.jsx`) работает как раньше; авторские
  настройки и камеры не тронуты; ворота `lint`/`build`/`check:water` зелёные.

  **Карта модулей новой воды** (`src/components/effects/water/`):
  - `gerstnerWaves.js` — 4 поезда Герстнера, бюджет Σ Q·k·A ≤ 0.8 (гарантия
    без самопересечений), «погода» `GERSTNER_WEATHER` — одна таблица для GLSL
    и JS (`gerstnerWeatherAt`), `gerstnerDisplace` (+ орбитальная скорость),
    `gerstnerPixelSlope` (неразрешённая сеткой доля в нормаль пикселя),
    `gerstnerFold` (барашки от незатухшего поля до горизонта, fwidth-AA),
    `gerstnerWhitecapMask` (пятна). `radialWaterGeometry.js` — меш от камеры
    до горизонта (~32k треугольников).
  - `GerstnerWaterSurface.jsx` — открытая вода; владеет полем пены
    (`useFoamField`), двигает часы воды (`timeline`, приоритет −30), у уреза
    `discard` по карте глубин (5 см), в полосе берега отдаёт воду за швом
    (`uShoreBand`), дно сквозь воду по Беру–Ламберту.
  - `waterShading.js` — общий шейдинг всех водных поверхностей:
    `shadeWater(world, n, view, pixel, foamUv, coverage, age, thickness, lift, bed)`;
    пена = тело с рваным краем + пузырьки (Worley) + молочная вуаль в
    координатах потока (`waterFlowUv`), возраст разъедает; вода, смотрящая
    вниз, отражает воду; дно — мокрый песок ×0.55 (`uBedColor`).
  - `foamField.js` — поле пены RGBA16F 768² в окне до 400 м впереди камеры:
    R плотность, G возраст, B мокрый песок, A лист заплеска; адвекция
    орбитальной скоростью + ветром, на песке — сток по уклону из карты глубин,
    впитывание 2 с, сушка `foamDry`; источники: складывание гребня (якобиан ×
    маска пятен) и боры лент `uBore[i] = (q, сила, полуширина, фронт набега)`;
    holder `coast.foamField` читают `ShoreWater` и ландшафт.
  - `surfProfile.js` — разрез обрушения: волна Bosboom & Stive (`f(θ)`, θp и
    размах совпадают с листом Дениса «Геометрия прибоя»), вздыбливание
    (сжатие фронта + наклон гребня), губа — баллистическая струя `x=Δu·a,
    z=w₀·a−g·a²/2` с толщиной, изнанкой, тенью трубы, `surfPlungeTime`;
    приземление → всплеск, вал, бор.
  - `BreakingWaves.jsx` — лофт разреза вдоль гребня в координатах берега; 4
    ленты по расписанию; лист + оболочка объёмной пены (raymarch 12 шагов по
    3D-шуму облаков, Beer/powder); высота вдоль гребня по погоде, линия
    обрушения по секциям (`coastBreakLine.js`: первый переход глубже→мельче
    от −160 м к урезу на изобате H/0.78, «Рефракция» — насколько гребень
    повторяет её форму); стоп-кадр/«Фаза обрушения»; бор пишет в поле.
  - `coastFrame.js` — вода знает берег: GLSL-двойник (`coastLocal`,
    `coastPoint`, `coastSwellFade`), **карта глубин** `ShoreDepthMap`
    (2048×1024 HalfFloat, q −336…+16 м, ±1 м) → `coastGround` одной выборкой;
    holder `coast.shoreDepth`. `coastBreakLine.js` — чистая математика для
    node-проверки.
  - `ShoreWater.jsx` — вода у уреза на сетке берега 0.5×1 м (грунт в
    атрибуте): вода встречает песок ровно там, где песок; плёнка заплеска
    (`swashFilm`) поднимает воду на песок по каналу A поля.
  - Ландшафт: `terrainMaterial.js` + `AzovTerrain.jsx` (prop `swash`) читают
    поле: мокрый песок `max(wet, поле.b)`, пена на песке из `поле.r` в его
    сетчатом кружеве; аналитическая волна ландшафта осталась (в сцене ≈ 0).
  - Лаборатория `src/water-lab/WaterLab.jsx`: настоящий `AzovTerrain`
    опубликованной сцены (без его воды), камеры в координатах берега
    (`at(q, s, y)`), стоп-кадр, сохранение настроек `ddg_water_lab_v1`,
    умолчания = тюнинг Дениса (вал 0.45 м, волна 11.5×0.57, порог пены 0.8/0.4).
  - Проверки `npm run check:water`: бюджет крутизны и меш; согласие
    сигнатур/юниформов шейдеров; θp/размах/время падения; линия обрушения
    (пляж, коса, секции); таблица погоды GLSL = JS.

  **Готово** (в лаборатории, на GPU проверено компиляцией и кадрами): зыбь
  от камеры до горизонта с погодой; барашки до горизонта пятнами; пена с
  памятью + возраст; губа по физике с трубой, тенью и изнанкой; объёмный вал;
  изобата и рефракция у косы; заплеск: мокрый песок, кружево, плёнка; вода на
  сетке берега без провалов под полигоны; часы с паузой; сохранение настроек.
  **Не готово**: брызги частицами с кончика губы; рост высоты по Грину при
  обмелении; пологость шельфа как настройка сцены (см. вопросы); цвета и
  яркости — подбирать Денису; **интеграция в продукт** — см. карту ниже.

  **Карта зависимостей продукта от старой воды** — всё это должно работать
  так же после переноса (бесшовно), проверять по пунктам:
  1. `WaterSurface.jsx` (пруд 82 м, `waterExtent`) + `useWaterRuntime.js`
     (пинг-понг симуляция ряби: высота, карта нормалей, пробы). Заменить на
     Герстнер + рябь-окно 48–64 м за камерой (в плане, не сделано); из
     симуляции убрать шумовой форсинг `noise(uv*8)` — источник «крестов».
  2. **Курсор** — `WaterInteractionPlane.jsx` (луч → плоскость → импульс в
     очередь `waterImpulseQueue.js`, уточнение по пробам). Нужен слой ряби
     поверх Герстнера, куда пишет курсор; палец рисует по всей воде.
  3. **Лодка** — `FloatingBoat.jsx` + `boatDynamics.js`: 5 проб высоты/нормали
     (fence-readback) и след (импульсы). Новая вода аналитическая: дать JS-
     пробы `sampleGerstnerAt(x,z,t)` (двойник `gerstnerDisplace` + погода) —
     без readback, дешевле; след лодки — в слой ряби.
  4. **Рыбы** (`HomeFishSchool`, пробы 8 Гц), **чайки** (`HomeSeagullFlock`,
     `SeagullLandingHabitat`, пробы при посадке) — те же JS-пробы.
  5. **Кувшинки** (`SurfaceVegetation.jsx`, `surfaceVegetationAnchors.js`) —
     якоря к поверхности: высота/нормаль из проб.
  6. **Скульптура в воде** (`StaticSculpture.jsx`, `blackStoneMaterial.js`) —
     ватерлиния/мокрая полоса и отражения; проверить, откуда берёт уровень.
  7. **Отражения/рефракция** — `WaterReflections.jsx`, `reflectionContext.js`,
     `refractionCapture.js`, `opticsGeometryLod.js` (планарные проходы для
     плоскости y=0, искажение по нормали). Перенести на радиальный меш: та же
     техника, нормаль из Герстнера + рябь; облака (`CloudSceneContext`) в
     отражении — сохранить. Это и есть «органичность» старого шейдера.
  8. **Каустика на дне** — ландшафт читает `uPondNormalMap` из симуляции
     (`runtime.normalTargetRef`); дать карту нормалей Герстнера (проход) или
     аналитическую каустику.
  9. **Дно** — `Seabed.jsx`, `UnderwaterAlgae.jsx` (тина/водоросли через
     рефракцию), `coastBandCoversPond`.
  10. **Дальняя вода** — `FarWaterSurface.jsx` + `farWaterGeometry.js` +
      `farWaterOptics.js` удалить; **танкер** (`HomeTanker`) стоит на дальней
      воде и качается по `coastWeather.swell` — посадить на Герстнер.
  11. **Полосы воды ландшафта** (`TerrainStrip water` → `WaterSurfaceV2
      shoreMode`, `settings.waterVisible`) и аналитический заплеск `coastWave`
      в `terrainShader.js` — заменяет `ShoreWater` + поле; оптические проходы
      ландшафта режут пляж по `coastWave` (`uTerrainOptics`) — перевести на
      поле/плёнку.
  12. **Звук** — эмиттер берега `audioRuntime.updateEmitter('shore')` в
      `AzovTerrain` — можно оставить.
  13. **Редактор и публикация** — новые ключи (волны, прибой, пена, вид) в
      `publishedHomeSceneKeys.js`, нормализация, снимки камер, вкладка «Вода»
      в редакторе (RU/EN, в существующем стиле), перенос через «В проект»;
      `publishedHomeSceneSettings.js` руками не трогать.
  14. **Бюджет** — `qualityProfile`: на телефоне поле 512², окно меньше,
      оболочка 8 шагов, карта глубин 1024×512; собственную цену воды мерить в
      продукте (в лаборатории 45–70 кад/с при 1.4–2.9 млн треугольников
      ландшафта на ближних LOD — вода в этом не видна). Смоук-тест и
      `scripts/check-water-seam.mjs` (шов пруд/дальняя вода) устареют — заменить.

  **Внимание при следующем запуске лаборатории у Дениса**: его ползунки теперь
  сохраняются в localStorage, а «Порог пены»/«Мягкость» сменили смысл — порог
  стоит на якобиане поля волн (1 — гладко, меньше — гребень складывается),
  прежние 0.80/0.40 в новой мере заливают пеной всё море. Нажать «Сброс»
  (он забывает сохранённое) или увести порог к 0.55/0.15.
  **Режим работы с Денисом** (его просьба): математика и логика — агент;
  визуальная оценка — Денис сам в лаборатории; в конце ответа — список «что
  посмотреть» и вопросы. Умолчания лаборатории = его текущий тюнинг.
  **Открытый вопрос — шельф**: у опубликованной сцены дно уходит на 2.75 м за
  36 м (`waterDepthMeters` пруда, зашитый `exp(q/12)` в `coastHeight`) —
  волна 1.1 м ломается в 8.6 м от уреза, 0.45 м — в 3 м; сейчас линия сдвинута
  ручкой «Сдвиг обрушения» −13.5 м. Честно — отдельная настройка сцены
  «пологость шельфа», не трогающая глубину пруда (водоросли, мидии). Ждёт «да».
  **Грабли**: `smoothstep(a,b,x)` с a>b — UB в GLSL (ANGLE→Metal давал 0 в
  программе ландшафта) — писать `1 − smoothstep(b,a,x)`, веса окон через
  `clamp`; в продукте вне воды ещё три таких места (`scenePostShaders.js:556`,
  `skyModel.js:118`, `pages/Map.jsx:420`). `coastHeight` в пиксельном шейдере
  — 18 кад/с, только карта глубин. `discard` выключает early-z — он один, в
  открытой воде. Прозрачная открытая вода лишает ландшафт раннего отсечения.
  Лаборатория берёт файлы воркдерева живьём — любая отладочная подсветка
  видна Денису; только за URL-флагом и сразу убирать. `node_modules` в
  воркдереве могут быть битыми («R3F: Hooks can only be used within the
  Canvas») — `npm ci` там, но сервера Дениса гасить/перезапускать и вкладки
  перезагружать жёстко (AGENTS §4.6).

- 2026-09-08, Claude, вода — губа по физике и настоящий берег в лаборатории.
  **`4ed5b90`** — обрушение как лофт с математикой: профиль волны Bosboom & Stive
  `f(θ)=cos θ+0.18cos 2θ+0.16sin 2θ` (θp и размах совпадают с листом «Геометрия
  прибоя» до 1e-4 — `surfProfile.check.js`), вздыбливание сжатием передней грани
  и наклоном гребня, губа — баллистическая струя `x=Δu·a, z=w₀·a−g·a²/2` с
  толщиной, тонущей к кончику, изнанкой и тенью в трубе; приземление → бор,
  лист отдаёт себя валу. Лофт стоит на зыби Герстнера (тот же `gerstnerDisplace`,
  те же затухания), края совпадают с открытой водой и растворяются в ней.
  Второй меш на том же лофте — оболочка над пеной, в ней 12-шаговый raymarch
  по 3D-шуму облаков: комки, разрывы, Beer/powder — пена по принципу облаков,
  не картой прозрачности. Вал пишет в поле пены; барашки в поле — пятнами;
  окно поля до 400 м при 768². Шейдинг: вода, смотрящая вниз, отражает воду,
  а не зеркало неба; тонкий лист пропускает свет неба; пена — рваное тело с
  пузырьками и молочной вуалью в координатах потока. В лаборатории: стоп-кадр
  и «Фаза обрушения», ползунки выброса/подъёма/толщины губы, наклона гребня,
  объёма и плотности вала, камера «Губа», честный счётчик кадра.
  **Marching Cubes отвергнут для сайта с цифрами**: участок гребня 12×3×3 м при
  вокселе 8 см — 216 тыс. ячеек × 15 вершин = 3.2 млн вершин в кадр плюс
  заполнение поля; лофт + оболочка + частицы дают нависание, комки и отрыв за
  ~2 % цены. Брызги — частицами (как whitewater в Houdini), ещё не сделаны.
  **`coastFrame.js`** (этот коммит) — вода знает берег: общий GLSL-двойник
  берега (`coastLocal`, `coastHeight`, коса) во всех трёх водных модулях.
  Лаборатория «Вода» рисует **настоящий `AzovTerrain`** опубликованной сцены
  (без его старой воды: `waterVisible:false`), синтетический пляж удалён.
  Гребни валов лежат на **изобате `глубина = H/0.78`** — JS считает линию по
  49 секциям (первый переход глубже→мельче от −160 м; `coastFrame.check.js`),
  шейдер интерполирует; ползунок «Рефракция» — насколько гребень повторяет
  форму изобаты (0 — прямой, 1 — точно по ней); у корня косы линия уходит с
  −8.6 до −40 м и гребень изгибается. «Сдвиг обрушения» — ручная поправка
  к изобате. Зыбь гаснет по **глубине** (`coastHeight`), а не по расстоянию
  до уреза материка — коса, мыс и отмели гасят одинаково; у уреза вода
  становится прозрачной в последние сантиметры глубины — двум поверхностям
  нечего делить, урез рисует мелкая сетка песка (это лечит мерцание уреза,
  которое Денис видел на плоском пляже). След бора в поле пены — по `q`.
  Камеры лаборатории заданы в координатах берега (`coastPoint`), новая «Коса».
  **Следующий коммит** (по замечаниям Дениса): (1) урез считается **в пикселе**
  по **карте глубин у берега** — `ShoreDepthMap` в `coastFrame.js` один раз
  рендерит `coastHeight` в координатах берега (2048×1024, q от −336 до +16 м,
  высоты ±1 м в 8 бит), вода читает её одной выборкой и в вершинах (затухание
  зыби по глубине), и в пикселе (`discard` у уреза) — вода непрозрачная, зубцов
  по диагоналям треугольников нет; прямой `coastHeight` в пиксельном шейдере
  давал 18 кад/с и выброшен. (2) Лента прибоя получила `polygonOffset` — её
  края лежат на зыби и не дерутся с ней за глубину. (3) «Порывы» у Герстнера:
  медленное аналитическое поле по площади гнёт фазу поездов (гребни блуждают)
  и меняет амплитуду группами, огибающая ≤ 1 — бюджет крутизны цел; ползунок
  во «Волнах». (4) Удар губы: всплеск объёма пены в точке падения, вал держится
  до конца бора, бор выше (0.55 H). (5) «Сдвиг обрушения» −12 м по умолчанию:
  у опубликованной сцены шельф уходит на 2.75 м за 36 м (уклон ~1:13; на листе
  Дениса 1:50), волна 1.1 м по физике ломается в 8.6 м от уреза и бору некуда
  бежать — честное решение: сделать пологость шельфа (`exp(q/12)` в
  `coastHeight`) настройкой сцены, это решение Дениса.
  Проверено на GPU (ANGLE metal): ошибок нет, 40–70 кад/с в зависимости от
  ракурса при 2.9 млн треугольников — почти всё ландшафт продукта на ближних
  LOD; собственную цену воды мерить в продукте, здесь её не видно.
  **Заплеск — связь с берегом** (следующий коммит): поле пены стало носителем
  заплеска: канал B — «мокро»; бор пишет в `.w` бора фронт набега (q кромки воды
  на песке), песок до фронта мокрый, на кромке ложится кружево; на песке пена
  впитывается за 2 с и стекает по уклону из карты глубин (backwash), песок сохнет
  за `foamDry` (40 с). `terrainMaterial` читает поле (`uSwashField/uSwashWindow`,
  `AzovTerrain` prop `swash`): `wet = max(wet, поле.b)`, пена на песке — поле.r в
  сетчатом кружеве самого ландшафта; его аналитическая волна остаётся (в сцене
  она почти выключена, `terrainWaveHeight` 0.02). Вода у берега показывает дно:
  `shadeWater` получил `bed` (0..1 по глубине из карты), «Цвет дна» в «Виде».
  Бор на песке сплющивается в лист (высота × (1 − 0.85·smoothstep до заплеска)).
  Карта глубин — HalfFloat. Вода обрывается там, где по карте глубина < 5 см:
  ближе к урезу за кромку спорили бы её `discard` и тест глубины по сетке
  ландшафта (лестница с шагом сетки); открытая полоса у ландшафта мокрая.
  **Грабли GLSL**: `smoothstep(a, b, x)` с a > b — неопределённое поведение;
  в программе ландшафта ANGLE→Metal вернул 0, и заплеск не доходил, пока веса
  окна не переписаны через `clamp`, а обратные ступени — как `1 − smoothstep`.
  Остались в продукте вне воды: `scenePostShaders.js:556`, `skyModel.js:118`
  (возможно JS), `pages/Map.jsx:420` — работают на текущих GPU, но это лотерея.
  **По замечаниям Дениса 08.09 (вечер)**, следующий коммит: (1) **часы воды** —
  `createSceneTimeline` (пауза больше не сбрасывает волну: часы r3f шли по
  стенному времени); все водные модули берут `timeline.elapsed`, Герстнер
  двигает часы на приоритете −30. (2) **Настройки лаборатории сохраняются** в
  localStorage (`ddg_water_lab_v1`), «Сброс» забывает; умолчания — тюнинг
  Дениса (вал 0.45 м, волна 11.5×0.57, порог пены 0.8/0.4 и т. д.).
  (3) **Пена до горизонта**: барашки считаются от незатухшего поля волн
  (`gerstnerFold`, fwidth-сглаживание) с аналитической маской пятен
  (`gerstnerWhitecapMask`), общей для поля и дали; погода/маска — одна
  таблица `GERSTNER_WEATHER` для GLSL и JS (проверяется). (4) **Вода на сетке
  берега** — `ShoreWater.jsx`: полоса 0.5×1 м от шва (q −24) до +8 м с высотой
  грунта в атрибуте; заплеск — канал A поля («лист сейчас», сходит с верха
  пляжа быстрее), плёнка `swashFilm` поднимает воду на песок; у уреза вода
  берёт тон мокрого песка и блестит — вода больше не проваливается под
  полигоны. Радиальная сетка в полосе отдаёт воду за швом. (5) **Гребень**:
  высота вдоль гребня — по погоде зыби в точке обрушения, и линия обрушения
  считается по секциям с этими высотами (большие секции ломаются раньше и
  дальше) — естественный пил и разрывы.
  Дно сквозь воду — по Беру–Ламберту (`bed = exp(−3·глубина)`), и это **мокрый**
  песок (×0.55, как у мокрой полосы ландшафта): у уреза вода и песок сходятся
  в один тон под френелевским бликом; первая версия мешала цвет дна через
  «просвет тонкого листа» и давала ярко-зелёный ковёр с жёсткой границей.
  Наблюдение Дениса: старый шейдер воды продукта был мягче/органичнее за счёт
  размытых отражений и ряби симуляции — при интеграции новая вода должна
  получить отражения/рефракцию продукта, а не только небо.
  **Не сделано / дальше**: брызги частицами с кончика губы; тонкая плёнка
  воды на песке (сейчас заплеск — мокрый песок + кружево, без зеркальной
  плёнки); рост высоты по Грину при обмелении;
  барашки в открытой воде всё ещё повторяют решётку Герстнера — нужна
  пространственная огибающая (порывы); цвета/яркость пены и сила выброса —
  подбирать Денису на GPU (софтверный рендер серый); интеграция в WaterScene
  (заменить `FarWaterSurface` и воду полос ландшафта на зыбь+валы, отражения/
  рефракция продукта). Авторские настройки не тронуты, в сцену не подключено.

- 2026-09-07, Claude, вода — шаг 1 сделан: **`fdff5a9`**, коллекция 13 «Вода»
  в лаборатории (`asset-lab.html?collection=water`, облака стали 14).
  Продуктовые модули: `water/radialWaterGeometry.js` (радиальный меш за камерой,
  32k треугольников при 112×144), `water/gerstnerWaves.js` (4 поезда, наборы,
  бюджет крутизны Σ Q·k·A ≤ 0.8 — `npm run check:water`),
  `water/GerstnerWaterSurface.jsx` (пена по якобиану из шума облаков
  `cloudNoise.js`: кружево — грани Worley, эрозия — мелкая октава, свет —
  Beer/powder). Лаборатория: вкладки Волны/Пена/Вид/Свет, ракурсы Берег/Сверху/
  Гребень, пресеты Штиль/Бриз/Волнение/Шторм, каркас. Проверено headless
  Playwright (SwiftShader): без ошибок, ~30 кад/с на софтверном GL.
  **`c01c805`** — дальняя вода: неразрешённая сеткой доля каждого поезда считается
  нормалью на пиксель (со сглаживанием гребня по fwidth), пена и рябь гаснут по
  метрам-на-пиксель, один Q на все поезда (первичный складывается первым),
  кружево пены вдоль гребня. Проверено сверху/берег/гребень без каши.
  **`bb9dc9e`** — прибой лентой (`water/BreakingWaves.jsx`, общий шейдинг воды
  `water/waterShading.js`): профиль из фазы обрушения — вал, стена, губа по дуге
  с закруткой под себя, схлопывание в бор до песка; пил вдоль гребня, период и
  разброс высоты; толщина губы → транслюценция. Вкладка «Прибой», синтетический
  пляж, ракурсы Прибой/Труба/Прибой сверху. Закрутка видна с торца гребня
  («Труба») на 11–16-й секунде после загрузки. Форму губы, пену на ней и
  цвета подбирать с Денисом на GPU — софтверный рендер серый.
  **`2ec9ecd`** — пена с памятью (шаг 1.5): RG-поле 512² в окне, которое едет
  впереди камеры (R — плотность, G — возраст). За тик поле сносится
  орбитальной скоростью Герстнера плюс ветер, плотность гаснет по времени
  жизни, источники — складывание гребня (якобиан) и обрушенная стена каждого
  живого вала: ленты прибоя пишут свой бор прямо в юниформу прохода. Возраст
  разъедает кружево как край облака — пятно истончается в нити, а не пропадает
  целиком. Вне окна — прежние аналитические барашки. `gerstnerDisplace` теперь
  отдаёт и горизонтальную орбитальную скорость. Во вкладке «Пена» — «Память
  пены», «Живёт на воде», «Плотность пены», «Окно памяти», «Снос ветром».
  Проверено на GPU (headless Playwright, ANGLE metal): ошибок нет, из ракурса
  «Прибой сверху» доля пенных пикселей 14.7 % с памятью против 0.02 % без,
  разницы в кадрах не видно (120 кад/с упирается в vsync). Статическая
  проверка согласия шейдеров — `npm run check:water`.
  Не сделано: обмеление и берег (песок, заплеск, время жизни на песке —
  для них нужен `coastHeight`), рябь под пальцем, интеграция в WaterScene
  (отражения/рефракция продукта). В сцену ничего не подключено; авторские
  настройки не тронуты.
  Замечено попутно: в этом воркдереве `node_modules` были битые — весь сайт
  и лаборатория падали с «R3F: Hooks can only be used within the Canvas
  component!». Лечится `npm ci` в воркдереве; к коду отношения не имеет.

- 2026-09-07, Claude: сессия Codex закрыта, его незакоммиченная работа разложена
  на два коммита в локальном `main`: **`e641008`** — шесть локальных 2K HDRI
  (ночь/пасмурно/солнце/закат/рассвет) с подписями RU/EN и дневной палитрой неба
  в `homeSceneLighting.js`; **`058a6bb`** — камеры с двумя форматами снимков
  (`editorCameraState.js`, расширенный `check:cameras`). Lint: 0 ошибок
  (1 warning в `CoastRocksPBR.jsx`), `check:cameras` прошёл, сборка не запускалась.
  Push не выполнялся. Побочный эффект HDRI-коммита: под дневным небом вода
  читается зеркалом — Денис это видел и принял как временное.
  Дальше — переделка воды по согласованному плану: один радиальный меш от камеры
  до горизонта (~25k треугольников вместо 248k у пруда 352²), волны Герстнера
  с ограничением суммарной крутизны ≤ 0.8 и пеной по якобиану, обмеление у
  берега по `coastHeight`, окно ряби 48–64 м следует за камерой (палец рисует
  по всей воде), удалить `FarWaterSurface`/`farWaterGeometry` и шумовой
  форсинг `noise(uv*8)` в симуляции (источник «крестов» под водой). Вода авторски
  растянута ползунком «Дальность дна» (1504 м) ради чистого горизонта с одним
  танкером — новый меш обязан покрывать это без доплаты. Старт после «давай»
  от Дениса: сначала коллекция «Вода» в лаборатории, затем sandbox 41213.
  **Пена «по подобию облаков»** (идея Дениса, 07.09, в тот же план воды):
  пена — не функция фазы волны (так сделан нынешний `coastFoam`, поэтому её
  «многовато» и она везде сразу), а *состояние с памятью*. Буфер пены RG в том же
  окне 48–64 м за камерой, что и рябь (ping-pong ~512²): R — плотность, G — возраст.
  Источники за тик: (1) складывание гребня — якобиан Герстнера ниже порога
  (барашки в открытой воде); (2) зона обрушения — высота волны > 0.78 глубины
  по `coastHeight` (вал ломается, всплеск, как на фото Азова); (3) заплеск — тонкий
  слой, бегущий по песку с фронтом волны. Между тиками буфер адвектируется
  горизонтальной скоростью Герстнера + откатом (на песке — вниз по градиенту
  `coastHeight`); плотность гаснет на воде медленно (6–10 с), на песке быстро
  (впитывается, ~2 с) — «уходит в песок и в отлив». Наборы волн: огибающая из
  биений двух близких частот — каждые 5–8 волн большая, между наборами пены
  почти нет; это и есть «волны друг за дружкой». Вид — из облаков: те же
  `fbm`/`worley` из `cloudNoise.js` для кружева и рваных краёв, порог растёт
  с возрастом (эрозия как у края облака), освещение Beer + powder + просвет
  солнца по толщине, но без raymarch — один слой, одна выборка; густая пена
  белая с мягкой теневой изнанкой, тонкая — кружево на зелёной воде. Вне окна —
  только аналитические барашки по якобиану. `coastWetness` остаётся для
  мокрого песка, его фронт берётся из той же фазы, что и заплеск. Цена: +1
  RG-буфер как у ряби, +3 шума на пиксель воды. Ползунки: наборы, время жизни
  на воде/песке, кружево, плотность. Порядок: после меша+Герстнера — буфер пены
  на плоскости в лаборатории, затем берег в sandbox.
  **Прибой с закруткой** (идея Дениса по True Surf, 07.09): труба — нависание,
  карта высот (Герстнер, симуляция, `coastWave`) его не умеет по определению,
  поэтому обрушение — отдельный слой поверх Герстнера. Лента гребень×профиль
  (~200×24 вершин, 2–4 волны живут одновременно): профиль — сплайн
  подошва→стена→губа→завиток, у каждой точки гребня фаза обрушения 0…1,
  точка обрушения бежит вдоль гребня (пил); после схлопывания лента гаснет
  в бор, который пишет в буфер пены с памятью — одна система с пеной.
  Правила рождения из океанологии: рост по Грину `H ∝ d^-1/4`, обрушение при
  `H/d ≈ 0.78` → линия гребня = изобата глубины `H/0.78` в координатах
  `coastLocal`, тип по числу Ирибаррена (пологое дно — рассыпающийся, крутая
  ступень — опрокидывающийся), плюс ручной ползунок. Транслюценция губы —
  толщина известна из профиля аналитически, без глубинных проходов.
  Швы: лента прибавляется к Герстнеру, подошва = та же функция, по краям
  добавка → 0. Прототип — вкладка «Прибой» в коллекции «Вода» с синтетическим
  пляжем и уклоном; форму губы и момент схлопывания подбирать с Денисом.

- 2026-09-07, UI облаков: в локальном `main` облака вынесены из «Света»
  в отдельную вкладку «Атмосфера → Облака» сразу после «Лучи» (RU/EN).
  Контролы и значения сохранены; вкладка проверена в редакторе 41212.
  Коммит `ui(editor): give clouds their own atmosphere tab`; push не выполнялся.

- 2026-09-07, **`930cdb2`**, по прямой просьбе Дениса новые камни подключены к сцене берега
  основного проекта / редактора 41212: `CoastRocksPBR.jsx`, лабораторные
  PBR-карты и формы, шесть вариантов валунов/осыпи и процедурная PBR-галька.
  Расстановка/масштабы взяты из существующих настроек, посадка и коллизии
  пересчитаны. Камеры и опубликованные настройки не редактировались.
  Проверки, сборка и браузерный осмотр пропущены по просьбе «быстро без проверок».
  Для обновления сохранённых React-мемоизаций нужно перезагрузить 41212.
  Изменения закоммичены отдельно; чужие незакоммиченные правки сохранены.

- 2026-09-07, живописные облака интегрированы в локальный `main` поверх
  **`db7c061`**; коммит **`16fc239`** (`feat(clouds): integrate procedural
  atmosphere with scene lighting and water`). Перенесён одобренный прототип `ae59cfe`,
  общий runtime подключён к WaterScene: солнце, CSM/обычные тени, растения,
  отражения воды, окружение и лучи с глубиной сцены. 14 новых настроек входят
  в редактор, обычные и рабочие камеры. Через штатную публикацию слой включён
  в общей сцене и семи обычных камерах; остальные нормализованные настройки
  совпали. Публикация штатно берёт общий фон из первой обычной камеры.
  Главный dev **http://localhost:41212/home/edit** перезапущен; сохранённая
  «Рабочая 3», время 11.3, все прежние поля локального состояния сохранены.
  Общая коллекция: **http://localhost:41215/asset-lab.html?collection=clouds**.
  Реальные кадры, отражения, low/high, post/туман, пауза, десять пиксельных
  GPU-проверок теней, scoped ESLint и production build прошли. Физический
  iOS и полный замер бюджета сцены не выполнялись. Подробности и пределы —
  `docs/painterly-clouds-lab.md`. Push/deploy не выполнялся. Ранее начатые
  изменения камер/HDRI оставлены незакоммиченными; в коммит облаков не входят.

- 2026-09-07, остановка доработки камней по просьбе Дениса: **`db7c061`**
  слит fast-forward из `codex/rocks-pbr` в локальный `main`. Срезы и угловатые
  выбоины связаны с маской смешивания двух PBR-слоёв; добавлен imagegen-излом,
  карты normal/roughness/AO/height, просмотр маски и параметры RU/EN.
  Треугольников столько же; карты GPU 24 / 6 MiB, галька сохраняет форму.
  Scoped ESLint, сборка лаборатории, 24 комбинации генератора и крупный
  ракурс прошли. Художественные итерации остановлены, физический iOS не проверен.
  Это перенос текущей лабораторной версии в основной проект; берег пока
  использует прежние модули. Авторские настройки и чужие изменения сохранены.
  Push не выполнялся; эта запись HANDOFF оставлена незакоммиченной поверх
  существовавших записей других агентов. Подробнее: `docs/rocks-pbr-lab.md`.

- 2026-09-07, прототип живописного неба: отдельный commit **`ae59cfe`**
  в ветке `codex/painterly-clouds`, worktree `.claude/worktrees/painterly-clouds`
  (база `9c9accd`). Коллекция 13 `clouds`, объёмные процедурные облака,
  общая проекция теней, лучи и три режима стоимости; открыто на
  **http://127.0.0.1:41216/asset-lab.html?collection=clouds**. Канонический 41215
  обслуживает рабочая копия камней; она и сервер камер не тронуты.
  Это самостоятельный лабораторный этап, не интегрированный в main/WaterScene
  и публикацию. В корне оставлена незакоммиченной только эта добавка к HANDOFF
  поверх чужих существовавших изменений. В своём worktree статус чистый.
  Проверены реальные дневной/вечерний/ночной/грозовой кадры, тени, качество,
  смена seed, пауза, узкая панель и ESLint новых файлов. Документ технологии,
  GPU-наблюдения и ограничения — `docs/painterly-clouds-lab.md` в worktree.
  Следующий этап: художественная оценка, затем согласованное подключение
  глубины сцены, материалов и водных отражений; push/deploy не выполнялся.

- 2026-09-07, PBR-камни в лаборатории: **`0d12344` слит fast-forward в локальный
  `main`**, исходная ветка `codex/rocks-pbr` (база `9c9accd`). Лаборатория
  **http://localhost:41215/asset-lab.html?collection=rocks** запущена из корня
  обычным `npm run lab`; временный сервер воркдерева остановлен. Добавлены
  плитчатый известняк, ракушечник, окатанные камни, шесть форм гальки, реальные
  сколы/выемки, карты альбедо/normal/roughness/AO/height, намокание и налёт.
  Модули — `src/terrain/rocks/`, исходные imagegen-карты и промпты —
  `assets-source/textures/rocks/`, генератор — `scripts/terrain/build-rock-textures.mjs`.
  Описание источников, физического масштаба и бюджета — `docs/rocks-pbr-lab.md`.
  Runtime-карты: около 3.1 / 0.7 МБ, оценка GPU 16 / 4 MiB; 17 инстансных
  групп в общем виде. Осмотрены три типа, микро/низ, галька, мокрый материал,
  оба режима света и максимальная плотность. Проверены 72 сочетания геометрии,
  scoped lint, сборка лаборатории, смена варианта/ракурсов и сброс без новых
  ошибок. На 390 CSS px устранено обрезание коллекции; физические iOS-устройства
  не проверялись. После переноса коллекция ещё раз просмотрена из главного чекаута.
  Новые модели подключены пока только к лаборатории; берег и его коллизии
  используют прежние модули. Следующий этап — согласованное внедрение в берег,
  мокрую кромку прибоя и отражения. Push/публикации не было. Все 16 ранее
  изменённых файлов камеры/света сохранили побайтовое содержимое при merge.
  Эта запись HANDOFF оставлена незакоммиченной вместе с предыдущими записями;
  чужие изменения и HDRI ниже остаются незакоммиченными.
- 2026-09-07, исправление состояния камер: база **`9c9accd`**, изменения
  оставлены незакоммиченными в `main` вместе с предыдущей заменой HDRI.
  Обычная и рабочая камера теперь хранят полный независимый снимок сцены:
  свет, атмосферу, постобработку и оба формата. «+» создаёт Desktop/Mobile
  с одинаковым текущим ракурсом; «Захватить» меняет только выбранный формат.
  Правки немедленно записываются в активную камеру, переключение сохраняет
  исходящую и применяет входящую; рабочая камера больше не меняет запомненную
  обычную. Сохраняются активная камера и формат после перезагрузки, дробный FOV;
  новые стандартные имена и ID не повторяют существующие. Публикация берёт
  общий фон настроек из первой обычной камеры, рабочие снимки остаются локальными.
  Логика — `lib/editorCameraState.js`, нормализация — `useHomeSceneSettings.js`.
  Старые рабочие камеры с одним ракурсом мигрируют в пару; отсутствовавшие
  исторические настройки света/обработки однократно наследуют текущую сцену.
  `npm run check:cameras` прошёл: 1200 смешанных действий, 240 переключений,
  60 циклов нормализации/сохранения с 427 полями снимка. Lint и build прошли
  (у сборки прежнее предупреждение о размере чанка). Во встроенном браузере
  проверены создание обоих типов, раздельный захват, независимость обработки
  и перезагрузка. Тестовые настройки sandbox восстановлены, вкладка закрыта.
  Главный dev **localhost:41212/home/edit** перезапущен и оставлен работающим:
  открыта авторская **«Рабочая 3»**, день 11.3, плёнка отключена. Все семь
  обычных камер после миграции совпали с исходными данными по каждому полю.
  `publishedHomeSceneSettings.js` не менялся; commit/push не выполнялись.
  Контракт записан в AGENTS.md §7 и скилле `ddg-render-runtime`.
  Доработка стартового загрузчика остаётся отдельной незавершённой задачей.
- 2026-09-07, замена HDRI по просьбе Дениса: база **`593ff04`**, изменения
  оставлены незакоммиченными в `main`. Скачаны шесть локальных HDR 2K от Poly Haven:
  ночь, пасмурно, ясное солнце, солнце с облаками, закат и розовый рассвет.
  Каталог и CC0-источники — `public/hdri/README.md`; подключение —
  `water/constants.js` и `WaterLights.jsx`. Названия RU/EN заменены в существующем
  меню; прежние ID сохранены для камер и настроек. Палитры воды согласованы с
  новыми вариантами. Имеющиеся авторские изменения в
  `publishedHomeSceneSettings.js` не трогались. По прямой просьбе пользователя
  проверки, сборка и просмотр сцены не выполнялись; commit/push не выполнялись.
  В главном dev с отключённым HMR новые карты появятся после обновления страницы.
- 2026-09-07, завершение интеграции косы: **`35ddb9f` слит в main и отправлен
  в origin/main**, база `4a026ce`, исходная ветка `codex/integrate-spit-scene`.
  Код рельефа уже был в main; в авторском файле
  отсутствовали параметры косы, поэтому главный dev и сайт её не включали.
  Из `127.0.0.1:41213` получены семь параметров и снимок «Коса» с Desktop/Mobile.
  Через `sanitizeHomeSceneSettingsForPublish` и штатный publish client коса
  включена в общей сцене и всех восьми прежних камерах; добавлена девятая
  **«Коса» (`camera-spit`)**. Старый id camera-4 занят авторской «Камерой 4» и
  сохранён. Проверка нормализованных снимков подтвердила отсутствие остальных
  изменений в прежних камерах. Перед переносом главный dev `localhost:41212`
  совпадал с опубликованной сценой. Расширенный diff файла включает явные
  значения новых настроек из sanitizer и штатное выравнивание корня с первой
  камерой. В главном dev публикация принята после повторного сравнения с
  исходной сценой (новых авторских изменений не было); рабочие камеры сохранены.
  Встроенный браузер показал косу с обоих сохранённых ракурсов, runtime сообщает
  `spit:true` и расширение дна до 480 м. Lint/build прошли, ссылки скилла проверены.
  Pages **34132745906** успешен; живой сайт отдаёт новую сборку
  `/assets/index-UfLrcdnu.js` и сообщает девять камер. При первом открытии
  некоторые текстуры загружались 60–80 секунд; это сетевое наблюдение,
  не измерение времени генерации геометрии или гарантированного FPS.
  Правила называют 41212 главным dev; добавлен скилл `ddg-render-runtime`.
  Запуск и создание пар Desktop/Mobile ещё остаются исследованными задачами,
  реализация которых ждёт отдельного сообщения Дениса о готовности.
  При старте dev зафиксировано React-предупреждение об обновлении
  `SceneReadyBeacon` из `EnvironmentCube`; учесть его при доработке запуска.
  В старой несведённой ветке `codex/site-layout-design` найден **`5eefbb3`**
  (30 августа), частичная правка загрузчика только главной страницы. Она не
  подключает готовность редактора и новых очередей рендера; использовать как
  материал для следующей задачи, не сливать всю ветку вместе с чужими изменениями
  портфолио. Ветки content-platform также не относятся к интеграции косы.
  Последний полный Checks **34128911772** на базе остановился в проверке
  непрерывности звуковых дорожек при возврате на главную; Pages был успешен.
- 2026-09-07, актуализация smoke после успешного запуска редактора в облаке:
  **`476d35a` слит и опубликован**, Pages `34127900008` успешен, на сайте проверена
  сборка `/assets/index-DHDb7aN_.js` и настоящий кадр без ошибок. Checks
  `34127900046` прошёл все маршруты, редактор, звук и WebGL fallback; затем нашёл
  устаревший контракт локальных настроек. В `codex/editor-smoke-contract` (база
  `476d35a`) `editorHeadingColor` и `editorCursor` учтены как локальные, добавлена
  обратная проверка против их попадания в публикацию. Контракт проверяется до
  запуска браузера; `SMOKE_PHASE=coverage node scripts/smoke-test.mjs` запускает
  только его, без сервера, браузера и cleanup процессов. Также smoke теперь
  открывает группу перед её разделом и выбирает видимость лодки по постоянному id,
  а не устаревшему номеру чекбокса. Сценарии и критерии проверки сохранены.
  Численный контракт и lint/build/bundle прошли. Навигация и именно «Лодка»
  проверены во встроенном браузере в несохраняемом preview. Последний коммит
  тестовой правки содержит эту запись; авторская сцена не переписывалась.
- 2026-09-07, уменьшение работы при старте растений: продолжение в
  **`codex/plant-atlas-mipmaps`**, база **`95200af`** (код апскейла — `1425708`).
  Последний коммит реализации содержит эту запись. В запекании impostor уровни
  mipmap теперь создаются после последнего из 24 ракурсов, а не после каждого.
  GPU harness подтвердил 72 → 3 пересчёта и точное совпадение RGBA для цвета,
  нормалей и координат на четырёх уровнях уменьшения (12 случаев). Восстановление
  target/tone mapping/viewport/clear alpha также проверено; сцена просмотрена.
  Lint, check:render-core, build и check:bundle прошли. Результаты — в конце
  `docs/render-upscale-2026-09-07.md`, harness — `scripts/check-plant-atlas-gpu.js`.
  Последний облачный Checks `34126704435` зафиксировал блокировку главного потока
  при открытии редактора, без полученных JS-ошибок. Это не подтверждённый CSS-баг
  и не доказательство, что единственная причина — mipmap. Pages `34126704384`
  успешен; выводы о физическом телефоне и общем ускорении по этому случаю не делать.
  Авторский файл настроек и состав/размещения растений не менялись.
- 2026-09-07, публикация ядра и следующий этап качества по прямой просьбе Дениса:
  **`4834ab6` уже слит в `main`, отправлен в `origin/main` и опубликован**.
  Pages run `34121589531` успешен, сайт denisdaragan.com просмотрен во встроенном
  браузере. Отдельный Checks `34121589512` упал на ожидании runtime-метрик через
  20 секунд; причина пока не подтверждена. Публикация и smoke — разные результаты.
  Продолжение — **`1425708`** из `codex/render-upscale-warmup`, база `4834ab6`;
  также слито в `main` и отправлено на сайт, Pages `34125681916` успешен.
  Проверена живая сборка `/assets/index-D8GLIEZd.js`, ошибок страницы нет.
  Checks `34125681864` прошёл публичные маршруты, но упал на видимости сцены
  редактора. В новом origin localhost:41213 при 1440×900 сбой не повторился.
  `codex/editor-startup-diagnostics` расширяет сбор ошибок также на исчезнувший
  контейнер (fallback и stack), не меняя таймауты или условия приёмки.
  Добавлены FSR 1 EASU/RCAS с RU/EN контролами, ограничением
  уменьшения маленького кадра, правильным однократным ACES и плёнкой после апскейла;
  исправлен выбор стороны FXAA. Общая очередь калибровки растений допускает не более
  двух шагов за кадр, прекращая запуск новых после 3 мс; отдельный шаг непрерываемый.
  Все 12 групп завершили прогрев, первая компиляция/чтение всё ещё может дать паузу.
  В dev можно измерять GPU при выключенном автобюджете. Smoke получил диагностику
  таймаута без ослабления приёмки. Авторский `publishedHomeSceneSettings.js` не менялся.
  Проверки: lint, check:render-core, build, check:bundle; 15 GPU случаев EASU/RCAS
  и 5 случаев production цветового пути; день/ночь, вода/растения, AA/AO, переключения,
  пауза и маленький кадр во встроенном браузере. Внешний Chrome не запускался.
  Подробности: **`docs/render-upscale-2026-09-07.md`**. Сравнимый прирост FPS и
  физические телефоны/NVIDIA/AMD не подтверждены. FSR по умолчанию выключен.
  Дальше: синхронный старт ассетов, повторная калибровка LOD при смене света,
  измерения на физических устройствах, затем оценка temporal AA. Текущий чекаут —
  `main`, код закоммичен; эта запись завершает документацию отдельным коммитом.
  Sandbox41213 перезапущен для новых публикуемых ключей с прежним config/cache
  `output/playwright/water-optics/`; каталог нужен живому серверу. Временный preview
  завершён, обычный `/home/edit` снова читает авторскую сцену. На сайте FSR по
  умолчанию выключен; включение выполняется в «Рендер → Постобработка».
- 2026-09-07, ядро света и качества, **после разрешения Дениса на реализацию**:
  работа в основном чекауте, ветка **`codex/lighting-core`**, база **`3b6eabf`**.
  Последний коммит реализации — содержащий эту запись (`git log -1`); изменения
  этой задачи закоммичены, в локальный `main` пока не слиты, push не выполнялся.
  Сделаны физическое смещение контакта теней, две зоны CSM с одной зоной на
  мобильном auto-профиле, общий приём теней ближней/дальней водой и просвечиванием
  растений, lifecycle CSM-материалов, MSAA с реальной пробой поддержки и FXAA fallback,
  опциональное контактное AO с отдельной непрозрачной глубиной. Добавлены пределы
  30/40/60/120 FPS и автобюджет с целью 30 FPS, измерением CPU/GPU и гистерезисом;
  он меняет частоту оптики и post resolution, не состав или расположение сцены.
  Найден и исправлен отказ калибровки дальних растений: async-пробы воды оставляли
  PIXEL_PACK_BUFFER bound, sync readPixels получал 1282. Теперь readback сохраняет
  и восстанавливает binding; в живом кадре у кустарника и шести видов деревьев
  PBO действительно был bound, ошибки чтения 0, фон black/white 0/255, parity=scene.
  Проверки: `npm run check:render-core`, lint, build; GPU AO для обычной и log depth
  (плоскость 255, контакт около 234.45, исключённая вода 255). Один встроенный браузер
  Codex, без Chrome: камеры, пауза, 1→2→1 зон, AO/AA, узкий viewport. M4 Max,
  1930×694, FXAA+AO и две зоны: 29.99 FPS при cap30, P95 34.1 мс, 0 кадров >50 мс
  из 240 после прогрева. Это не проверка физических iPhone/Android/NVIDIA/AMD.
  Подробности и контракты: **`docs/render-core-2026-09-07.md`**; исходный аудит
  `docs/lighting-audit-2026-09-07.md` теперь также включён в коммит.
  Sandbox41213 перезапущен с прежним config/cache `output/playwright/water-optics/`
  для нового списка публикуемых ключей; каталог нужен серверу. Редактор открыт
  на обычном `/home/edit`. QA выполнялась через dev-only `?preview=1`, который
  не сохраняет изменения сцены; SHA-256 черновика до/после QA совпал.
  `publishedHomeSceneSettings.js`, «В проект», «На сайт» не трогались.
  Дальше: физические устройства и длительный прогрев/нагрев, затем при измеренной
  необходимости бюджет прогрева ассетов и temporal AA/upscaling. TAA/FSR/GI,
  универсальный SSS и полная замена LOD в эту реализацию не входят. Ограничения
  теней отдельных ассетов и локальных источников перечислены в документе ядра.
- 2026-09-07, интеграция и остаточные артефакты воды: работа выполнялась прямо
  в основном `main` по просьбе Дениса. Последний коммит кода **`6fc2b08`**;
  береговые находки — **`78d278f`**. Все завершённые ветки косы/берега/ассетов уже
  входили в `main`; старые отдельные ветки контента/вёрстки не относятся к задаче.
  Серые прямые клинья при сильном зуме и вращении были выходом за охват старого
  снимка дна. Теперь проверяется пересечение текущего фрустума с высотным диапазоном
  волн: снимок с запасом проекции 20% обновляется до показа непокрытой воды.
  При анимации срочная рефракция занимает существующий слот оптики, отражение ждёт;
  размер текстур и обычная частота не увеличены. Цена запаса — меньшая плотность
  texel при том же разрешении. Привязка текстур/матриц выполняется перед рисованием;
  дальняя вода также читает глубину через камеру снимка и оба кодирования depth.
  Проверки: scoped ESLint, production build, 25 CPU случаев покрытия, по 12 GPU
  случаев действующих ближнего/дальнего шейдеров (перспективная и logarithmic depth).
  Во встроенном браузере проверены резкие повороты, серии wheel zoom, FOV 18–75°,
  смена камер и пауза. Диагностика показанных кадров не нашла непокрытой воды,
  максимум один оптический проход при анимации; после финальной загрузки 151
  шейдерная программа без ошибок. Проверочная обёртка renderer удалена, анимация
  возобновлена, FOV возвращён к авторским 74°. Физические телефоны не проверялись.
  Коряги и каменные круги подключены в редактор и на настоящий берег через общие
  запросы рельефа; численно проверены 344 размещения в шести профилях и 66 наборов
  геометрии/LOD. В живой сцене просмотрены ствол, корневище и круг, контакт с песком.
  **Якорь, сваи и труба — сохранённые неактивные прототипы** в remainsModel/Material;
  их коллекция лаборатории и интерфейс ещё не закончены, в сцену они не включены.
  Sandbox **41213 теперь запущен из основного чекаута**, прежний сервер воркдерева
  остановлен; origin и авторский черновик сохранены. Временный Vite config/cache —
  игнорируемый `output/playwright/water-optics/`; он нужен работающему серверу.
  `publishedHomeSceneSettings.js`, освещение Дениса и черновики других origins
  не переписывались. «В проект», «На сайт» и push не выполнялись.
  Незакоммиченным остаётся отдельный отчёт аудита освещения ниже; текущая вершина
  с этой записью — `git log -1`. Дальше: отзыв Дениса на воду и отдельное продолжение
  света/трёх прототипов; результаты этого ремонта не нужно искать в воркдереве.
- 2026-09-07, аудит освещения на базе **`04af765`**: добавлен
  `docs/lighting-audit-2026-09-07.md`; отчёт оставлен незакоммиченным его агентом.
  Runtime и авторская сцена аудитом не менялись. Проверены исходники, встроенный
  браузер 41213, узкий формат, математика bias; прошли `check:lighting`,
  `check:render-capabilities`, `check:materials`. Цель Дениса — красивые стабильные
  30 FPS. Основные находки: bias меняет физический масштаб с фрустумом, скрытый
  потолок мягкости, неполный приём теней водой/ассетами и просвечиванием листьев,
  отсутствие общего AO и AA-замены в мобильном post-пути. Дальше — реализация
  этапами из отчёта после завершения параллельного ремонта фликера воды.
  На момент аудита другой агент менял воду и интегрировал береговые находки;
  аудит не является приёмкой этих работ или физических iPhone/Android/NVIDIA/AMD.
- 2026-09-07, предыдущий этап косы: `8345321` → интеграция `9e1e4c4` →
  `04af765`. Поджаты подводные плечи до 28 м, добавлены локально крутые склоны,
  свал у конца и скруглены оба примыкания; дно под косой автоматически расширяется
  до 480 м. Земля, вода, прибой, мокрый песок, коллизии и звук используют общий
  рельеф. В лабораторию ландшафт не добавлялся. На origin 41213 сохранена камера
  **04 «Коса»** с обоими форматами; технические **Коса сверху / На косе** следуют
  рельефу. `04af765` исправил декодирование старого снимка дна и полосы глубины,
  но сильные движения всё ещё открывали границу снимка; окончание ремонта выше.
  Подробности геометрии и прежних CPU/GPU проверок — `TERRAIN.md`.
- 2026-09-07, доработка коряг по отзывам Дениса: `4de6d55` — округлый объёмный комель,
  пять толстых изогнутых корней с ответвлениями и обломами, опорный наклон ствола;
  PBR прибрежного камня и сглаженная неодинаковая геометрия каменного круга.
  Imagegen-исходник и промт — `assets-source/textures/shore-stone/`, карты 1024²
  (albedo / normal / AO-roughness-height), повтор 60 см — `public/textures/shore-stone/`.
  Воспроизведение — `scripts/terrain/build-shore-stone-textures.mjs`; это оценка
  микрорельефа по изображению, не скан. Бюджет круга 13 720 / 2 520 треугольников,
  корневища 14 896 / 3 521. Проверены живые макро и низ, варианты 1/17, песок,
  день/ночь и влажность, 390×844; ошибок WebGL нет. Линтер модулей, 72 численных
  сочетания геометрии и стык декодированных WebP (0/255) прошли. Подробности и «Сделано»
  обновлены в `docs/driftwood-lab-plan.md` и `docs/tree-lab-plan.md`. Всё в коллекции 12
  на 41215; перенос на настоящий берег и в редактор по-прежнему ждёт отдельного
  решения Дениса. Работа сохранена локальными коммитами, push не выполнялся.
  Незакоммиченного после записи состояния нет; текущая вершина — `git log -1`.
- 2026-09-07, коряги: локальный коммит `31531a5` — первая коллекция **12 «Коряги»**,
  `http://127.0.0.1:41215/asset-lab.html?collection=driftwood`. Ветка, лежащий ствол,
  корневище, наклонный ствол из песка, пень и каменный круг; параметры формы/материала,
  два уровня неподвижной геометрии, общий набор и студийная россыпь, белый/песчаный пол.
  Модули: `plants/deadwoodModel.js`, `deadwoodMaterial.js`, `terrain/stoneRingModel.js`;
  размеры, бюджеты и подробности — `docs/driftwood-lab-plan.md`. Проверены живые виды,
  макро и низ, переход ночь/день, RU/EN, 390×844 и 768×1024 в Chromium; линтер модулей,
  детерминизм, индексы, 60 сочетаний формы и двух уровней, интервалы россыпи.
  **Денис просит сначала обсудить ассеты**: к настоящему берегу, редактору и публикации
  они не подключены. Следующее — его отзыв по силуэтам, выбеленности и разрушению,
  затем отдельное решение о переносе и размещении через запросы рельефа. Лаборатория
  работает из `main` на прежнем 41215, новые серверы не запускались; черновик редактора
  и опубликованная сцена не менялись. Коммиты этой работы в сеть не отправлены.
  Незакоммиченного после записи состояния нет; текущая вершина — `git log -1`.
- 2026-09-06: библиотека лаборатории — девять коллекций по группам редактора с общей
  навигацией (01–02 существа, 03–05 объекты, 06–09 озеленение), лодка, кувшинки и
  водоросли как компоненты сцены, рыбы/чайки/камень на модулях продукта, значения из
  опубликованной сцены, `npm run check:lab-links`, единые правила `AGENTS.md`,
  `npm run handoff`. Затем огни танкера спрайтами (`src/tanker/lights.js`, три ключа
  редактора, файл сцены переопубликован пайплайном) и повтор карт лодки
  (`RepeatWrapping`: UV лодки тайлятся до 5.9, clamp размазывал доски).
  Затем прибой: рельеф в проходах оптики режется по волне заплеска (тёмная полоса у
  кромки ушла), пена и мокрый песок считаются аналитически из фазы волны. Затем сетка
  берега: зоны плотности рядов внутри полосы, юбки по глубине щели и без них в каркасе;
  рельеф и береговая вода вместе стали примерно вдвое легче по треугольникам; оптика
  рисует рельеф двойником в четверть треугольников; прибой считается только в полосе
  у воды, высота грунта из вершины — кадр на пляже с 28 до ~15 мс.
  Затем полировка редактора по `docs/editor-polish-plan.md` (17 пунктов Дениса):
  реестр объектов `lib/sceneObjects.js` (ведомость и переключатели из него),
  «Камеры» в корне, главная рабочая камера `work-main`, технические кадры,
  полёт на паузе, пробел, шапка «Ouroboros Engine 1.4.0» с кнопками
  «Откатить / В проект / На сайт», ⚙ настройки редактора, честный цвет лодки
  (опубликованный мигрирован пайплайном), узлы «Камни» и «Галька» с коллекцией
  10 в лаборатории. Дальше — лаборатория трав по `docs/grass-lab-plan.md`: ответы
  Дениса и решения записаны там же, промты для его текстур —
  `public/textures/grass/source/prompts.md`. 2026-09-07: карты Дениса собраны в атласы
  (`npm run grass:textures`), пучок `plants/grassModel.js` на общем конвейере,
  коллекция 10 «Травы» (камни — 11); трава в сцене (`plants/grassMeadow.js`,
  `CoastGrass.jsx`, ключи `grass*`, вкладка «Травы», кадры «Степь/Дюна/Камыш»);
  дальнее поле в шейдере рельефа (`plants/grassField.js`, плитки дёрна Дениса как
  слои массива, блок «Дальнее поле», притоптанный дёрн на тропах); `npm run check:grass`
  (детерминизм, ниши, бюджеты, атласы). По отзыву Дениса: единый свет лаборатории
  (`asset-lab/SceneLight.jsx`, переключатель Сцена/Студия в навигации), режим «Все»
  в травах, подложка `carpet` (карты под всем, ключ `grassCarpet`), блок «Цвет» суши
  (`terrainSaturation/Contrast/Brightness/Green/Dry`), тон травы. Дальше: отзыв
  Дениса по картинке, телефон, лёжки и память порыва.
  С 2026-09-06 Денис просит пушить каждый коммит (`git push origin HEAD:main`).
  2026-09-07 (дно): по картинке Дениса — дно шельфа: уклон шельфа за коленом
  (`terrainShelfSlope`, геометрический ключ), луга водорослей / ил / мидиевые
  банки как процедурное поле в координатах берега (`coastBedCover`, CPU-двойник
  `sampleSeabedCover`, три уже загруженных слоя массива с тонировкой, новых
  текстур нет), рябь на песке, цветение поверх лугов с рассеянием и линиями пены
  по ветру; песок под водой везде мокрый (раньше за 26 м рисовался сухим —
  белёсое дно и ступенька); пунктир на краю шельфа (−96 м) убран перекрытием
  дальней воды на полметра и общей зыбью полосы и дальней воды. Ползунки —
  «Ландшафт → Дно → Шельф Азова». Подробности — `TERRAIN.md` «The bed of the shelf».
  Известное: `scripts/check-water-surface-gpu.html` падает на пробе «left edge»
  и на нетронутом HEAD (страница старше четырёх правок шейдера) — не чинилось;
  далеко у горизонта на краю полосы остаются редкие светлые точки, были и раньше.
  Мерцание воды на дальних камерах (видео Дениса): не z-fighting, а алиасинг
  процедурных полос зыби и метровых шумов дна — при неподвижной камере кадры
  не меняются, при сдвиге на 3 px муар по всему морю; теперь косинусы зыби и
  шумы дна гасятся по размеру пикселя (`farWaterCrest`, `bedDetail`, см. `TERRAIN.md`).
  Последний коммит — см. `git log -1`; незакоммиченного нет.
- 2026-09-07, деревья (отдельная линия работы, параллельно траве): план по аналогам Дениса —
  `docs/tree-lab-plan.md` (виды, ниши, сухостой и валежник, порядок работ). Сделан шаг 1:
  `treeModel.js` растит виды параметрами формы (стволы, провис, кучерявость, веточки,
  сухостой) на втором потоке случайности — авторский лох seed 7 не сдвинулся (отпечаток
  в `check-trees.mjs`); таблица видов `plants/treeSpecies.js` (лох, гребенщик, алыча,
  вяз приземистый, ива, сухостой) на существующем атласе листа: тон листа, цвет коры и
  цветение в материалах и проекциях; коллекция 07 с переключателем видов. Сцена сажает
  по-прежнему только лох. Дальше по плану: отзыв Дениса по силуэтам, затем пни и валежник,
  затем ниши от рельефа и ключи видов в редакторе.
  Вечером 7 сентября запущен и остановлен (лимит времени Дениса) воркфлоу-аудит уровней
  детализации и света `wf_e425f2cd-8cf` (скрипт: `~/.claude/projects/-Users-denisdaragan-Documents-Not0-Not1-2026-DDG-buro-Syte/c519ac92-f0b0-4a24-a953-da6f87511122/workflows/scripts/tree-lod-light-audit-wf_e425f2cd-8cf.js`):
  шесть из семи читателей и большинство проверок готовы, выводы и приоритеты правок — в
  `docs/tree-lab-plan.md`, раздел «Аудит 7 сентября». Продолжить дешево: Workflow({scriptPath, resumeFromRunId:
  'wf_e425f2cd-8cf'}) — готовые агенты вернутся из кэша, доработает только читатель «свет карточек» и критик.
  По следу аудита уже исправлено: кора дальних карточек ×0.86 вместо ×1.5 (`PlantPopulation.jsx`).
  Вечером 7 сентября — паритет карточек: `plants/plantParity.js` калибрует карточку по
  геометрии под светом сцены (по кадрам, пять mip, по свету и против), нормаль карточки —
  из ракурса запекания, окружение растений как у рельефа, `treesTranslucency`, `p.lod??0`;
  проверка `npm run check:plants-parity`. Числа и остаток — в плане деревьев, раздел «Сделано 7 сентября, вечер».
  Ночью 7 сентября — кора Дениса (`assets-source/textures/bark`, `npm run bark:textures`, метрическая
  развёртка ветвей без спирали) и купольные кроны ивы, вяза и алычи (`crown:'dome'` в `treeModel.js`);
  раздел «Сделано 7 сентября, ночь» в плане деревьев. В сцене калибровка карточек сама отключается
  (`light:'invalid'`) — открытая нитка.
  8 сентября — деревья рассажены по нишам берега (`coastPlanting.js`: бровка, лесополоса, балки, тыл пляжа;
  виды по долям, сухостой), популяция на вид в `CoastTrees.jsx`, ключи и кадры во вкладке «Деревья»;
  раздел «Сделано 8 сентября» в плане деревьев. Дальше: пни и валежник, перетекание уровней, телефон.
- 2026-09-07, грунты обрыва (ветка `claude/azov-soils` из воркдерева `.claude/worktrees/azov-soils`,
  влита в `main` fast-forward'ом по слову Дениса поверх незакоммиченной работы агента деревьев). По референсам из папки
  «Грунты» обрыв перекрашен из рыжего в дневную жёлто-серую охру Таганрогского берега и
  разложен на ярусы по высоте стенки: гумусовая шапка с влажной полосой под дёрном, лёсс
  с тонкими пластами и бороздками стока (и в нормали), осыпь у подножия; свежий скол —
  пятнами на крутых гранях, «Выветривание» гасит его. Новое общее поле подножия
  `coastSoil` / `sampleCoastSoil` (языки осыпи на пляж лопастями, просачивание у
  подножия, у оползней и в устьях балок) читают материал, `surfaceAt().talus/.seep` и
  сухость растений. Конус оползня в геометрии перенесён к подножию и выходит на пляж
  (`terrainTalus` — геометрический ключ). Два ползунка в «Суша → Поверхность»: «Осыпь и
  языки грунта», «Пласты и потёки». По отзыву Дениса — блок «Бровка»: полоса дёрна
  вдоль кромки плато с фактурой плиток Дениса в полный вес и своим цветом
  (`terrainRimWidth`, `terrainRimColor`). Затем «Влажные островки»: зелень фактурой дёрна
  там, где копится влага и тень — просачивание у подножия, спокойная кромка за заплеском,
  ореолы кустов, деревьев и валунов (синий канал карты покрова, валуны добавлены как
  источники) — `terrainOasis`, `terrainOasisColor`; только материал, травяные пучки это
  поле пока не читают. Затем «Выброшенная тина» (`terrainWrack`): клочья водорослей по линии
  заплеска с параллаксом, старая бурая линия выше, кормятся лугами шельфа и штормом. Затем «Рыхлый песок» (`terrainLooseSand`): вмятины и
  ямки утоптанного сухого пляжа в нормали и параллаксе, розово-серые наносы, тёмные
  минеральные полосы у заплеска, штормовая линия ракушки, рябь на мокром песке.
  Всё влито в `main` по слову Дениса, незакоммиченного нет; воркдерево и ветка оставлены
  для продолжения. Дальше (по его отзыву в новом чате): ямки глубже и с чётким краем, если
  захочет; пучки травы, читающие поле сырости; корни из стенки и комья у подножия. Проверено: lint, build, `check:terrain`,
  `check:plants`, `scripts/check-terrain-gpu.html` (560 проб). Известное:
  `scripts/check-terrain-ui.mjs` ищет кнопки «Обвал/Спуск/Покров» в секции «Суша», а они
  переехали в «Камеры» — скрипт устарел, не чинился. Дальше: отзыв Дениса по тону и силе
  пластов; корни из стенки под дерновым козырьком и комья у подножия — геометрией
  (инстансы вдоль бровки) после одобрения шейдера; корни у стволов и валежник — у агента
  деревьев. Подробности — `TERRAIN.md` «The ground of the bluff».
- Открытые нитки: список «Что дальше» в `ASSET_LAB.md`; проверки по просьбе Дениса.
- Воркдеревья `.claude/worktrees/*`: `codex-azov-coast` слито в `main` 2026-09-05,
  остальные три — старые detached HEAD, можно удалять (`git worktree remove`).

## Что это

Главная страница `/` — 3D-сцена: пруд, лодка, скульптура-«голова», кувшинки, водоросли.
Редактор сцены — `/home/edit`. Стек: React + Vite + Three.js / react-three-fiber.
Запуск — через записи `.claude/launch.json`; карта портов и правила для агентов — в `AGENTS.md` (порты 41211–41215).
Хостинг GitHub Pages, деплой автоматом по push в `main`. Кнопка «На сайт» в редакторе
публикует сцену, коммитит один этот файл и пушит `HEAD:main` (`vite.config.js`,
`deploy: true` в `/__home-scene/publish`); «В проект» только пишет файл — этим путём
ходит смоук, и до сети он не дотягивается.

## Поток данных — три состояния, не путать

```
редактор → черновик (localStorage)  →  Publish  →  publishedHomeSceneSettings.js  →  главная
           ddg_home_scene_settings_v1            (файл в репозитории)
```

- **Черновик** живёт в браузере, пишется на каждое движение ползунка. Только редактор.
- **Publish** делает POST на дев-эндпоинт `/__home-scene/publish` (плагин в `vite.config.js`),
  который переписывает `publishedHomeSceneSettings.js`. **Работает только на localhost.**
- **Главная** читает опубликованный файл, черновик её не касается.

`publishedHomeSceneKeys.js` — список того, что вообще публикуется (сейчас 332 ключа).
Настройка, которой там нет, останется локальной: так живут `freeCamera`, `showPointerDebug`,
`showPerformanceHud`, `debugWireframe` — инструменты вьюпорта, а не свойства сцены.
Смоук-тест сверяет: каждый контрол редактора либо в этом списке, либо в его
`DEV_LOCAL_EDITOR_KEYS`. Забыть ключ нельзя — тест упадёт.

## Две среды: редактор Дениса и песочница агента

| Запись в `.claude/launch.json` | Порт | Чья |
|---|---|---|
| `site` | 41211 | Дениса, главная. |
| `editor` | 41212 | Главный dev Дениса: `http://localhost:41212/home/edit`. |
| `sandbox` | 41213 | Агента. Здесь можно ломать. |
| `dist` | 41214 | Превью собранного `dist/`. |
| `lab` | 41215 | Общая. Лаборатория ассетов, черновика сцены нет. |

Настройки редактора хранятся отдельно для каждого origin (протокол, имя хоста,
порт). `localhost` и `127.0.0.1` тоже имеют **разные localStorage**. Код при этом
может быть общим: главный dev и песочница запускаются из одного чекаута.

Обмен идёт через опубликованный файл, а не через localStorage:

```
песочница → Publish → publishedHomeSceneSettings.js → редактор: «Откатить»
```

Кнопка «Откатить» двухшаговая — она затирает текущий черновик,
а в черновике может лежать несохранённый день работы.

**Правило для агента:** работать в песочнице (41213) или из свежего профиля
Playwright. В 41211/41212 заходить только
смотреть — не трогая ползунки. Один раз уже было: скролл над панелью незаметно
сдвинул количество кувшинок в его сцене.

`useHomeSceneSettings.js` — дефолты и **клампы**. Значение вне диапазона молча прижимается,
так что если ползунок «не действует до конца», сначала смотри кламп там.

## Сцена

`WaterScene.jsx` — сборка. Внутри `SceneCanvas.jsx` (WebGL-контекст, DPR, фолбэк без WebGL).
Модули в `src/components/effects/water/`:

| Модуль | За что отвечает |
|---|---|
| `useWaterRuntime.js` | GPU-симуляция воды: ping-pong текстуры высоты + нормаль-мапа |
| `WaterSurface.jsx` | Поверхность: displacement по height-field, Френель, отражение/преломление, штамп stencil |
| `Seabed.jsx` | Дно: рельеф, каустика, текстура |
| `WaterReflections.jsx` | Планарные отражение и преломление: пересъёмка сцены в текстуры |
| `WaterInteractionPlane.jsx` | Курсор → рябь. Рейкаст с уточнением по высоте волны |
| `WaterCameraRig.jsx` | Камера по авторской позе корзины; OrbitControls в редакторе |
| `WaterLights.jsx` | Свет и HDRI (пресет `night` зашит локально в `public/hdri/`) |
| `FloatingBoat.jsx` | Плавучесть по пяти поплавкам; stencil-вырез сухого кокпита |
| `StaticSculpture.jsx` | Скульптура на дне |
| `SurfaceVegetation.jsx` | Кувшинки |
| `UnderwaterAlgae.jsx` | Подводные водоросли |
| `pbrMaterial.js` | Конвенция PBR: цветовые пространства карт, шкала отражений среды |
| `useDragOnPlane.js` | Общий драг объекта по плоскости в редакторе |

Шейдеры — в `src/components/effects/shaders/`. Освещение считается один раз в
`homeSceneLighting.js` и раздаётся всем объектам как `lighting`; профиль качества
устройства — в `qualityProfile.js`.

**Вода — height-field симуляция, не Gerstner.** Деталь ряби живёт в нормаль-мапе,
не в полигонах. Лодка плавает по пяти поплавкам, читающим высоту из текстуры симуляции;
физика не зависит от меша модели.

## Лаборатория ассетов

`asset-lab.html` (запись `lab`, порт 41215, `npm run lab`) — белая сцена, где каждый
ассет смотрят до интеграции: существа (01 рыбы, 02 чайки), объекты (03 чёрный камень,
04 танкер, 05 лодка), озеленение (06 куст и 07 дерево лоха, 08 кувшинки, 09 водоросли).
Список — `src/asset-lab/assetCatalog.js`, навигация по группам на каждой странице.
Коллекции импортируют модули продукта и стартуют с опубликованной сцены, копий нет
(таблица «что откуда», `npm run check:lab-links` и задачи — в `ASSET_LAB.md`).
Сборка сайта её не включает.

## Берег: суша, растения, танкер

Ветка `codex/azov-coast` (Codex, сентябрь 2026) добавила вокруг пруда
процедурное азовское побережье; влита в `main` 5 сентября 2026 вместе с камерами
и облачным небом. Подробности — в `TERRAIN.md`, `src/plants/README.md`,
`src/tanker/README.md`; повторяемый подход — в `.agents/skills/`.

| Модуль | За что отвечает |
|---|---|
| `src/terrain/` | Аналитический берег: `terrainModel.js` — высота, нормаль, поверхность и рейкаст в береговых координатах (u поперёк, s вдоль); `AzovTerrain.jsx` — полосы рельефа с геоморфингом LOD, камни, ракушечник и прибрежные полосы воды (`WaterSurfaceV2` в `shoreMode`) |
| `src/plants/` | Лох узколистный: `oleasterModel.js` — куст и общие примитивы (`createPlantBuilder`), `treeModel.js` — взрослое дерево на обрыве, `PlantPopulation.jsx` — инстансы с тремя LOD и атласом проекций, `plantHabitat.js` — расстановка по `surfaceAt`, `coastPlanting.js` — полосы посадки кустов и рощи |
| `src/tanker/` | Речной танкер на горизонте: процедурная модель, погода, кильватер, звук |

Погода одна: `coastWeather(definition)` в `terrain/settings.js` отдаёт прибой,
пену, ветер и зыбь (`swell`, `swellBearing`); её читают растения, дальняя вода,
рябь пруда, танкер и шейдеры через `uCoastSwell`. Новая связь с ветром или штормом
добавляется туда, а не отдельным ползунком.

Вода и берег связаны в трёх местах: шейдер пруда получает `uShoreMode` и
аналитическую береговую волну (`terrainShader.js`), пробы поверхности для лодки и
птиц взвешиваются береговой линией (`applyCoastToProbes` в `useWaterRuntime.js`),
а прибрежные полосы рисуются тем же материалом, что и пруд.

Ключи `terrain*`, `shrubs*`, `trees*`, `tanker*` берутся из `DEFAULT_*_SETTINGS`
своих модулей и разворачиваются в `publishedHomeSceneKeys.js` — новая настройка
берега добавляется в дефолты модуля и его `*_RANGES`, остальное подхватывается.

## Освещение: Sky + Sun + Moon

Одна таблица рассеяния питает всё, что обязано быть согласованным: видимое небо,
небо в отражении воды, заполняющий свет на материалах и цвет ключевого света.
Они не могут разойтись — это одни и те же числа.

| Файл | Что делает |
|---|---|
| `effects/sky/skyModel.js` | Физика: дуга солнца, воздушная масса, рассеяние, таблица неба. Чистый JS, без three — запускается в node |
| `effects/sky/skyModel.check.js` | `npm run check:sky`. Проверяет модель числами, а не скриншотом |
| `effects/shaders/skyShader.js` | Общий GLSL: `skyRadiance()` и `celestialBody()`. Импортируется небом И водой |
| `effects/water/skyEnvironment.js` | Мост в three: DataTexture + PMREM в `scene.environment` |
| `effects/water/SkyDome.jsx` | Видимое небо: три вершины на дальней плоскости |
| `effects/homeSceneLighting.js` | Контракт: раздел `sky` плюс ключевой свет, разложенный на цвет и уровень |

**Ключевые правила системы:**

- **Цвет солнца никто не выбирает.** Он получается из ослабления в воздухе: низкое
  солнце краснеет потому, что его свет прошёл больше атмосферы. `sunTint` — это
  цвет солнца *над* атмосферой, почти белый; всё тёплое возникает само.
- **Время суток ведёт высоту, азимут остаётся авторским.** В полдень дуга
  возвращает ровно заданные направление и высоту, поэтому старые сцены открываются
  там же, где были (`sunBearing` ← `moonAzimuth`, `sunNoonElevation` ← `moonElevation`).
- **Облачность — один физический факт, а не два ползунка.** Она забирает энергию у
  пучка и отдаёт её куполу, поэтому тень слабеет и смягчается сама, а кадр не темнеет.
- **`directShare`** — доля прямого света; ровно столько тень имеет право убрать.
  Считается в той же петле, что и таблица.
- **Тень доходит до воды** одной аппаратной выборкой (`sampler2DShadow`), без второй
  карты теней. Умножает блик, искры, солнечную дорожку и рассеяние в толще —
  поэтому мутная вода показывает тень **сильнее**, а не стирает её.
- **HDRI вспомогательный.** `envMode`: `sky` (по умолчанию, ничего не грузится),
  `sky+hdri`, `hdri`.

**Светильники** — группа «Свет» в редакторе: два источника, у каждого тело и цель,
оба двигаются манипулятором как отдельные объекты (как Corona Light). Угол конуса
180° превращает прожектор в точечный источник. «Участвует в отражениях» — это слой,
а не флаг видимости: снятая галочка убирает из зеркала и сам источник, и его свет.
Тени они не отбрасывают намеренно — вторая карта теней самое дорогое, что могла бы
попросить эта функция.

План целиком — `docs/lighting-plan.md`.

Аудит воды, курсора, волн, пены, погоды и ветра с замерами и порядком работ —
`docs/water-audit-2026-09-05.md`.

**Не проверено:** теневая выборка в собственном шейдере на живом iPhone. На
десктопе компилируется; это доказательство только для десктопа. Проверка:
`npm run dev:lan`, на телефоне открыть `http://<адрес>:41211/?hud=1` — в HUD строка
`SHADOW`. Если сцена вместо картинки показывает экран «WebGL не поддерживается» —
шейдер не скомпилировался, и запасной путь описан в плане (Фаза 3, `BasicShadowMap`).

## Редактор

Панель — дерево сцены в три уровня, описанное декларативно в
`components/editor/editorTree.js`: **группа → узел → аспекты**.
Группы: Ландшафт, Озеленение, Объекты, Атмосфера, Рендер, Интерфейс.
Выбор хранится одной строкой `"группа/узел"`. Узел с `devOnly: true` виден только в dev.

Контролы живут в `components/editor/sections/*.jsx` — по файлу на группу. Внутри длинной
секции блоки разделяются `<SectionHeading subtle />`: та же черта, что у заголовка аспекта,
но тише, чтобы вложенность читалась.

Добавить настройку — пять мест, все обязательны:
1. дефолт в `getBaseHomeSceneSettings`
2. кламп в `normalizeHomeSceneSettings`
3. ключ в `publishedHomeSceneKeys.js` (или в `DEV_LOCAL_EDITOR_KEYS` смоука, если она локальная)
4. контрол в нужной секции
5. подписи RU + EN в `i18n/translations.js`

**Рендер → Видимость** — общие выключатели: вода, дно, кувшинки, водоросли, лодка,
скульптура, отражения. Тени, постобработка и лучи переключаются в своих разделах,
здесь не дублируются.

**Рендер → Дебаг** (только dev): свободная камера, каркас (wireframe), отладка курсора,
FPS/память, режим отладки.

## Композиция: две корзины

`lib/layout.js`. Ровно две — `desktop` и `portrait`, обе с кадром 16:9; различается только
авторская поза камеры. Выбор по аспекту окна: `< 1` → portrait. Некастомизированная корзина
наследует desktop. Каждая хранит `cameraPosition/Target`, `cameraFov`, `frameInset`,
`boatPosition`, `sculpturePosition`.

## Ассеты

`public/` содержит **только то, что запрашивает рантайм**. Всё, из чего это собрано,
лежит в `assets-source/` и в деплой не уезжает:

- модели — `assets-source/models/*.obj` → `npm run convert:models` → `public/models/*.glb`
- кувшинки — `assets-source/lily/lily-atlas-source.png` → `npm run generate:lily`
  (`scripts/generate-lily-pbr.mjs`) → промежуточные PNG там же, а в `public/textures/lily/`
  кладутся три webp-атласа, которые грузит сцена

Модели раздаются в GLB, не в OBJ: текстовый OBJ весил 10.4 МБ на две модели и держал
показ сцены, пока браузер его разбирал. Конвертер режет меш по группам материалов
(корпус — один меш с чередованием дерево/металл), сваривает вершины каждой части
отдельно и сохраняет **имена материалов** — лодка выбирает металл по имени
`OBJ_wire_metall`, так что переименование молча покрасит уключины деревом.

## Грабли

- **Небо считается на процессоре один раз при загрузке.** Правка, которая заставит
  его пересчитываться каждый кадр, съест телефон. Ключ пересборки — `skyKey` в
  `skyEnvironment.js`, и он намеренно завязан на входы неба, а не на объект настроек.
- **Нормаль-мапа воды — не геометрический уклон.** Она усилена `normalStrength` ради
  освещения ряби. Строить по ней наклон объекта нельзя: кувшинки от этого встают дыбом.
  Наклон и высоту бери из height-field.
- **Вода непрозрачна и пишет глубину.** Всё, что оказалось ниже её поверхности, вырезается
  depth-тестом. Чтобы объект просвечивал сквозь воду, он должен попасть в проход преломления
  (`WaterReflections`) — и только подводной своей частью, иначе получишь его в кадре дважды
  и тёмную кайму. Глобальная плоскость отсечения так резать по волне не умеет и на кастомные
  шейдеры вообще не действует: рез делает сам шейдер объекта.
- **HMR ломает проходы отражений.** Правка модулей сцены может оставить чёрную воду или
  пустое преломление. Это артефакт разработки — лечится перезагрузкой страницы, к коду
  отношения не имеет. Прежде чем чинить «поломку», перезагрузи.
- **Скриншот из песочницы — плохой судья.** Проверять визуал лучше в браузере пользователя.
  Проверка кода = чистая компиляция + пустая консоль.
- **Постобработка включена** в опубликованной сцене. Без неё туман и лучи не
  работали вовсе — они живут только в этом проходе. Включить её удалось лишь после
  того, как в пост вернули тонмаппинг: three выкомпиливает его для рендера в
  текстуру, а весь кадр рисуется именно туда, поэтому тонмаппинга не было ни на
  одном пути. Рендерер теперь ACES Filmic.
- **Лучи видны, только когда солнце в кадре.** Обе авторские камеры смотрят вниз,
  поэтому при высоком солнце лучей не будет — это не поломка, а геометрия.
- **У береговой волны два тела.** `coastWave` в `terrain/terrainShader.js` (GPU: вода,
  пена, мокрый песок) и `sampleCoastWave` в `terrain/terrainModel.js` (CPU: лодка, пробы,
  посадка) обязаны быть одной формулой. Правишь одну — правь вторую, иначе лодка
  поплывёт не по той воде, что нарисована. Шум в волне поэтому не хэш, а суммы синусов.
  Та же пара у огибающей и мокрого песка: `coastWaveGain`/`coastWetnessAtHeight` и
  `sampleCoastWaveGain`/`sampleCoastWetness`.
- **Прибой без памяти.** Пена на песке не хранит историю: `coastSwashAt` считает
  аналитически, сколько секунд назад вода ушла с точки (арксинус огибающей плюс шаг
  Ньютона по скошенной волне) и где линия максимального заплеска. Поэтому телефон и
  десктоп рисуют одну и ту же пену. Кромка воды рваная одной функцией `coastEdgeRag`,
  на неё завязаны и отсечка воды, и кружево, и вырез рельефа в оптике.
- **«Рёбра под землёй» — юбки.** У каждой полосы рельефа (64 м вдоль берега) по краям
  висят вертикальные шторки: они прячут щели между соседними полосами разной
  детализации. Теперь они висят на глубину щели (0,45 м на ровном, до 5,6 м на обрыве),
  каркасный режим их не рисует (`drawRange` по `userData.topTriangles`), и внутри полосы
  три зоны сетки (шельф ¼ рядов, берег с обрывом полные ряды, плато ¼) со своими
  короткими юбками на стыках. Морф LOD считается по зонам, поэтому проверка
  `check:terrain` требует, чтобы счётчики колонок в полосах делились на два на каждый
  уровень, а срез плато (`plateauCut`) был один на весь берег.
- **Берег дорог пикселями, не треугольниками.** На камере у воды выключение рельефа
  снимало 19 мс из 28 при том, что рельеф это ~170k треугольников. Кадр держал
  фрагментный шейдер песка на весь экран: `coastHeight` (сорок трансцендентных) и
  прибой считались на каждый пиксель. Теперь высота грунта берётся из вершины, а
  прибой и мокрый песок считаются только в полосе q −26..12 м (в береговой воде пена
  тоже). Проходы оптики рисуют рельеф «двойником» на тех же буферах с четвертью
  треугольников. Любая новая функция в шейдере берега — сначала спросить, нужна ли
  она вне полосы у воды.
- **Рельеф режет себя в проходах оптики сам.** Общая плоскость отсечения на уровне
  спокойной воды не видит заплеск: песок под набегающей плёнкой не попадал в захват
  преломления, и вода у кромки показывала тёмное «тело» вместо песка. Теперь
  `uTerrainOptics` (1 преломление, 2 отражение) в `terrainMaterial.js` режет по
  `coastWave`, а чанк `clipping_planes_fragment` у рельефа заменён — глобальные
  плоскости на него больше не действуют.
- **Публикация только на localhost.** На задеплоенном сайте кнопка Publish неактивна.
- **Опубликованный файл — художественная работа Дениса.** Не откатывать и не переписывать
  руками.

## Проверки

| Команда | Что делает |
|---|---|
| `npm run lint` | ESLint |
| `npm run build` | Сборка |
| `npm run check:bundle` | Размеры и состав чанков по манифесту |
| `npm run check:sky` | Модель неба: дуга солнца, покраснение, энергия облачности |
| `npm run convert:models` | Пересобрать GLB из исходных OBJ |
| `npm run dev:lan` | Dev-сервер, видимый с телефона в той же сети |
| `npm run check:plants` | Куст и дерево: детерминизм, крепления, LOD-бюджеты, посадка на берегу |
| `npm run check:plants-parity` | Паритет трёх уровней растений в живой лаборатории (Playwright, нужен сервер `lab` или `PLANT_LAB_URL`): яркость карточки против геометрии на трёх дистанциях |
| `npm run check:terrain` | Рельеф: контракты высоты/нормали, контакт растений с грунтом |
| `npm run check:coast-contracts` | Стык воды и берега, таймлайн сцены, дальние чайки, LOD оптики |
| `npm run smoke` | Playwright: маршруты, WebGL-фолбэк, аудио, миграция черновика, покрытие publish-ключей, публикация end-to-end, память в долгой сессии, мобилка |

CI: `checks.yml` гоняет всё это на push и PR. `deploy-pages.yml` перед публикацией
прогоняет только быстрые детерминированные проверки (lint, build, check:bundle) —
браузерный смоук намеренно не может заблокировать деплой сайта.

## Замер на реальном устройстве

Профиль качества для слабых устройств написан, но на живом телефоне не проверялся —
эмуляция размера окна не эмулирует мощность GPU (в песочнице всё равно останется
десктопная видеокарта).

1. `npm run dev:lan` — Vite слушает на всех интерфейсах и печатает адрес в сети.
2. На телефоне в той же сети открыть `http://<адрес>:41211/?hud=1`.
3. `?hud=1` включает HUD прямо на главной (в dev-сборке): FPS, p95 кадра, память,
   треугольники, эффективное разрешение симуляции. Переключатель в панели редактора
   для этого не нужен — панелью на телефоне не поуправляешь.

Для сравнения, десктоп на M4 Max: 120 fps, 8.3 мс средний кадр, p99 10.8 мс,
42 вызова отрисовки, 852k треугольников — то есть упирается в частоту экрана.

**Смоуку нужна машина целиком** — перед запуском останови дев-серверы, иначе получишь
ложные падения из-за конкуренции за GPU.
