# Карта кода — водная сцена denisdaragan.com

> Прочитай этот файл первым. Общение с пользователем — по-русски.
> Денис — художник, не программист: технические решения принимай сам, спрашивай только про художественное.
> Деплой — это push в `main` на GitHub; с 2026-09-02 его делает кнопка «На сайт» в редакторе
> или агент по просьбе Дениса (`git push origin HEAD:main`, только fast-forward, после локальных ворот).

## Состояние (обновлять в конце каждой сессии — AGENTS.md §3)

- 2026-09-07, публикация ядра и следующий этап качества по прямой просьбе Дениса:
  **`4834ab6` уже слит в `main`, отправлен в `origin/main` и опубликован**.
  Pages run `34121589531` успешен, сайт denisdaragan.com просмотрен во встроенном
  браузере. Отдельный Checks `34121589512` упал на ожидании runtime-метрик через
  20 секунд; причина пока не подтверждена. Публикация и smoke — разные результаты.
  Продолжение — **`codex/render-upscale-warmup`**, база `4834ab6`, коммит реализации
  содержит эту запись. Добавлены FSR 1 EASU/RCAS с RU/EN контролами, ограничением
  уменьшения маленького кадра, правильным однократным ACES и плёнкой после апскейла;
  исправлен выбор стороны FXAA. Общая очередь калибровки растений допускает не более
  двух шагов за кадр, прекращая запуск новых после 3 мс; отдельный шаг непрерываемый.
  Все 12 групп завершили прогрев, первая компиляция/чтение всё ещё может дать паузу.
  В dev можно измерять GPU при выключенном автобюджете. Smoke получил диагностику
  таймаута без ослабления приёмки. Авторский `publishedHomeSceneSettings.js` не менялся.
  Проверки: lint, check:render-core, build, check:bundle; 15 GPU случаев EASU/RCAS
  и 5 случаев production цветового пути; день/ночь, вода/растения, AA/AO, переключения,
  пауза и маленький кадр во встроенном браузере. Внешний Chrome не запускался.
  Подробности: **`docs/render-upscale-2026-09-07.md`**. Сравнимый прирост FPS и
  физические телефоны/NVIDIA/AMD не подтверждены. FSR по умолчанию выключен.
  Дальше: синхронный старт ассетов, повторная калибровка LOD при смене света,
  измерения на физических устройствах, затем оценка temporal AA. Новые изменения
  закоммичены этой записью; финальную интеграцию/публикацию смотреть в следующей записи.
- 2026-09-07, ядро света и качества, **после разрешения Дениса на реализацию**:
  работа в основном чекауте, ветка **`codex/lighting-core`**, база **`3b6eabf`**.
  Последний коммит реализации — содержащий эту запись (`git log -1`); изменения
  этой задачи закоммичены, в локальный `main` пока не слиты, push не выполнялся.
  Сделаны физическое смещение контакта теней, две зоны CSM с одной зоной на
  мобильном auto-профиле, общий приём теней ближней/дальней водой и просвечиванием
  растений, lifecycle CSM-материалов, MSAA с реальной пробой поддержки и FXAA fallback,
  опциональное контактное AO с отдельной непрозрачной глубиной. Добавлены пределы
  30/40/60/120 FPS и автобюджет с целью 30 FPS, измерением CPU/GPU и гистерезисом;
  он меняет частоту оптики и post resolution, не состав или расположение сцены.
  Найден и исправлен отказ калибровки дальних растений: async-пробы воды оставляли
  PIXEL_PACK_BUFFER bound, sync readPixels получал 1282. Теперь readback сохраняет
  и восстанавливает binding; в живом кадре у кустарника и шести видов деревьев
  PBO действительно был bound, ошибки чтения 0, фон black/white 0/255, parity=scene.
  Проверки: `npm run check:render-core`, lint, build; GPU AO для обычной и log depth
  (плоскость 255, контакт около 234.45, исключённая вода 255). Один встроенный браузер
  Codex, без Chrome: камеры, пауза, 1→2→1 зон, AO/AA, узкий viewport. M4 Max,
  1930×694, FXAA+AO и две зоны: 29.99 FPS при cap30, P95 34.1 мс, 0 кадров >50 мс
  из 240 после прогрева. Это не проверка физических iPhone/Android/NVIDIA/AMD.
  Подробности и контракты: **`docs/render-core-2026-09-07.md`**; исходный аудит
  `docs/lighting-audit-2026-09-07.md` теперь также включён в коммит.
  Sandbox41213 перезапущен с прежним config/cache `output/playwright/water-optics/`
  для нового списка публикуемых ключей; каталог нужен серверу. Редактор открыт
  на обычном `/home/edit`. QA выполнялась через dev-only `?preview=1`, который
  не сохраняет изменения сцены; SHA-256 черновика до/после QA совпал.
  `publishedHomeSceneSettings.js`, «В проект», «На сайт» не трогались.
  Дальше: физические устройства и длительный прогрев/нагрев, затем при измеренной
  необходимости бюджет прогрева ассетов и temporal AA/upscaling. TAA/FSR/GI,
  универсальный SSS и полная замена LOD в эту реализацию не входят. Ограничения
  теней отдельных ассетов и локальных источников перечислены в документе ядра.
- 2026-09-07, интеграция и остаточные артефакты воды: работа выполнялась прямо
  в основном `main` по просьбе Дениса. Последний коммит кода **`6fc2b08`**;
  береговые находки — **`78d278f`**. Все завершённые ветки косы/берега/ассетов уже
  входили в `main`; старые отдельные ветки контента/вёрстки не относятся к задаче.
  Серые прямые клинья при сильном зуме и вращении были выходом за охват старого
  снимка дна. Теперь проверяется пересечение текущего фрустума с высотным диапазоном
  волн: снимок с запасом проекции 20% обновляется до показа непокрытой воды.
  При анимации срочная рефракция занимает существующий слот оптики, отражение ждёт;
  размер текстур и обычная частота не увеличены. Цена запаса — меньшая плотность
  texel при том же разрешении. Привязка текстур/матриц выполняется перед рисованием;
  дальняя вода также читает глубину через камеру снимка и оба кодирования depth.
  Проверки: scoped ESLint, production build, 25 CPU случаев покрытия, по 12 GPU
  случаев действующих ближнего/дальнего шейдеров (перспективная и logarithmic depth).
  Во встроенном браузере проверены резкие повороты, серии wheel zoom, FOV 18–75°,
  смена камер и пауза. Диагностика показанных кадров не нашла непокрытой воды,
  максимум один оптический проход при анимации; после финальной загрузки 151
  шейдерная программа без ошибок. Проверочная обёртка renderer удалена, анимация
  возобновлена, FOV возвращён к авторским 74°. Физические телефоны не проверялись.
  Коряги и каменные круги подключены в редактор и на настоящий берег через общие
  запросы рельефа; численно проверены 344 размещения в шести профилях и 66 наборов
  геометрии/LOD. В живой сцене просмотрены ствол, корневище и круг, контакт с песком.
  **Якорь, сваи и труба — сохранённые неактивные прототипы** в remainsModel/Material;
  их коллекция лаборатории и интерфейс ещё не закончены, в сцену они не включены.
  Sandbox **41213 теперь запущен из основного чекаута**, прежний сервер воркдерева
  остановлен; origin и авторский черновик сохранены. Временный Vite config/cache —
  игнорируемый `output/playwright/water-optics/`; он нужен работающему серверу.
  `publishedHomeSceneSettings.js`, освещение Дениса и черновики других origins
  не переписывались. «В проект», «На сайт» и push не выполнялись.
  Незакоммиченным остаётся отдельный отчёт аудита освещения ниже; текущая вершина
  с этой записью — `git log -1`. Дальше: отзыв Дениса на воду и отдельное продолжение
  света/трёх прототипов; результаты этого ремонта не нужно искать в воркдереве.
- 2026-09-07, аудит освещения на базе **`04af765`**: добавлен
  `docs/lighting-audit-2026-09-07.md`; отчёт оставлен незакоммиченным его агентом.
  Runtime и авторская сцена аудитом не менялись. Проверены исходники, встроенный
  браузер 41213, узкий формат, математика bias; прошли `check:lighting`,
  `check:render-capabilities`, `check:materials`. Цель Дениса — красивые стабильные
  30 FPS. Основные находки: bias меняет физический масштаб с фрустумом, скрытый
  потолок мягкости, неполный приём теней водой/ассетами и просвечиванием листьев,
  отсутствие общего AO и AA-замены в мобильном post-пути. Дальше — реализация
  этапами из отчёта после завершения параллельного ремонта фликера воды.
  На момент аудита другой агент менял воду и интегрировал береговые находки;
  аудит не является приёмкой этих работ или физических iPhone/Android/NVIDIA/AMD.
- 2026-09-07, предыдущий этап косы: `8345321` → интеграция `9e1e4c4` →
  `04af765`. Поджаты подводные плечи до 28 м, добавлены локально крутые склоны,
  свал у конца и скруглены оба примыкания; дно под косой автоматически расширяется
  до 480 м. Земля, вода, прибой, мокрый песок, коллизии и звук используют общий
  рельеф. В лабораторию ландшафт не добавлялся. На origin 41213 сохранена камера
  **04 «Коса»** с обоими форматами; технические **Коса сверху / На косе** следуют
  рельефу. `04af765` исправил декодирование старого снимка дна и полосы глубины,
  но сильные движения всё ещё открывали границу снимка; окончание ремонта выше.
  Подробности геометрии и прежних CPU/GPU проверок — `TERRAIN.md`.
- 2026-09-07, доработка коряг по отзывам Дениса: `4de6d55` — округлый объёмный комель,
  пять толстых изогнутых корней с ответвлениями и обломами, опорный наклон ствола;
  PBR прибрежного камня и сглаженная неодинаковая геометрия каменного круга.
  Imagegen-исходник и промт — `assets-source/textures/shore-stone/`, карты 1024²
  (albedo / normal / AO-roughness-height), повтор 60 см — `public/textures/shore-stone/`.
  Воспроизведение — `scripts/terrain/build-shore-stone-textures.mjs`; это оценка
  микрорельефа по изображению, не скан. Бюджет круга 13 720 / 2 520 треугольников,
  корневища 14 896 / 3 521. Проверены живые макро и низ, варианты 1/17, песок,
  день/ночь и влажность, 390×844; ошибок WebGL нет. Линтер модулей, 72 численных
  сочетания геометрии и стык декодированных WebP (0/255) прошли. Подробности и «Сделано»
  обновлены в `docs/driftwood-lab-plan.md` и `docs/tree-lab-plan.md`. Всё в коллекции 12
  на 41215; перенос на настоящий берег и в редактор по-прежнему ждёт отдельного
  решения Дениса. Работа сохранена локальными коммитами, push не выполнялся.
  Незакоммиченного после записи состояния нет; текущая вершина — `git log -1`.
- 2026-09-07, коряги: локальный коммит `31531a5` — первая коллекция **12 «Коряги»**,
  `http://127.0.0.1:41215/asset-lab.html?collection=driftwood`. Ветка, лежащий ствол,
  корневище, наклонный ствол из песка, пень и каменный круг; параметры формы/материала,
  два уровня неподвижной геометрии, общий набор и студийная россыпь, белый/песчаный пол.
  Модули: `plants/deadwoodModel.js`, `deadwoodMaterial.js`, `terrain/stoneRingModel.js`;
  размеры, бюджеты и подробности — `docs/driftwood-lab-plan.md`. Проверены живые виды,
  макро и низ, переход ночь/день, RU/EN, 390×844 и 768×1024 в Chromium; линтер модулей,
  детерминизм, индексы, 60 сочетаний формы и двух уровней, интервалы россыпи.
  **Денис просит сначала обсудить ассеты**: к настоящему берегу, редактору и публикации
  они не подключены. Следующее — его отзыв по силуэтам, выбеленности и разрушению,
  затем отдельное решение о переносе и размещении через запросы рельефа. Лаборатория
  работает из `main` на прежнем 41215, новые серверы не запускались; черновик редактора
  и опубликованная сцена не менялись. Коммиты этой работы в сеть не отправлены.
  Незакоммиченного после записи состояния нет; текущая вершина — `git log -1`.
- 2026-09-06: библиотека лаборатории — девять коллекций по группам редактора с общей
  навигацией (01–02 существа, 03–05 объекты, 06–09 озеленение), лодка, кувшинки и
  водоросли как компоненты сцены, рыбы/чайки/камень на модулях продукта, значения из
  опубликованной сцены, `npm run check:lab-links`, единые правила `AGENTS.md`,
  `npm run handoff`. Затем огни танкера спрайтами (`src/tanker/lights.js`, три ключа
  редактора, файл сцены переопубликован пайплайном) и повтор карт лодки
  (`RepeatWrapping`: UV лодки тайлятся до 5.9, clamp размазывал доски).
  Затем прибой: рельеф в проходах оптики режется по волне заплеска (тёмная полоса у
  кромки ушла), пена и мокрый песок считаются аналитически из фазы волны. Затем сетка
  берега: зоны плотности рядов внутри полосы, юбки по глубине щели и без них в каркасе;
  рельеф и береговая вода вместе стали примерно вдвое легче по треугольникам; оптика
  рисует рельеф двойником в четверть треугольников; прибой считается только в полосе
  у воды, высота грунта из вершины — кадр на пляже с 28 до ~15 мс.
  Затем полировка редактора по `docs/editor-polish-plan.md` (17 пунктов Дениса):
  реестр объектов `lib/sceneObjects.js` (ведомость и переключатели из него),
  «Камеры» в корне, главная рабочая камера `work-main`, технические кадры,
  полёт на паузе, пробел, шапка «Ouroboros Engine 1.4.0» с кнопками
  «Откатить / В проект / На сайт», ⚙ настройки редактора, честный цвет лодки
  (опубликованный мигрирован пайплайном), узлы «Камни» и «Галька» с коллекцией
  10 в лаборатории. Дальше — лаборатория трав по `docs/grass-lab-plan.md`: ответы
  Дениса и решения записаны там же, промты для его текстур —
  `public/textures/grass/source/prompts.md`. 2026-09-07: карты Дениса собраны в атласы
  (`npm run grass:textures`), пучок `plants/grassModel.js` на общем конвейере,
  коллекция 10 «Травы» (камни — 11); трава в сцене (`plants/grassMeadow.js`,
  `CoastGrass.jsx`, ключи `grass*`, вкладка «Травы», кадры «Степь/Дюна/Камыш»);
  дальнее поле в шейдере рельефа (`plants/grassField.js`, плитки дёрна Дениса как
  слои массива, блок «Дальнее поле», притоптанный дёрн на тропах); `npm run check:grass`
  (детерминизм, ниши, бюджеты, атласы). По отзыву Дениса: единый свет лаборатории
  (`asset-lab/SceneLight.jsx`, переключатель Сцена/Студия в навигации), режим «Все»
  в травах, подложка `carpet` (карты под всем, ключ `grassCarpet`), блок «Цвет» суши
  (`terrainSaturation/Contrast/Brightness/Green/Dry`), тон травы. Дальше: отзыв
  Дениса по картинке, телефон, лёжки и память порыва.
  С 2026-09-06 Денис просит пушить каждый коммит (`git push origin HEAD:main`).
  2026-09-07 (дно): по картинке Дениса — дно шельфа: уклон шельфа за коленом
  (`terrainShelfSlope`, геометрический ключ), луга водорослей / ил / мидиевые
  банки как процедурное поле в координатах берега (`coastBedCover`, CPU-двойник
  `sampleSeabedCover`, три уже загруженных слоя массива с тонировкой, новых
  текстур нет), рябь на песке, цветение поверх лугов с рассеянием и линиями пены
  по ветру; песок под водой везде мокрый (раньше за 26 м рисовался сухим —
  белёсое дно и ступенька); пунктир на краю шельфа (−96 м) убран перекрытием
  дальней воды на полметра и общей зыбью полосы и дальней воды. Ползунки —
  «Ландшафт → Дно → Шельф Азова». Подробности — `TERRAIN.md` «The bed of the shelf».
  Известное: `scripts/check-water-surface-gpu.html` падает на пробе «left edge»
  и на нетронутом HEAD (страница старше четырёх правок шейдера) — не чинилось;
  далеко у горизонта на краю полосы остаются редкие светлые точки, были и раньше.
  Мерцание воды на дальних камерах (видео Дениса): не z-fighting, а алиасинг
  процедурных полос зыби и метровых шумов дна — при неподвижной камере кадры
  не меняются, при сдвиге на 3 px муар по всему морю; теперь косинусы зыби и
  шумы дна гасятся по размеру пикселя (`farWaterCrest`, `bedDetail`, см. `TERRAIN.md`).
  Последний коммит — см. `git log -1`; незакоммиченного нет.
- 2026-09-07, деревья (отдельная линия работы, параллельно траве): план по аналогам Дениса —
  `docs/tree-lab-plan.md` (виды, ниши, сухостой и валежник, порядок работ). Сделан шаг 1:
  `treeModel.js` растит виды параметрами формы (стволы, провис, кучерявость, веточки,
  сухостой) на втором потоке случайности — авторский лох seed 7 не сдвинулся (отпечаток
  в `check-trees.mjs`); таблица видов `plants/treeSpecies.js` (лох, гребенщик, алыча,
  вяз приземистый, ива, сухостой) на существующем атласе листа: тон листа, цвет коры и
  цветение в материалах и проекциях; коллекция 07 с переключателем видов. Сцена сажает
  по-прежнему только лох. Дальше по плану: отзыв Дениса по силуэтам, затем пни и валежник,
  затем ниши от рельефа и ключи видов в редакторе.
  Вечером 7 сентября запущен и остановлен (лимит времени Дениса) воркфлоу-аудит уровней
  детализации и света `wf_e425f2cd-8cf` (скрипт: `~/.claude/projects/-Users-denisdaragan-Documents-Not0-Not1-2026-DDG-buro-Syte/c519ac92-f0b0-4a24-a953-da6f87511122/workflows/scripts/tree-lod-light-audit-wf_e425f2cd-8cf.js`):
  шесть из семи читателей и большинство проверок готовы, выводы и приоритеты правок — в
  `docs/tree-lab-plan.md`, раздел «Аудит 7 сентября». Продолжить дешево: Workflow({scriptPath, resumeFromRunId:
  'wf_e425f2cd-8cf'}) — готовые агенты вернутся из кэша, доработает только читатель «свет карточек» и критик.
  По следу аудита уже исправлено: кора дальних карточек ×0.86 вместо ×1.5 (`PlantPopulation.jsx`).
  Вечером 7 сентября — паритет карточек: `plants/plantParity.js` калибрует карточку по
  геометрии под светом сцены (по кадрам, пять mip, по свету и против), нормаль карточки —
  из ракурса запекания, окружение растений как у рельефа, `treesTranslucency`, `p.lod??0`;
  проверка `npm run check:plants-parity`. Числа и остаток — в плане деревьев, раздел «Сделано 7 сентября, вечер».
  Ночью 7 сентября — кора Дениса (`assets-source/textures/bark`, `npm run bark:textures`, метрическая
  развёртка ветвей без спирали) и купольные кроны ивы, вяза и алычи (`crown:'dome'` в `treeModel.js`);
  раздел «Сделано 7 сентября, ночь» в плане деревьев. В сцене калибровка карточек сама отключается
  (`light:'invalid'`) — открытая нитка.
  8 сентября — деревья рассажены по нишам берега (`coastPlanting.js`: бровка, лесополоса, балки, тыл пляжа;
  виды по долям, сухостой), популяция на вид в `CoastTrees.jsx`, ключи и кадры во вкладке «Деревья»;
  раздел «Сделано 8 сентября» в плане деревьев. Дальше: пни и валежник, перетекание уровней, телефон.
- 2026-09-07, грунты обрыва (ветка `claude/azov-soils` из воркдерева `.claude/worktrees/azov-soils`,
  влита в `main` fast-forward'ом по слову Дениса поверх незакоммиченной работы агента деревьев). По референсам из папки
  «Грунты» обрыв перекрашен из рыжего в дневную жёлто-серую охру Таганрогского берега и
  разложен на ярусы по высоте стенки: гумусовая шапка с влажной полосой под дёрном, лёсс
  с тонкими пластами и бороздками стока (и в нормали), осыпь у подножия; свежий скол —
  пятнами на крутых гранях, «Выветривание» гасит его. Новое общее поле подножия
  `coastSoil` / `sampleCoastSoil` (языки осыпи на пляж лопастями, просачивание у
  подножия, у оползней и в устьях балок) читают материал, `surfaceAt().talus/.seep` и
  сухость растений. Конус оползня в геометрии перенесён к подножию и выходит на пляж
  (`terrainTalus` — геометрический ключ). Два ползунка в «Суша → Поверхность»: «Осыпь и
  языки грунта», «Пласты и потёки». По отзыву Дениса — блок «Бровка»: полоса дёрна
  вдоль кромки плато с фактурой плиток Дениса в полный вес и своим цветом
  (`terrainRimWidth`, `terrainRimColor`). Затем «Влажные островки»: зелень фактурой дёрна
  там, где копится влага и тень — просачивание у подножия, спокойная кромка за заплеском,
  ореолы кустов, деревьев и валунов (синий канал карты покрова, валуны добавлены как
  источники) — `terrainOasis`, `terrainOasisColor`; только материал, травяные пучки это
  поле пока не читают. Затем «Выброшенная тина» (`terrainWrack`): клочья водорослей по линии
  заплеска с параллаксом, старая бурая линия выше, кормятся лугами шельфа и штормом. Затем «Рыхлый песок» (`terrainLooseSand`): вмятины и
  ямки утоптанного сухого пляжа в нормали и параллаксе, розово-серые наносы, тёмные
  минеральные полосы у заплеска, штормовая линия ракушки, рябь на мокром песке.
  Всё влито в `main` по слову Дениса, незакоммиченного нет; воркдерево и ветка оставлены
  для продолжения. Дальше (по его отзыву в новом чате): ямки глубже и с чётким краем, если
  захочет; пучки травы, читающие поле сырости; корни из стенки и комья у подножия. Проверено: lint, build, `check:terrain`,
  `check:plants`, `scripts/check-terrain-gpu.html` (560 проб). Известное:
  `scripts/check-terrain-ui.mjs` ищет кнопки «Обвал/Спуск/Покров» в секции «Суша», а они
  переехали в «Камеры» — скрипт устарел, не чинился. Дальше: отзыв Дениса по тону и силе
  пластов; корни из стенки под дерновым козырьком и комья у подножия — геометрией
  (инстансы вдоль бровки) после одобрения шейдера; корни у стволов и валежник — у агента
  деревьев. Подробности — `TERRAIN.md` «The ground of the bluff».
- Открытые нитки: список «Что дальше» в `ASSET_LAB.md`; проверки по просьбе Дениса.
- Воркдеревья `.claude/worktrees/*`: `codex-azov-coast` слито в `main` 2026-09-05,
  остальные три — старые detached HEAD, можно удалять (`git worktree remove`).

## Что это

Главная страница `/` — 3D-сцена: пруд, лодка, скульптура-«голова», кувшинки, водоросли.
Редактор сцены — `/home/edit`. Стек: React + Vite + Three.js / react-three-fiber.
Запуск — через записи `.claude/launch.json`; карта портов и правила для агентов — в `AGENTS.md` (порты 41211–41215).
Хостинг GitHub Pages, деплой автоматом по push в `main`. Кнопка «На сайт» в редакторе
публикует сцену, коммитит один этот файл и пушит `HEAD:main` (`vite.config.js`,
`deploy: true` в `/__home-scene/publish`); «В проект» только пишет файл — этим путём
ходит смоук, и до сети он не дотягивается.

## Поток данных — три состояния, не путать

```
редактор → черновик (localStorage)  →  Publish  →  publishedHomeSceneSettings.js  →  главная
           ddg_home_scene_settings_v1            (файл в репозитории)
```

- **Черновик** живёт в браузере, пишется на каждое движение ползунка. Только редактор.
- **Publish** делает POST на дев-эндпоинт `/__home-scene/publish` (плагин в `vite.config.js`),
  который переписывает `publishedHomeSceneSettings.js`. **Работает только на localhost.**
- **Главная** читает опубликованный файл, черновик её не касается.

`publishedHomeSceneKeys.js` — список того, что вообще публикуется (сейчас 332 ключа).
Настройка, которой там нет, останется локальной: так живут `freeCamera`, `showPointerDebug`,
`showPerformanceHud`, `debugWireframe` — инструменты вьюпорта, а не свойства сцены.
Смоук-тест сверяет: каждый контрол редактора либо в этом списке, либо в его
`DEV_LOCAL_EDITOR_KEYS`. Забыть ключ нельзя — тест упадёт.

## Две среды: редактор Дениса и песочница агента

| Запись в `.claude/launch.json` | Порт | Чья |
|---|---|---|
| `site` | 41211 | Дениса, главная. |
| `editor` | 41212 | Дениса. Здесь его черновик. |
| `sandbox` | 41213 | Агента. Здесь можно ломать. |
| `dist` | 41214 | Превью собранного `dist/`. |
| `lab` | 41215 | Общая. Лаборатория ассетов, черновика сцены нет. |

Разные порты — разные origin, а значит **разные localStorage**. Эксперименты в
песочнице физически не могут задеть настройки Дениса: это не договорённость, а
свойство браузера.

Обмен идёт через опубликованный файл, а не через localStorage:

```
песочница → Publish → publishedHomeSceneSettings.js → редактор: «Откатить»
```

Кнопка «Откатить» двухшаговая — она затирает текущий черновик,
а в черновике может лежать несохранённый день работы.

**Правило для агента:** работать в песочнице (41213) или из свежего профиля
Playwright. В 41211/41212 заходить только
смотреть — не трогая ползунки. Один раз уже было: скролл над панелью незаметно
сдвинул количество кувшинок в его сцене.

`useHomeSceneSettings.js` — дефолты и **клампы**. Значение вне диапазона молча прижимается,
так что если ползунок «не действует до конца», сначала смотри кламп там.

## Сцена

`WaterScene.jsx` — сборка. Внутри `SceneCanvas.jsx` (WebGL-контекст, DPR, фолбэк без WebGL).
Модули в `src/components/effects/water/`:

| Модуль | За что отвечает |
|---|---|
| `useWaterRuntime.js` | GPU-симуляция воды: ping-pong текстуры высоты + нормаль-мапа |
| `WaterSurface.jsx` | Поверхность: displacement по height-field, Френель, отражение/преломление, штамп stencil |
| `Seabed.jsx` | Дно: рельеф, каустика, текстура |
| `WaterReflections.jsx` | Планарные отражение и преломление: пересъёмка сцены в текстуры |
| `WaterInteractionPlane.jsx` | Курсор → рябь. Рейкаст с уточнением по высоте волны |
| `WaterCameraRig.jsx` | Камера по авторской позе корзины; OrbitControls в редакторе |
| `WaterLights.jsx` | Свет и HDRI (пресет `night` зашит локально в `public/hdri/`) |
| `FloatingBoat.jsx` | Плавучесть по пяти поплавкам; stencil-вырез сухого кокпита |
| `StaticSculpture.jsx` | Скульптура на дне |
| `SurfaceVegetation.jsx` | Кувшинки |
| `UnderwaterAlgae.jsx` | Подводные водоросли |
| `pbrMaterial.js` | Конвенция PBR: цветовые пространства карт, шкала отражений среды |
| `useDragOnPlane.js` | Общий драг объекта по плоскости в редакторе |

Шейдеры — в `src/components/effects/shaders/`. Освещение считается один раз в
`homeSceneLighting.js` и раздаётся всем объектам как `lighting`; профиль качества
устройства — в `qualityProfile.js`.

**Вода — height-field симуляция, не Gerstner.** Деталь ряби живёт в нормаль-мапе,
не в полигонах. Лодка плавает по пяти поплавкам, читающим высоту из текстуры симуляции;
физика не зависит от меша модели.

## Лаборатория ассетов

`asset-lab.html` (запись `lab`, порт 41215, `npm run lab`) — белая сцена, где каждый
ассет смотрят до интеграции: существа (01 рыбы, 02 чайки), объекты (03 чёрный камень,
04 танкер, 05 лодка), озеленение (06 куст и 07 дерево лоха, 08 кувшинки, 09 водоросли).
Список — `src/asset-lab/assetCatalog.js`, навигация по группам на каждой странице.
Коллекции импортируют модули продукта и стартуют с опубликованной сцены, копий нет
(таблица «что откуда», `npm run check:lab-links` и задачи — в `ASSET_LAB.md`).
Сборка сайта её не включает.

## Берег: суша, растения, танкер

Ветка `codex/azov-coast` (Codex, сентябрь 2026) добавила вокруг пруда
процедурное азовское побережье; влита в `main` 5 сентября 2026 вместе с камерами
и облачным небом. Подробности — в `TERRAIN.md`, `src/plants/README.md`,
`src/tanker/README.md`; повторяемый подход — в `.agents/skills/`.

| Модуль | За что отвечает |
|---|---|
| `src/terrain/` | Аналитический берег: `terrainModel.js` — высота, нормаль, поверхность и рейкаст в береговых координатах (u поперёк, s вдоль); `AzovTerrain.jsx` — полосы рельефа с геоморфингом LOD, камни, ракушечник и прибрежные полосы воды (`WaterSurfaceV2` в `shoreMode`) |
| `src/plants/` | Лох узколистный: `oleasterModel.js` — куст и общие примитивы (`createPlantBuilder`), `treeModel.js` — взрослое дерево на обрыве, `PlantPopulation.jsx` — инстансы с тремя LOD и атласом проекций, `plantHabitat.js` — расстановка по `surfaceAt`, `coastPlanting.js` — полосы посадки кустов и рощи |
| `src/tanker/` | Речной танкер на горизонте: процедурная модель, погода, кильватер, звук |

Погода одна: `coastWeather(definition)` в `terrain/settings.js` отдаёт прибой,
пену, ветер и зыбь (`swell`, `swellBearing`); её читают растения, дальняя вода,
рябь пруда, танкер и шейдеры через `uCoastSwell`. Новая связь с ветром или штормом
добавляется туда, а не отдельным ползунком.

Вода и берег связаны в трёх местах: шейдер пруда получает `uShoreMode` и
аналитическую береговую волну (`terrainShader.js`), пробы поверхности для лодки и
птиц взвешиваются береговой линией (`applyCoastToProbes` в `useWaterRuntime.js`),
а прибрежные полосы рисуются тем же материалом, что и пруд.

Ключи `terrain*`, `shrubs*`, `trees*`, `tanker*` берутся из `DEFAULT_*_SETTINGS`
своих модулей и разворачиваются в `publishedHomeSceneKeys.js` — новая настройка
берега добавляется в дефолты модуля и его `*_RANGES`, остальное подхватывается.

## Освещение: Sky + Sun + Moon

Одна таблица рассеяния питает всё, что обязано быть согласованным: видимое небо,
небо в отражении воды, заполняющий свет на материалах и цвет ключевого света.
Они не могут разойтись — это одни и те же числа.

| Файл | Что делает |
|---|---|
| `effects/sky/skyModel.js` | Физика: дуга солнца, воздушная масса, рассеяние, таблица неба. Чистый JS, без three — запускается в node |
| `effects/sky/skyModel.check.js` | `npm run check:sky`. Проверяет модель числами, а не скриншотом |
| `effects/shaders/skyShader.js` | Общий GLSL: `skyRadiance()` и `celestialBody()`. Импортируется небом И водой |
| `effects/water/skyEnvironment.js` | Мост в three: DataTexture + PMREM в `scene.environment` |
| `effects/water/SkyDome.jsx` | Видимое небо: три вершины на дальней плоскости |
| `effects/homeSceneLighting.js` | Контракт: раздел `sky` плюс ключевой свет, разложенный на цвет и уровень |

**Ключевые правила системы:**

- **Цвет солнца никто не выбирает.** Он получается из ослабления в воздухе: низкое
  солнце краснеет потому, что его свет прошёл больше атмосферы. `sunTint` — это
  цвет солнца *над* атмосферой, почти белый; всё тёплое возникает само.
- **Время суток ведёт высоту, азимут остаётся авторским.** В полдень дуга
  возвращает ровно заданные направление и высоту, поэтому старые сцены открываются
  там же, где были (`sunBearing` ← `moonAzimuth`, `sunNoonElevation` ← `moonElevation`).
- **Облачность — один физический факт, а не два ползунка.** Она забирает энергию у
  пучка и отдаёт её куполу, поэтому тень слабеет и смягчается сама, а кадр не темнеет.
- **`directShare`** — доля прямого света; ровно столько тень имеет право убрать.
  Считается в той же петле, что и таблица.
- **Тень доходит до воды** одной аппаратной выборкой (`sampler2DShadow`), без второй
  карты теней. Умножает блик, искры, солнечную дорожку и рассеяние в толще —
  поэтому мутная вода показывает тень **сильнее**, а не стирает её.
- **HDRI вспомогательный.** `envMode`: `sky` (по умолчанию, ничего не грузится),
  `sky+hdri`, `hdri`.

**Светильники** — группа «Свет» в редакторе: два источника, у каждого тело и цель,
оба двигаются манипулятором как отдельные объекты (как Corona Light). Угол конуса
180° превращает прожектор в точечный источник. «Участвует в отражениях» — это слой,
а не флаг видимости: снятая галочка убирает из зеркала и сам источник, и его свет.
Тени они не отбрасывают намеренно — вторая карта теней самое дорогое, что могла бы
попросить эта функция.

План целиком — `docs/lighting-plan.md`.

Аудит воды, курсора, волн, пены, погоды и ветра с замерами и порядком работ —
`docs/water-audit-2026-09-05.md`.

**Не проверено:** теневая выборка в собственном шейдере на живом iPhone. На
десктопе компилируется; это доказательство только для десктопа. Проверка:
`npm run dev:lan`, на телефоне открыть `http://<адрес>:41211/?hud=1` — в HUD строка
`SHADOW`. Если сцена вместо картинки показывает экран «WebGL не поддерживается» —
шейдер не скомпилировался, и запасной путь описан в плане (Фаза 3, `BasicShadowMap`).

## Редактор

Панель — дерево сцены в три уровня, описанное декларативно в
`components/editor/editorTree.js`: **группа → узел → аспекты**.
Группы: Ландшафт, Озеленение, Объекты, Атмосфера, Рендер, Интерфейс.
Выбор хранится одной строкой `"группа/узел"`. Узел с `devOnly: true` виден только в dev.

Контролы живут в `components/editor/sections/*.jsx` — по файлу на группу. Внутри длинной
секции блоки разделяются `<SectionHeading subtle />`: та же черта, что у заголовка аспекта,
но тише, чтобы вложенность читалась.

Добавить настройку — пять мест, все обязательны:
1. дефолт в `getBaseHomeSceneSettings`
2. кламп в `normalizeHomeSceneSettings`
3. ключ в `publishedHomeSceneKeys.js` (или в `DEV_LOCAL_EDITOR_KEYS` смоука, если она локальная)
4. контрол в нужной секции
5. подписи RU + EN в `i18n/translations.js`

**Рендер → Видимость** — общие выключатели: вода, дно, кувшинки, водоросли, лодка,
скульптура, отражения. Тени, постобработка и лучи переключаются в своих разделах,
здесь не дублируются.

**Рендер → Дебаг** (только dev): свободная камера, каркас (wireframe), отладка курсора,
FPS/память, режим отладки.

## Композиция: две корзины

`lib/layout.js`. Ровно две — `desktop` и `portrait`, обе с кадром 16:9; различается только
авторская поза камеры. Выбор по аспекту окна: `< 1` → portrait. Некастомизированная корзина
наследует desktop. Каждая хранит `cameraPosition/Target`, `cameraFov`, `frameInset`,
`boatPosition`, `sculpturePosition`.

## Ассеты

`public/` содержит **только то, что запрашивает рантайм**. Всё, из чего это собрано,
лежит в `assets-source/` и в деплой не уезжает:

- модели — `assets-source/models/*.obj` → `npm run convert:models` → `public/models/*.glb`
- кувшинки — `assets-source/lily/lily-atlas-source.png` → `npm run generate:lily`
  (`scripts/generate-lily-pbr.mjs`) → промежуточные PNG там же, а в `public/textures/lily/`
  кладутся три webp-атласа, которые грузит сцена

Модели раздаются в GLB, не в OBJ: текстовый OBJ весил 10.4 МБ на две модели и держал
показ сцены, пока браузер его разбирал. Конвертер режет меш по группам материалов
(корпус — один меш с чередованием дерево/металл), сваривает вершины каждой части
отдельно и сохраняет **имена материалов** — лодка выбирает металл по имени
`OBJ_wire_metall`, так что переименование молча покрасит уключины деревом.

## Грабли

- **Небо считается на процессоре один раз при загрузке.** Правка, которая заставит
  его пересчитываться каждый кадр, съест телефон. Ключ пересборки — `skyKey` в
  `skyEnvironment.js`, и он намеренно завязан на входы неба, а не на объект настроек.
- **Нормаль-мапа воды — не геометрический уклон.** Она усилена `normalStrength` ради
  освещения ряби. Строить по ней наклон объекта нельзя: кувшинки от этого встают дыбом.
  Наклон и высоту бери из height-field.
- **Вода непрозрачна и пишет глубину.** Всё, что оказалось ниже её поверхности, вырезается
  depth-тестом. Чтобы объект просвечивал сквозь воду, он должен попасть в проход преломления
  (`WaterReflections`) — и только подводной своей частью, иначе получишь его в кадре дважды
  и тёмную кайму. Глобальная плоскость отсечения так резать по волне не умеет и на кастомные
  шейдеры вообще не действует: рез делает сам шейдер объекта.
- **HMR ломает проходы отражений.** Правка модулей сцены может оставить чёрную воду или
  пустое преломление. Это артефакт разработки — лечится перезагрузкой страницы, к коду
  отношения не имеет. Прежде чем чинить «поломку», перезагрузи.
- **Скриншот из песочницы — плохой судья.** Проверять визуал лучше в браузере пользователя.
  Проверка кода = чистая компиляция + пустая консоль.
- **Постобработка включена** в опубликованной сцене. Без неё туман и лучи не
  работали вовсе — они живут только в этом проходе. Включить её удалось лишь после
  того, как в пост вернули тонмаппинг: three выкомпиливает его для рендера в
  текстуру, а весь кадр рисуется именно туда, поэтому тонмаппинга не было ни на
  одном пути. Рендерер теперь ACES Filmic.
- **Лучи видны, только когда солнце в кадре.** Обе авторские камеры смотрят вниз,
  поэтому при высоком солнце лучей не будет — это не поломка, а геометрия.
- **У береговой волны два тела.** `coastWave` в `terrain/terrainShader.js` (GPU: вода,
  пена, мокрый песок) и `sampleCoastWave` в `terrain/terrainModel.js` (CPU: лодка, пробы,
  посадка) обязаны быть одной формулой. Правишь одну — правь вторую, иначе лодка
  поплывёт не по той воде, что нарисована. Шум в волне поэтому не хэш, а суммы синусов.
  Та же пара у огибающей и мокрого песка: `coastWaveGain`/`coastWetnessAtHeight` и
  `sampleCoastWaveGain`/`sampleCoastWetness`.
- **Прибой без памяти.** Пена на песке не хранит историю: `coastSwashAt` считает
  аналитически, сколько секунд назад вода ушла с точки (арксинус огибающей плюс шаг
  Ньютона по скошенной волне) и где линия максимального заплеска. Поэтому телефон и
  десктоп рисуют одну и ту же пену. Кромка воды рваная одной функцией `coastEdgeRag`,
  на неё завязаны и отсечка воды, и кружево, и вырез рельефа в оптике.
- **«Рёбра под землёй» — юбки.** У каждой полосы рельефа (64 м вдоль берега) по краям
  висят вертикальные шторки: они прячут щели между соседними полосами разной
  детализации. Теперь они висят на глубину щели (0,45 м на ровном, до 5,6 м на обрыве),
  каркасный режим их не рисует (`drawRange` по `userData.topTriangles`), и внутри полосы
  три зоны сетки (шельф ¼ рядов, берег с обрывом полные ряды, плато ¼) со своими
  короткими юбками на стыках. Морф LOD считается по зонам, поэтому проверка
  `check:terrain` требует, чтобы счётчики колонок в полосах делились на два на каждый
  уровень, а срез плато (`plateauCut`) был один на весь берег.
- **Берег дорог пикселями, не треугольниками.** На камере у воды выключение рельефа
  снимало 19 мс из 28 при том, что рельеф это ~170k треугольников. Кадр держал
  фрагментный шейдер песка на весь экран: `coastHeight` (сорок трансцендентных) и
  прибой считались на каждый пиксель. Теперь высота грунта берётся из вершины, а
  прибой и мокрый песок считаются только в полосе q −26..12 м (в береговой воде пена
  тоже). Проходы оптики рисуют рельеф «двойником» на тех же буферах с четвертью
  треугольников. Любая новая функция в шейдере берега — сначала спросить, нужна ли
  она вне полосы у воды.
- **Рельеф режет себя в проходах оптики сам.** Общая плоскость отсечения на уровне
  спокойной воды не видит заплеск: песок под набегающей плёнкой не попадал в захват
  преломления, и вода у кромки показывала тёмное «тело» вместо песка. Теперь
  `uTerrainOptics` (1 преломление, 2 отражение) в `terrainMaterial.js` режет по
  `coastWave`, а чанк `clipping_planes_fragment` у рельефа заменён — глобальные
  плоскости на него больше не действуют.
- **Публикация только на localhost.** На задеплоенном сайте кнопка Publish неактивна.
- **Опубликованный файл — художественная работа Дениса.** Не откатывать и не переписывать
  руками.

## Проверки

| Команда | Что делает |
|---|---|
| `npm run lint` | ESLint |
| `npm run build` | Сборка |
| `npm run check:bundle` | Размеры и состав чанков по манифесту |
| `npm run check:sky` | Модель неба: дуга солнца, покраснение, энергия облачности |
| `npm run convert:models` | Пересобрать GLB из исходных OBJ |
| `npm run dev:lan` | Dev-сервер, видимый с телефона в той же сети |
| `npm run check:plants` | Куст и дерево: детерминизм, крепления, LOD-бюджеты, посадка на берегу |
| `npm run check:plants-parity` | Паритет трёх уровней растений в живой лаборатории (Playwright, нужен сервер `lab` или `PLANT_LAB_URL`): яркость карточки против геометрии на трёх дистанциях |
| `npm run check:terrain` | Рельеф: контракты высоты/нормали, контакт растений с грунтом |
| `npm run check:coast-contracts` | Стык воды и берега, таймлайн сцены, дальние чайки, LOD оптики |
| `npm run smoke` | Playwright: маршруты, WebGL-фолбэк, аудио, миграция черновика, покрытие publish-ключей, публикация end-to-end, память в долгой сессии, мобилка |

CI: `checks.yml` гоняет всё это на push и PR. `deploy-pages.yml` перед публикацией
прогоняет только быстрые детерминированные проверки (lint, build, check:bundle) —
браузерный смоук намеренно не может заблокировать деплой сайта.

## Замер на реальном устройстве

Профиль качества для слабых устройств написан, но на живом телефоне не проверялся —
эмуляция размера окна не эмулирует мощность GPU (в песочнице всё равно останется
десктопная видеокарта).

1. `npm run dev:lan` — Vite слушает на всех интерфейсах и печатает адрес в сети.
2. На телефоне в той же сети открыть `http://<адрес>:41211/?hud=1`.
3. `?hud=1` включает HUD прямо на главной (в dev-сборке): FPS, p95 кадра, память,
   треугольники, эффективное разрешение симуляции. Переключатель в панели редактора
   для этого не нужен — панелью на телефоне не поуправляешь.

Для сравнения, десктоп на M4 Max: 120 fps, 8.3 мс средний кадр, p99 10.8 мс,
42 вызова отрисовки, 852k треугольников — то есть упирается в частоту экрана.

**Смоуку нужна машина целиком** — перед запуском останови дев-серверы, иначе получишь
ложные падения из-за конкуренции за GPU.
