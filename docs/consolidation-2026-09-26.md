# Объединение работ — 26 сентября 2026

По просьбе Дениса объединены текущие разработки и сохранены отдельные исторические варианты. База GitHub: `ed04fbf`. Сборка объединения: ветка `codex/publish-all-2026-09-26` в `infallible-diffie-462766` (на этот чекаут настроен установленный OUROBOROS).

## Что входит в общую версию

- Материалы: Pinterest, процедурные покрытия и фальц, параллакс, библиотека, просмотр по пробелу, выбор граней по B/Shift и общая проекция модели; фоторендер.
- Последний почвопокров и мох (`codex/groundcover`, merge `93eecca`).
- Ограждения вдоль нарисованных линий (`codex/rostov-fence`, `8968427`).
- Огонь по сплайну, дым и лаборатория №20 (`claude/fire-spline-smoke-effect-320783`, `1783a65`). Нумерация существующих коллекций сохранена.
- HTML-альбом проекта (`claude/html-minimalist-album-0c6df5`, `d8ade4c`).
- CMS, контентный манифест и локальный резервный источник портфолио (`codex/content-platform`, `7437807`). Это публикация исходников, отдельный CMS-сервис на сервере не разворачивался.
- Редактор портфолио, список с превью и экран загрузки (`codex/site-layout-design`, `aa9ef10`). Сохранены текущие RU/EN маршруты, звук, проекты движка и редактор.
- Сохранены уникальные старые исследования интерфейса и обзор движка из исходного чекаута.

При интеграции восстановлены совместимые импорты, реестры, подсказки RU/EN, конфигурация Vite и проверка динамического чанка HomeEdit. Справочник пересобран из живого редактора: 800 параметров / 49 разделов. Исправлен типизированный ответ CMS. Проверка огня теперь использует заводской рельеф как фиксированную тестовую поверхность, а не изменяемую авторскую сцену.

На главной странице воспроизведено превышение лимита 16 текстур в шейдере дома: каждый материал удерживал сразу три комплекта карт. Материалы теперь используют три общих sampler-имени со своим комплектом дерева, черепицы или металла. Проверка геометрии подтверждает соответствие комплектов всем вариантам дома; повторная отрисовка прошла без ошибки WebGL.

## История без отката текущего продукта

Эти варианты сохраняются на GitHub отдельными ветками, их старый код и настройки не накладываются поверх нового:

| Ветка | Сохранённая работа |
| --- | --- |
| `codex/drawn-topiary` (`a6b308c`) | Исходные незакоммиченные разработки изгородей, локальные примеры и исследования интерфейса; актуальная реализация уже перенесена в общую ветку ранее. |
| `codex/azov-coast` (`e44c04e`) | Старый авторский вариант сцены Азова. |
| `codex/painterly-clouds` (`ae59cfe`) | Ранний прототип облаков; более поздняя реализация уже находится в main. |
| `codex/archive-local-main-2026-09-26` | История старого локального репозитория (`remotes/local/main`), не являющаяся продолжением текущего main. |

Незакоммиченные изменения CMS (`e4eba61`) и конфигурация отдельного редактора портфолио (`79eecb7`) предварительно сохранены и затем влиты. Stash отсутствует. Авторский `publishedHomeSceneSettings.js` совпадает с исходным GitHub main; проекты Ростова, камеры и пользовательская библиотека этой задачей не редактировались.

## Проверка

Пройдены lint, production build, check:bundle, check:ai-materials, check:render-resources, check:fire, check:editor, check:cameras, check:lab-links, check:path-trace, check:house, groundcover.check.js и test:content. В CMS прошли lint:config и пять тестов. Остаётся существующее предупреждение react-refresh в CoastRocksPBR и предупреждение размера чанка сборки.

В свежем браузерном профиле на sandbox 41213 проверены редактор, открытие материалов по B, портфолио с превью, английские ссылки, выход экрана загрузки и главная сцена; в лаборатории огня видны активные пламя и дым. Ошибок WebGL после исправления дома нет. Проверка визуала не означает художественного принятия огня или всех исторических прототипов. Полный smoke локально не запускался; его состояние следует смотреть в GitHub Actions.

Временная среда проверки изолирована от пользовательских проектов. Итоговые исходники предназначены для main в корневом чекауте и на GitHub; установленный OUROBOROS следует перезапустить, чтобы сервер и интерфейс загрузили согласованную версию.
